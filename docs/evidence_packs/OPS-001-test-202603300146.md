# EvidencePack

Task ID: OPS-001
Role: test
Date: 2026-03-30T01:50:00+08:00
Acceptance Gate: G1
Conclusion: pass

## Env Snapshot

- Link: D:\xtquant-mcp\repo\docs\env_snapshots\OPS-001-test-202603300146.md
- Host: CHIYU (Windows 11)
- Shell: PowerShell 7
- Config:
  - D:\xtquant-mcp\instance\prod\config\data_gateway.local.yaml
  - D:\xtquant-mcp\instance\prod\config\trade_gateway.local.yaml

## Test Scope

1. 独立复核 `G0/G1` 的进程、端口和真实 gateway 运行态。
2. 按 `docs/OPS-001_ARTIFACT_DISPOSITION.md` 对已知历史污染 artifact 执行最小必要隔离演练。
3. 复核 `/healthz` 的 `evidence_scope`、`evidence_state_root`、`evidence_artifact_root`。
4. 复核状态读取不会把已知 fake 污染当作正式证据。

## Commands

1. 基础就绪检查
   - `Get-Process XtMiniQmt -ErrorAction SilentlyContinue | Select-Object Id,ProcessName,StartTime,Path`
   - `Test-NetConnection -ComputerName 127.0.0.1 -Port 58610`
   - `Test-NetConnection -ComputerName 127.0.0.1 -Port 8765`
   - `Test-NetConnection -ComputerName 127.0.0.1 -Port 8766`
2. 污染识别与读取拦截复核（隔离前）
   - `Get-Content D:\xtquant-mcp\instance\prod\state\data_resources\xtdata_service_status.json`
   - `Select-String ... data_gateway_calls.jsonl ... source=fake`
   - `python -c` 等价脚本调用 `read_data_resource(cfg, "xtdata://service/status")`
3. 最小隔离演练
   - 将以下文件移动到 quarantine：
     - `D:\xtquant-mcp\instance\prod\state\data_resources\xtdata_service_status.json`
     - `D:\xtquant-mcp\instance\prod\artifacts\data_gateway\20260327\data_gateway_calls.jsonl`
   - 生成 `D:\xtquant-mcp\instance\prod\artifacts\quarantine\202603300146\manifest.json`
4. 真实 gateway 启动与健康检查
   - `scripts\wake_data_gateway.ps1 ...`
   - `scripts\wake_trade_gateway.ps1 ...`
   - `Invoke-RestMethod http://127.0.0.1:8766/healthz`
   - `Invoke-RestMethod http://127.0.0.1:8765/healthz`
5. MCP 调用复核
   - `POST http://127.0.0.1:8766/mcp` -> `tools/call xtdata.status`
   - `POST http://127.0.0.1:8766/mcp` -> `resources/read xtdata://service/status`
   - `POST http://127.0.0.1:8765/mcp` -> `tools/call session.status`
   - `POST http://127.0.0.1:8765/mcp` -> `resources/read trade://session/current`

## Raw Results

- Pre-check:
  - `XtMiniQmt` 进程存在。
  - `127.0.0.1:58610` 可达。
  - `8765/8766` 在启动前不可达。
- Known historical contamination confirmed:
  - `xtdata_service_status.json` 内容包含 `"source": "fake"`。
  - 历史 `data_gateway_calls.jsonl` 命中 fake 行。
- Anti-fake read behavior confirmed:
  - 隔离前调用 `read_data_resource(cfg, "xtdata://service/status")` 返回
    `{"available": false, "reason": "fake_payload_blocked", "state_scope": "prod", "source": "fake"}`。
- Quarantine result:
  - moved_count=2，原路径文件不存在，quarantine 目录保留原文件与 `manifest.json`。
- Gateway health after wake:
  - data/trade `/healthz` 均 `ok=true`。
  - 两端均返回：
    - `evidence_scope: "prod"`
    - `evidence_state_root: "D:\\xtquant-mcp\\instance\\prod\\state"`
    - `evidence_artifact_root: "D:\\xtquant-mcp\\instance\\prod\\artifacts"`
- MCP and audit evidence:
  - `xtdata.status` 返回 `ready=true`，`source` 为 runtime/connectivity 语义，不是 fake。
  - 新 call log 记录包含 `trace_id`、`server_ts`、`evidence_scope=prod`、`state_root`。
  - 代表性 trace:
    - data: `3cf09e3b-5a69-460e-bb12-cac156615b3b`
    - trade: `f085ba41-4ba4-4449-b7e6-2be63e3a6b2e`

## Artifact Refs

- Quarantine Root:
  - D:\xtquant-mcp\instance\prod\artifacts\quarantine\202603300146
- Quarantine Manifest:
  - D:\xtquant-mcp\instance\prod\artifacts\quarantine\202603300146\manifest.json
- Quarantined Files:
  - D:\xtquant-mcp\instance\prod\artifacts\quarantine\202603300146\state\data_resources\xtdata_service_status.json
  - D:\xtquant-mcp\instance\prod\artifacts\quarantine\202603300146\artifacts\data_gateway\20260327\data_gateway_calls.jsonl
- Fresh Call Logs:
  - D:\xtquant-mcp\instance\prod\artifacts\data_gateway\20260330\data_gateway_calls.jsonl
  - D:\xtquant-mcp\instance\prod\artifacts\trade_gateway\20260330\trade_gateway_calls.jsonl
- Fresh Gateway Logs:
  - D:\xtquant-mcp\instance\prod\logs\data_gateway\data_gateway_20260330_014821.log
  - D:\xtquant-mcp\instance\prod\logs\trade_gateway\trade_gateway_20260330_014822.log

## Failure Classification

- fail_env: none in this run.
- fail_design: none in this run.
- blocked: none in this run.

## Verdict

`pass`（仅针对 OPS-001 本次独立测试范围：`G0/G1`）。  
本次证据表明历史 fake 污染已完成最小隔离并可追溯，真实 prod gateway 运行态可复核且 `evidence_*` 字段一致，状态读取未将已知 fake 污染当作正式证据。

## Risk Notes

1. 本次仅隔离已识别样本（2 个文件）；若存在其他未识别历史污染，仍需按同一策略继续巡检。
2. `session.status` 当前为 `session_not_ready`，不影响 OPS-001 的 `G0/G1` 结论，但不应外推为 `G3` 通过。
3. 本结论不等于审查放行或跨卡结论。

## Board Suggestion

- Suggested Status: `In Independent Test` -> 可提交 `In Review`
- Suggested Blocking Reason: `<empty>`
- Suggested Release Wording: 本卡独立测试 `pass`，待 review 判定是否从任务层面放行。

禁止自动进入下一卡。
