# EvidencePack

Task ID: VAL-003
Role: test
Date: 2026-03-31T23:06:35.5710980+08:00
Acceptance Gate: G4
Conclusion: blocked

## Env Snapshot

- Link: [VAL-003-202603312305.md](../env_snapshots/VAL-003-202603312305.md)

## Scope

1. Gather fresh runtime preflight evidence on the current host for `VAL-003/G4`.
2. Decide whether the governed write path is executable right now under repo rules.
3. Stop before `order.place` if any `G4` hard stop applies.

## Go/No-Go Packet

- side: `BUY`
- symbol: `515880.SH`
- qty: `100`
- price_mode: `l1_protect`
- cancel_timeout: `30s`
- execution result in this run: `NO-GO`
- `order.place` executed: `no`

## Commands

1. Context and rule alignment:
   - `Get-Content docs\task_cards\VAL-003.md`
   - `Get-Content docs\ACCEPTANCE_STANDARD.md`
   - `Get-Content docs\OPERATIONS_RUNBOOK.md`
   - `Get-Content docs\CURRENT_STATUS.md`
   - `Get-Content docs\evidence_packs\README.md`
   - `Get-Content docs\env_snapshots\README.md`
2. Fresh host preflight:
   - `Get-Date -Format o`
   - `Get-Process XtMiniQmt,miniquote -ErrorAction SilentlyContinue | Select-Object ProcessName,Id,StartTime,Path`
   - `Test-NetConnection -ComputerName 127.0.0.1 -Port 58610 | Select-Object ComputerName,RemotePort,TcpTestSucceeded`
   - `Test-NetConnection -ComputerName 127.0.0.1 -Port 8765 | Select-Object ComputerName,RemotePort,TcpTestSucceeded`
   - `Test-NetConnection -ComputerName 127.0.0.1 -Port 8766 | Select-Object ComputerName,RemotePort,TcpTestSucceeded`
   - `Invoke-RestMethod -Uri http://127.0.0.1:8765/healthz | ConvertTo-Json -Depth 8`
   - `Invoke-RestMethod -Uri http://127.0.0.1:8766/healthz | ConvertTo-Json -Depth 8`
   - `netstat -ano | Select-String ':8765'`
   - `netstat -ano | Select-String ':8766'`
   - `Get-Process -Id 50404 | Select-Object Id,ProcessName,StartTime,Path`
   - `Get-Process -Id 46732 | Select-Object Id,ProcessName,StartTime,Path`
3. Fresh runtime probe:
   - inline `D:\xtquant-mcp\venv313\Scripts\python.exe` HTTP client against `http://127.0.0.1:8765/mcp`
   - `initialize`
   - `tools/call probe.connection`
   - `resources/read diag://probe/latest`
4. Artifact back-link and environment metadata:
   - `Select-String -Path D:\xtquant-mcp\instance\prod\artifacts\trade_gateway\20260331\trade_gateway_calls.jsonl -Pattern 'b071ee46-7770-469e-a39f-7fae8ca38873'`
   - `hostname`
   - `$PSVersionTable.PSVersion.ToString()`
   - `git rev-parse HEAD`

## Raw Results

- Formal posture before runtime checks:
  - [VAL-003 task card](../task_cards/VAL-003.md) still records `Status: Blocked` and `Blocking Reason: broker_blocked`.
  - [CURRENT_STATUS.md](../CURRENT_STATUS.md) still lists `VAL-003` as the only remaining `Trade Lane Write` item and keeps it at `Blocked / broker_blocked`.
  - [OPERATIONS_RUNBOOK.md](../OPERATIONS_RUNBOOK.md) states that if the target task is `VAL-003` and it is still `Blocked / broker_blocked`, the operator must stop in the blocker-recovery branch and must not execute `order.place`.

- Fresh wall clock and host facts:
  - current Beijing time at preflight start: `2026-03-31T23:02:56.2308520+08:00`
  - snapshot recording time: `2026-03-31T23:06:35.5710980+08:00`
  - host: `CHIYU`
  - shell: `PowerShell 7.6.0`

- Fresh process and port facts:
  - `XtMiniQmt.exe` present:
    - pid `27152`
    - start `2026-03-31 16:31:43`
    - path `D:\lh\国金证券QMT交易端\bin.x64\XtMiniQmt.exe`
  - `miniquote.exe` present:
    - pid `28824`
    - start `2026-03-31 16:31:44`
    - path `D:\lh\国金证券QMT交易端\bin.x64\miniquote.exe`
  - `127.0.0.1:58610` -> `TcpTestSucceeded=True`
  - `127.0.0.1:8765` -> `TcpTestSucceeded=True`
  - `127.0.0.1:8766` -> `TcpTestSucceeded=True`
  - current listeners:
    - `127.0.0.1:8765 LISTENING 50404`
    - `127.0.0.1:8766 LISTENING 46732`
  - gateway processes:
    - trade gateway listener pid `50404`, process `python`, start `2026-03-31 19:14:15`, path `C:\Python313\python.exe`
    - data gateway listener pid `46732`, process `python`, start `2026-03-31 16:51:59`, path `C:\Python313\python.exe`

- Fresh `/healthz` facts:
  - trade gateway `/healthz`:
    - `ok=true`
    - `server_name=xtqmtTradeGateway`
    - `bind_port=8765`
    - `enabled_tools` includes `order.place`, `order.status`, `order.cancel`, `orders.list`, and `fills.list`
    - `account_contract=single_account_primary`
    - `account_input_mode=service_context_only`
    - `evidence_scope=prod`
  - data gateway `/healthz`:
    - `ok=true`
    - `server_name=xtqmtDataGateway`
    - `bind_port=8766`
    - `evidence_scope=prod`

- Fresh trade preflight probe:
  - `probe.connection` at `server_ts=2026-03-31T23:04:26`
  - `ok=true`
  - `reason=ok`
  - `session_id=1111`
  - `read_only_ready=true`
  - `write_permission_ready=true`
  - `write_permission_blocked=false`
  - `fresh_connect_attempted=false`
  - `fresh_connect_pass=false`
  - `precheck.userdata_precheck_write_permission.up_queue_xtquant_exists=true`
  - `write_permission_probe.implies_write_permission=false`
  - `probe_scope_note=probe.connection 的只读 readiness 可复用 active owner-managed shadow session；write_permission 仍仅来自独立预检，不代表 public orders.list 或写权限已就绪`
  - `trace_id=b071ee46-7770-469e-a39f-7fae8ca38873`
  - artifact back-link:
    - `D:\xtquant-mcp\instance\prod\artifacts\trade_gateway\20260331\trade_gateway_calls.jsonl` line `154`
    - `D:\xtquant-mcp\instance\prod\artifacts\trade_probe_latest.json`
  - `diag://probe/latest` returned the same runtime shape and timestamp.

- Additional environment observations:
  - latest trade logs in this window:
    - `D:\xtquant-mcp\instance\prod\logs\trade_gateway\trade_gateway_20260331_191415.log`
    - `D:\xtquant-mcp\instance\prod\logs\trade_gateway\trade_gateway_20260331_191415.stderr.log`
  - latest data logs in this window:
    - `D:\xtquant-mcp\instance\prod\logs\data_gateway\data_gateway_20260331_165159.log`
    - `D:\xtquant-mcp\instance\prod\logs\data_gateway\data_gateway_20260331_165159.stderr.log`
  - `git rev-parse HEAD` failed with `fatal: not a git repository (or any of the parent directories): .git`

## G4 Hard-Stop Assessment

1. Hard stop: current task still `Blocked / broker_blocked`.
   - This is explicitly recorded in the current [task card](../task_cards/VAL-003.md), [status snapshot](../CURRENT_STATUS.md), and [operations runbook](../OPERATIONS_RUNBOOK.md).
   - Under repo rules, this alone is sufficient to stop before `order.place`.

2. Hard stop: `market_window_closed` applies right now.
   - This preflight was executed at `2026-03-31T23:02:56.2308520+08:00` through `2026-03-31T23:06:35.5710980+08:00` Beijing time.
   - That wall-clock window is outside the live CN A-share trading session required for `VAL-003/G4`.
   - Per [ACCEPTANCE_STANDARD.md](../ACCEPTANCE_STANDARD.md) and [OPERATIONS_RUNBOOK.md](../OPERATIONS_RUNBOOK.md), `market_window_closed` is a hard stop and `order.place` must not be called.

3. Fresh probe does not override the hard stops.
   - `write_permission_ready=true` only proves that the current write-permission precheck did not fail.
   - The same payload states `write_permission_probe.implies_write_permission=false`.
   - The runbook explicitly says `probe.connection.write_permission_ready=true` is not sufficient to claim `G4 ready`.

## Design and Environment Separation

- Environment-side blocker observed in this run:
  - current wall clock is outside the trading window, so `market_window_closed` blocks any live `G4` write attempt right now.
- Governance/control-plane blocker observed in this run:
  - `VAL-003` still carries the formal `Blocked / broker_blocked` posture, so this run cannot lawfully start the governed-write chain.
- Design-side note:
  - this preflight did not collect any new evidence that would justify reclassifying the no-go as `fail_design`.
  - it also did not gather the independent higher-gate closure artifacts needed to remove the existing `VAL-003` block.

## Execution Boundary

- `order.place` was not executed.
- Because `order.place` was not executed, the governed write chain was intentionally not started:
  - `order.status` for the requested packet was not executed
  - `orders.list` for write-followup on this packet was not executed
  - `order.cancel` was not executed
  - `fills.list` for this packet was not executed

## Verdict

`blocked`. Fresh runtime preflight on the current host shows that MiniQMT, xtdata, and both gateways are up, and the trade-side probe can still return `read_only_ready=true` plus a non-failing write precheck. That does not make the governed write path executable now. Two independent `G4` hard stops still apply in this same window: `VAL-003` remains formally `Blocked / broker_blocked`, and the preflight was run at `2026-03-31 23:02-23:06 +08:00`, so `market_window_closed` applies. Under repo rules the correct action is to stop before `order.place`, record a no-go packet, and keep the task conclusion at `blocked`.
