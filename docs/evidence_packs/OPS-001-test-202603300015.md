# EvidencePack

Task ID: OPS-001
Role: test
Date: 2026-03-30T00:13:00+08:00
Acceptance Gate: G0
Conclusion: partial

## Env Snapshot

- Link: D:\xtquant-mcp\repo\docs\env_snapshots\OPS-001-test-202603300015.md
- Host: CHIYU (Windows 11)
- Shell: PowerShell 7.6.0
- Config:
  - D:\xtquant-mcp\repo\docs\task_cards\OPS-001.md
  - D:\xtquant-mcp\repo\docs\change_packages\OPS-001.md

## Test Scope

1. 独立验证 fake/test 状态与 prod 证据隔离是否生效。
2. 验证 server tests 不再写入 `instance/prod`。
3. 验证 prod-scope resource 对 `source=fake` payload 的阻断。
4. 验证 `/healthz` 暴露 `evidence_scope`、`evidence_state_root`、`evidence_artifact_root`。

## Commands

1. `$tmp='D:\xtquant-mcp\instance\test_tmp\OPS-001-test'; New-Item -ItemType Directory -Force -Path $tmp | Out-Null; $env:TEMP=$tmp; $env:TMP=$tmp; D:\xtquant-mcp\venv313\Scripts\python.exe -m unittest -v tests.test_data_gateway_server tests.test_trade_gateway_server`
2. `$tmp='D:\xtquant-mcp\instance\test_tmp\OPS-001-test'; New-Item -ItemType Directory -Force -Path $tmp | Out-Null; $env:TEMP=$tmp; $env:TMP=$tmp; D:\xtquant-mcp\venv313\Scripts\python.exe -m unittest -v tests.test_data_gateway_service tests.test_trade_gateway_config`
3. `$tmp='D:\xtquant-mcp\instance\test_tmp\OPS-001-test'; New-Item -ItemType Directory -Force -Path $tmp | Out-Null; $env:TEMP=$tmp; $env:TMP=$tmp; @'...prod fake payload check...'@ | D:\xtquant-mcp\venv313\Scripts\python.exe -`

## Raw Results

- Unit tests group A:

```text
test_health_payload_exposes_non_prod_evidence_scope ... ok
test_initialize ... ok
test_read_data_resource_blocks_fake_payload_in_prod_scope ... ok
test_status_tool_call ... ok
test_tools_list_contains_status ... ok
test_initialize_login_resource_and_prompt ... ok
test_read_trade_resource_blocks_fake_payload_in_prod_scope ... ok

Ran 7 tests in 0.013s
OK
```

- Unit tests group B:

```text
test_status_search_history_download_and_subscription ... ok
test_load_example_config ... ok

Ran 2 tests in 0.069s
OK
```

- Prod fake payload targeted check:

```text
{'available': False, 'reason': 'fake_payload_blocked', 'state_scope': 'prod', 'source': 'fake'}
```

## Artifact Refs

- EvidencePack: `D:\xtquant-mcp\repo\docs\evidence_packs\OPS-001-test-202603300015.md`
- EnvSnapshot: `D:\xtquant-mcp\repo\docs\env_snapshots\OPS-001-test-202603300015.md`
- Target fake payload file: `D:\xtquant-mcp\instance\prod\state\data_resources\xtdata_service_status.json`

## Failure Classification

- fail_env: none observed in this run.
- fail_design: none observed in this tested scope.

## Verdict

`partial`。本次独立测试在代码级和定向核查上通过，确认隔离与拦截机制生效；但尚未包含真实 gateway 进程下的端到端 `G0/G1` 运行态复核，因此不宣称放行。
