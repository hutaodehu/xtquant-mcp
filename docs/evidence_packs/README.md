# EvidencePack 目录

本目录存放正式测试与审查证据。

命名约定：

- `docs/evidence_packs/<TaskID>-<Role>-<YYYYMMDDHHMM>.md`

使用规则：

1. 正式独立测试必须落单独 `EvidencePack`。
2. 审查阶段可复用测试证据，但需要补充 findings 与 release decision。
3. `pending` 占位只能用于预留路径，不能冒充正式通过结论。
