# EvidencePack

Task ID: TG-006
Role: test
Date: 2026-04-13T18:56:45+08:00
Acceptance Gate: G4
Stage: independent-test-bounded-deterministic-scope
Conclusion: partial

## Scope

本次独立测试严格限定为 `TG-006` 的 bounded deterministic implementation 验证，只检查以下 claim：

1. `order.place` 的写前 `connect_gate` 是否已经改为消费与 `probe.connection` 同源的 machine-readable authority truth，而不是继续维护另一套独立 gate 推导。
2. `order.place` 的返回包是否新增 additive `write_authority_snapshot`，并在 gate 拦截路径下稳定回传阻断原因、resolved session、observed probe session 与 `session_plan_version`。
3. 在同一 deterministic fixture 下，`probe.connection` 与 `order.place` 是否对写前 authority 给出一致的阻断语义。
4. 公共 MCP `order.place` 入口是否继续通过 governed service path 暴露上述 contract，而不是绕开服务端路径。

本次明确不做：

1. 不执行 live gateway / broker / MiniQMT 验收。
2. 不创建 `EnvSnapshot`。
3. 不把 bounded deterministic test 外推成 formal `G4 pass`、formal green 或 `VAL-003` 解阻。
4. 不宣布 closeout，不生成 `ReviewPack`。

## Inputs Read

- `AGENTS.md`
- `docs/task_cards/TG-006.md`
- `docs/change_packages/TG-006.md`
- `docs/EXECUTION_AND_ARTIFACT_STANDARD.md`
- `docs/WORKFLOW_AND_BOARD.md`
- `docs/VAL-003_G4_EXECUTION_PLAN.md`
- `docs/ACCEPTANCE_STANDARD.md`
- `docs/evidence_packs/TG-001-test-202603311525.md`
- `xtqmt_mcp/trade_ops.py`
- `tests/test_trade_order_submission_contract.py`
- `tests/test_trade_gateway_server.py`

## Code Snapshot

- Repository snapshot: `/mnt/d/xtquant-mcp/repo`
- Git metadata: 当前挂载副本不是 Git worktree，`git status --short` 返回 `fatal: not a git repository`；本次证据按当前工作目录快照记录，不补写 commit 结论。

## Commands

1. `rg -n "write_authority_snapshot|connect_gate_failed|probe\\.connection|order\\.place|same-plan|same_plan|session_plan|resolved_session|write_authority" xtqmt_mcp/trade_ops.py tests/test_trade_order_submission_contract.py tests/test_trade_gateway_server.py docs/task_cards/TG-006.md docs/change_packages/TG-006.md`
2. `nl -ba tests/test_trade_order_submission_contract.py | sed -n '280,380p'`
3. `nl -ba tests/test_trade_order_submission_contract.py | sed -n '440,530p'`
4. `nl -ba xtqmt_mcp/trade_ops.py | sed -n '600,725p'`
5. `nl -ba xtqmt_mcp/trade_ops.py | sed -n '1840,1915p'`
6. `nl -ba tests/test_trade_gateway_server.py | sed -n '680,770p'`
7. `python -m unittest tests.test_trade_order_submission_contract -v`
8. `python -m unittest tests.test_trade_gateway_server -v`
9. `date '+%Y%m%d%H%M %Y-%m-%dT%H:%M:%S%z'`

## Raw Results

### Code Inspection

- `xtqmt_mcp/trade_ops.py:618-680`
  - `_run_connect_gate()` 在有 shadow probe 的路径下直接调用 `self.probe_connection()`，并从 `probe.connection` payload 生成 `write_authority_snapshot`。
  - `connect_gate.pass` 的判定显式要求：
    - `same_plan_verdict`
    - `probe_complete_verdict`
    - `fresh_connect_verified`
    - `write_authority_ready`
  - `gate_source` 固定回填为 `probe.connection`，不再表现为另一套 shadow gate 主真相。
- `xtqmt_mcp/trade_ops.py:682-704`
  - `_write_authority_snapshot_from_probe_payload()` 将 `probe.connection` payload 规整成 additive `write_authority_snapshot`，包含：
    - `blocking_reason`
    - `resolved_session_id`
    - `observed_probe_session_id`
    - `same_plan_verdict`
    - `probe_complete_verdict`
    - `fresh_connect_verified`
    - `write_authority_ready`
    - `session_plan_version`
    - `source = probe.connection`
- `xtqmt_mcp/trade_ops.py:1852-1868`
  - `order.cancel()` 在 connect gate 拦截时也复用同一个 `write_authority_snapshot`，说明写前 authority 已沿 governed write path 复用，不是只在单点返回。
- `tests/test_trade_order_submission_contract.py:281-318`
  - `test_order_place_connect_gate_failure_marks_local_intercept_without_broker_submit` 断言：
    - `code = connect_gate_failed`
    - `connect_gate.gate_source = probe.connection`
    - `connect_gate.reason = probe_session_differs_from_resolved_write_session`
    - `write_authority_snapshot.blocking_reason = probe_session_differs_from_resolved_write_session`
    - `write_authority_snapshot.resolved_session_id = 2111`
    - `write_authority_snapshot.observed_probe_session_id = 101`
- `tests/test_trade_order_submission_contract.py:323-365`
  - `test_probe_connection_and_order_place_share_same_write_authority_truth_under_same_fixture` 直接断言同一 fixture 下：
    - `probe.payload["reason"] == placed.payload["write_authority_snapshot"]["blocking_reason"]`
    - `probe.payload["session_id"] == placed.payload["write_authority_snapshot"]["resolved_session_id"]`
    - `probe.payload["observed_probe_session_id"] == placed.payload["write_authority_snapshot"]["observed_probe_session_id"]`
    - `probe.payload["session_plan_version"] == placed.payload["write_authority_snapshot"]["session_plan_version"]`
- `tests/test_trade_order_submission_contract.py:440-465`
  - `test_order_place_connect_gate_unstable_still_marks_local_gate_contract_flags` 覆盖 `connect_failed` 路径，确认 `connect_gate.reason` 与 `write_authority_snapshot.blocking_reason` 同步为 `connect_failed`。
- `tests/test_trade_order_submission_contract.py:470-517`
  - green path fixture 允许 `_run_connect_gate()` 返回带 `write_authority_snapshot` 的正向快照，证明新增字段为 additive contract，而非仅失败时存在。
- `tests/test_trade_gateway_server.py:684-719`
  - `test_order_place_routes_through_governed_server_write_path` 证明公共 MCP `order.place` 仍通过 `service.place_order(...)` 的 governed path 暴露，并保留 `connect_gate_failed`、`governed_write_path`、`write_path`、`gate_sequence` 等 envelope。

### Test Command 7

```text
test_order_cancel_green_path_marks_broker_cancel_scope ... ok
test_order_cancel_respects_same_write_authority_gate_as_order_place ... ok
test_order_place_connect_gate_failure_marks_local_intercept_without_broker_submit ... ok
test_order_place_connect_gate_unstable_still_marks_local_gate_contract_flags ... ok
test_order_place_green_path_marks_broker_submit_scope ... ok
test_order_place_requires_non_empty_kill_switch_file_in_prod_scope ... ok
test_probe_connection_and_order_place_share_same_write_authority_truth_under_same_fixture ... ok

----------------------------------------------------------------------
Ran 7 tests in 0.537s

OK
```

观察：

1. `TG-006` 最直接相关的三条 contract test 全部通过：
   - `test_order_place_connect_gate_failure_marks_local_intercept_without_broker_submit`
   - `test_probe_connection_and_order_place_share_same_write_authority_truth_under_same_fixture`
   - `test_order_place_connect_gate_unstable_still_marks_local_gate_contract_flags`
2. 额外两条 green path / cancel path 测试通过，说明新增 `write_authority_snapshot` 未破坏受控写路径与相邻 governed contract。

### Test Command 8

```text
test_health_payload_reports_prod_kill_switch_release_blocker ... ok
test_initialize_login_resource_and_prompt ... ok
test_order_place_routes_through_governed_server_write_path ... ok
test_order_resources_are_cached_to_distinct_uris ... ok
test_primary_account_flow_rejects_explicit_account_selectors ... ok
test_primary_account_flow_schemas_hide_explicit_account_inputs ... ok
test_read_trade_resource_blocks_fake_payload_in_prod_scope ... ok
test_session_current_resource_exposes_base_and_effective_session_resolution ... ok
test_session_manager_builder_uses_runtime_cfg_by_default_and_custom_factory_when_provided ... ok
test_trade_context_factory_is_forwarded_into_session_manager_builder ... ok
test_trade_ops_needs_distinguish_warm_health_from_public_orders ... ok

----------------------------------------------------------------------
Ran 11 tests in 0.292s

OK
```

观察：

1. 公共 gateway suite 通过，说明 `order.place` 的 governed server envelope 与 session/current 相关 contract 在当前 deterministic 范围内未见回归。
2. 本次 suite 不包含 live gateway / broker 交互，只能支撑 repo-local deterministic 结论。

### Command 9

- 结果：`202604131856 2026-04-13T18:56:45+0800`

## Verification Notes

1. `TG-006` 在实现边界内已经存在明确开发交付物，且 ChangePack 标注的 self-check 两组测试在独立重跑中全部通过。
2. 代码与测试共同支持以下 bounded claim：
   - `order.place` 写前 gate 已消费 `probe.connection` 同源 truth。
   - `write_authority_snapshot` 已作为 additive contract 落到返回包。
   - 同一 deterministic fixture 下，`probe.connection` 与 `order.place` 的阻断原因、resolved session、observed probe session、`session_plan_version` 一致。
3. 这份工件只证明“已实现 + 已 self-test + 已形成首个独立 deterministic 测试证据”，不证明 formal authority 已转绿。
4. `VAL-003` 当前 formal posture 仍是 blocked reopen plan；本次没有触碰 live packet、gateway recovery、fresh probe、真实 broker 写入，因此不能把本工件外推成 `VAL-003` green，也不能据此 closeout `TG-006`。

## Findings

未发现新的 scoped `fail_design` 或 `fail_env` finding。

需要保留的边界说明：

1. `TG-006` 的独立测试目前仅覆盖 deterministic repo-local contract 与 governed gateway envelope。
2. formal authority carrier、fresh runtime proof、`VAL-003` reopen 条件均不在本次证据范围内。

## Failure Classification

- 本次 bounded deterministic independent test 未触发新的环境失败或设计失败。
- 尚未覆盖的 live / formal proof 不记为本次失败项；它们属于明确的 out-of-scope downstream work。

## Evidence Paths

- `docs/task_cards/TG-006.md`
- `docs/change_packages/TG-006.md`
- `docs/EXECUTION_AND_ARTIFACT_STANDARD.md`
- `docs/WORKFLOW_AND_BOARD.md`
- `docs/VAL-003_G4_EXECUTION_PLAN.md`
- `docs/ACCEPTANCE_STANDARD.md`
- `xtqmt_mcp/trade_ops.py`
- `tests/test_trade_order_submission_contract.py`
- `tests/test_trade_gateway_server.py`

## Formal Conclusion

`partial`

理由：

1. `TG-006` 的实现边界已被代码检查与 deterministic 独立测试支持。
2. 开发自测已存在，且本次 test 角色独立重跑得到一致结果。
3. 但任务卡最高 gate 仍为 `G4`，本次没有执行 live broker / fresh runtime 验收，也没有形成 formal authority green。
4. 因此当前只能诚实落为首个独立测试 EvidencePack，不能 closeout，也不能把结论升级为 `pass`。
