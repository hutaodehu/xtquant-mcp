# EvidencePack

Task ID: TG-005
Role: test
Date: 2026-03-31T19:16:00+08:00
Acceptance Gate: G3
Conclusion: pass

## Env Snapshot

- Link: [TG-005-test-202603311916.md](../env_snapshots/TG-005-test-202603311916.md)
- Host: `CHIYU`
- Shell: `PowerShell 7.6.0`
- Repo Root: `D:\xtquant-mcp\repo`
- Config: `D:\xtquant-mcp\instance\prod\config\trade_gateway.local.yaml`

## Test Scope

1. Only verify whether TG-005 eliminated the current public `orders.list` first blocker: `'NoneType' object has no attribute 'query_open_orders'`.
2. Keep scope inside `G3` public-read lane only:
   `miniqmt.ensure_logged_in -> session.warm -> session.status -> probe.connection -> account.show -> positions.list -> orders.list -> snapshot.l1`.
3. Explicitly determine whether current public `orders.list` is:
   - broker success
   - explicit degraded fallback
   - fail_env
   - fail_design
4. Do not call `order.place`, do not start `VAL-003`, and do not make any `G4 ready` claim.

## Execution Route

1. Deterministic repo-local verification:
   - `python -m unittest -v tests.test_trade_ops_warm_health tests.test_trade_gateway_server`
   - Result: `pass` (`11` tests, `OK`)
2. Runtime drift check before live rerun:
   - Existing trade gateway listener on `127.0.0.1:8765` was healthy, but process `pid=48672` had `CreationDate=2026-03-31 18:28:40`.
   - TG-005 repo files were written later:
     - `xtqmt_mcp/trade_ops.py` -> `2026-03-31 19:05:19`
     - `xtqmt_mcp/trade_gateway/bootstrap.py` -> `2026-03-31 19:05:31`
     - `xtqmt_mcp/trade_gateway/server.py` -> `2026-03-31 19:05:40`
   - Therefore a reuse-only rerun against `pid=48672` would not verify current repo truth.
3. Fresh repo-backed live rerun:
   - Restarted only trade gateway:
     - `Stop-Process -Id 48672 -Force`
     - `pwsh -File D:\xtquant-mcp\repo\scripts\wake_trade_gateway.ps1`
   - Confirmed new trade gateway listener:
     - `127.0.0.1:8765 -> pid 50404`
     - process start time `2026-03-31 19:14:15`
     - command line:
       `"D:\xtquant-mcp\venv313\Scripts\python.exe" D:\xtquant-mcp\repo\scripts\run_trade_gateway_http.py --config D:\xtquant-mcp\instance\prod\config\trade_gateway.local.yaml`
4. Ordered live MCP chain on the fresh process:
   - `initialize`
   - `tools/call miniqmt.ensure_logged_in {"login_timeout_seconds":20}`
   - `tools/call session.warm {}`
   - `tools/call session.status {}`
   - `tools/call probe.connection {}`
   - `tools/call account.show {}`
   - `tools/call positions.list {}`
   - `tools/call orders.list {}`
   - `tools/call snapshot.l1 {"code":"000001.SZ"}`
   - `resources/read trade://session/current`
   - `resources/read diag://probe/latest`
   - `resources/read diag://login/latest`

## Key Results

### Deterministic repo-local checks

- `tests.test_trade_ops_warm_health`:
  - broker adapter missing path no longer crashes
  - explicit fallback and machine-readable failure contract both covered
- `tests.test_trade_gateway_server`:
  - `session.warm` and public `orders.list` wiring both require broker + shadow
  - `orders.list` tool description still distinguishes public fallback vs warm-health-only shadow

### Runtime drift observation on stale process

- Reused old process `pid=48672` still reproduced the historical blocker:
  - `orders.list.ok=false`
  - `error.code=server_env_not_ready`
  - `error.message='NoneType' object has no attribute 'query_open_orders'`
  - `trace_id=c647be9f-ffe0-49c4-85d2-3e4b419a58e2`
- This was not used as the final verdict because that process predated TG-005 source writes.

### Fresh live rerun against current repo process

- `miniqmt.ensure_logged_in`
  - `ok=true`
  - `status=already_logged_in`
  - `trace_id=6700a2d4-e292-4528-9dcf-cbbfdfc1fb80`
- `session.warm`
  - `ok=true`
  - `ready=true`
  - `session_id=1111`
  - warm trace `orders.list` remained `source=xttrader_shadow`, `read_scope=warm_health_only`, `count=3`
  - `trace_id=0fbb42e8-ae7e-490e-8170-606071a1c59c`
- `session.status`
  - `ok=true`
  - `ready=true`
  - `session_id=1111`
  - `trace_id=b3d0bfb9-4989-4fd0-aed4-14f07bd3232a`
- `probe.connection`
  - `ok=true`
  - `reason=ok`
  - `read_only_ready=true`
  - `write_permission_ready=true`
  - `probe_mode=owner_managed_session_reuse`
  - `fresh_connect_attempted=false`
  - `trace_id=ee594fde-0d62-4f8a-bea1-f1b785bfef58`
- `account.show`
  - `ok=true`
  - `source=xttrader_shadow`
  - `cash=4199.19`
  - `total_asset=108999.19`
  - `trace_id=a83670d6-6e48-46e1-b3c3-cd9ab05260db`
- `positions.list`
  - `ok=true`
  - `count=2`
  - `trace_id=875bf3a2-88c0-4b17-9720-a32a2df32cac`
- `orders.list`
  - `ok=true`
  - `trace_id=96bede07-52a2-45ed-b6fd-fa5fe3245adc`
  - current payload shape:
    - `ok=true`
    - `source=broker`
    - `read_scope=public_broker`
    - `degraded=false`
    - `fallback_used=false`
    - `count=0`
    - `rows=[]`
    - `broker_read.source=broker`
    - `broker_read.ok=true`
    - `broker_read.error=''`
    - `broker_read.connected_before=false`
    - `broker_read.connected_after=true`
    - `broker_read.fresh_connect_attempted=true`
    - `broker_read.fresh_connect_ok=true`
  - absent fields:
    - no `failure_classification`
    - no fallback reason
    - no server error envelope
- `snapshot.l1`
  - `ok=true`
  - `code=000001.SZ`
  - `source=online_pull`
  - `trace_id=ae063b7e-4009-4896-8c5d-6a3b9f4a9d21`

## orders.list Classification

- broker success: `yes`
- explicit degraded fallback: `no`
- fail_env: `no`
- fail_design: `no`

Rationale:

1. Public `orders.list` on the fresh TG-005-backed process returned `ok=true`, not an exception envelope.
2. Payload is broker-backed and non-degraded:
   - `source=broker`
   - `read_scope=public_broker`
   - `degraded=false`
   - `fallback_used=false`
3. Broker connect/read path succeeded in-band:
   - `broker_read.ok=true`
   - `broker_read.connected_after=true`
   - `broker_read.fresh_connect_attempted=true`
   - `broker_read.fresh_connect_ok=true`
4. The previous first blocker `'NoneType' object has no attribute 'query_open_orders'` did not reproduce on the fresh process that actually loaded current repo code.

## Resource / State Truth

- `trade://session/current`
  - `ready=true`
  - `account_id=8883884325`
  - `owner_account_id=8883884325`
  - `session_id=1111`
  - `warmed_at=2026-03-31T19:14:56`
- `diag://probe/latest`
  - `ok=true`
  - `reason=ok`
  - `session_id=1111`
  - `read_only_ready=true`
  - `write_permission_ready=true`
  - `read_only_probe_source=active_owner_shadow`
  - `write_permission_probe_source=userdata_precheck`
- `diag://login/latest`
  - `ok=true`
  - `status=already_logged_in`
  - main window title remained `8883884325 - 国金证券QMT交易端 2.0.8.300`

## Artifact Refs

- Call log:
  - `D:\xtquant-mcp\instance\prod\artifacts\trade_gateway\20260331\trade_gateway_calls.jsonl`
- Trade gateway logs:
  - `D:\xtquant-mcp\instance\prod\logs\trade_gateway\trade_gateway_20260331_191415.log`
  - `D:\xtquant-mcp\instance\prod\logs\trade_gateway\trade_gateway_20260331_191415.stderr.log`
- State files:
  - `D:\xtquant-mcp\instance\prod\state\trade_resources\trade_session_current.json`
  - `D:\xtquant-mcp\instance\prod\state\trade_resources\diag_probe_latest.json`
  - `D:\xtquant-mcp\instance\prod\state\trade_resources\diag_login_latest.json`

## Final Verdict

`TG-005` is independently verified as `pass` for the scoped `G3` public-read lane.

The stale pre-patch process still showed the historical `NoneType.query_open_orders` blocker, but that was runtime drift, not current repo truth. After restarting only trade gateway and rerunning the ordered `G3` chain against the fresh TG-005-backed process, public `orders.list` returned broker success with `ok=true`, `source=broker`, `read_scope=public_broker`, `degraded=false`, and `fallback_used=false`. Therefore the current first blocker is closed, and this card is not blocked by environment. This evidence does not make any `VAL-003` or `G4 ready` claim.
