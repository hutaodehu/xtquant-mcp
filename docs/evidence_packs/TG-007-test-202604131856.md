# EvidencePack

Task ID: TG-007
Role: test
Date: 2026-04-13T18:56:00+08:00
Acceptance Gate: G4
Stage: independent-test-bounded-implementation-scope
Conclusion: partial

## Scope

本次独立测试严格限定为 `TG-007` 当前实现范围验证，只检查以下 claim：

1. bootstrap/base session truth、effective session truth、runtime override event 是否已经明确拆层，而不是继续由 runtime 健康检查原地覆写基础真相。
2. `session.warm` / `session.status` / session-state summary 是否已把 `session_resolution`、`effective_session_resolution`、`runtime_session_override` 三层同时暴露给 operator。
3. `probe.connection` 在 write-adjacent payload 上是否继续锚定 resolved write session，而不是被只读 probe session 或兼容字段误导。
4. 仅使用 repo 内现有 deterministic 测试、代码检查和文档口径检查给出结论。

本次明确不做：

1. 不执行 live write，不做 `order.place` 真机写入。
2. 不修改 formal truth，不修改 `CURRENT_STATUS.md`，不改变 `VAL-003` 的 blocked posture。
3. 不创建 ReviewPack，不把当前结论外推为 closeout 或 `G4 pass`。

## Inputs Read

- `AGENTS.md`
- `docs/task_cards/TG-007.md`
- `docs/change_packages/TG-007.md`
- `docs/EXECUTION_AND_ARTIFACT_STANDARD.md`
- `docs/WORKFLOW_AND_BOARD.md`
- `docs/VAL-003_G4_EXECUTION_PLAN.md`
- `docs/evidence_packs/TG-001-test-202603311525.md`
- `docs/MCP_DESIGN.md`
- `docs/OPERATIONS_RUNBOOK.md`
- `docs/ACCEPTANCE_STANDARD.md`
- `xtqmt_mcp/session_resolution.py`
- `xtqmt_mcp/trade_ops.py`
- `xtqmt_mcp/trade_gateway/session_manager.py`
- `tests/test_trade_gateway_bootstrap.py`
- `tests/test_trade_gateway_session_manager.py`
- `tests/test_trade_probe_readiness_split.py`

## Commands

1. `rg -n "effective_session_resolution|runtime_session_override|realign_session_resolution|session_resolution|same_plan_verdict" xtqmt_mcp tests docs`
2. `python -m unittest tests.test_trade_gateway_bootstrap tests.test_trade_gateway_session_manager -v`
3. `python -m unittest tests.test_trade_probe_readiness_split -v`
4. 代码/文档段复核：
   - `xtqmt_mcp/session_resolution.py:133-192`
   - `xtqmt_mcp/trade_ops.py:250-272`
   - `xtqmt_mcp/trade_ops.py:375-416`
   - `xtqmt_mcp/trade_ops.py:1421-1534`
   - `xtqmt_mcp/trade_gateway/session_manager.py:41-87`
   - `xtqmt_mcp/trade_gateway/session_manager.py:112-125`
   - `xtqmt_mcp/trade_gateway/session_manager.py:365-388`
   - `xtqmt_mcp/trade_gateway/session_manager.py:426-445`
   - `xtqmt_mcp/trade_gateway/session_manager.py:460-467`
   - `tests/test_trade_gateway_bootstrap.py:233-380`
   - `tests/test_trade_gateway_session_manager.py:283-377`
   - `tests/test_trade_probe_readiness_split.py:146-230`
   - `tests/test_trade_probe_readiness_split.py:335-487`
   - `docs/MCP_DESIGN.md:224-249`
   - `docs/OPERATIONS_RUNBOOK.md:218-218`
   - `docs/OPERATIONS_RUNBOOK.md:251-257`
   - `docs/ACCEPTANCE_STANDARD.md:175-175`
   - `docs/ACCEPTANCE_STANDARD.md:211-211`
   - `docs/ACCEPTANCE_STANDARD.md:228-228`
   - `docs/ACCEPTANCE_STANDARD.md:268-269`

## Raw Results

### Command 1

- `docs/MCP_DESIGN.md:236-243`
  - `TG-007` 之后，session-state 类载体要求显式区分三层：
    - `session_resolution` = bootstrap/base truth
    - `effective_session_resolution` = 当前有效视图
    - `runtime_session_override` = realign 事件元数据
- `docs/OPERATIONS_RUNBOOK.md:218`
  - 当 `probe.connection.session_id` 与 `observed_probe_session_id` 不一致且 `same_plan_verdict=false` 时，operator 必须以顶层 resolved write session 为主真相，不能把只读 probe session 当写路径真相。
- `docs/OPERATIONS_RUNBOOK.md:251-257` 与 `docs/ACCEPTANCE_STANDARD.md:175,211,228,268-269`
  - `effective_session_plan` 仍是 write-path / G4 的唯一主真相；warm、probe、write、审查都必须回链同一套 plan。

### Command 2

```text
Ran 14 tests in 0.575s

OK
```

关键覆盖点：

- `tests.test_trade_gateway_bootstrap.TradeGatewayBootstrapTests.test_realign_session_resolution_preserves_seed_truth_and_records_runtime_event`
  - runtime realign 后保留 `seed_resolved_session_id=1111`、`resolved_base_session_id=1111`，并写出 `runtime_resolution_event`，证明 base truth 未被静默改写。
- `tests.test_trade_gateway_bootstrap.TradeGatewayBootstrapTests.test_order_place_rebuilds_prebuilt_write_broker_after_session_realign`
  - write broker 会按 realigned session 重新构建，实际 write-path 读取 effective session truth，而不是继续沿用旧 base config。
- `tests.test_trade_gateway_session_manager.TradeGatewaySessionManagerTests.test_warm_and_status_expose_live_owner_session_id_when_service_reports_it`
  - `summary["session_resolution"]["resolved_session_id"] == 901`
  - `summary["effective_session_resolution"]["resolved_session_id"] == 1100`
  - `summary["runtime_session_override"]["event_source"] == "owner_managed_session"`
- `tests.test_trade_gateway_session_manager.TradeGatewaySessionManagerTests.test_warm_and_status_follow_probe_realign_when_fresh_verify_succeeds`
  - `summary["session_resolution"]["resolved_session_id"] == 2100`
  - `summary["effective_session_resolution"]["resolved_session_id"] == 2101`
  - `summary["runtime_session_override"]["event_source"] == "probe.connection"`
- `tests.test_trade_gateway_session_manager.TradeGatewaySessionManagerTests.test_warm_and_status_realign_to_resolved_write_session_before_fresh_connect_turns_green`
  - 即使 `fresh_connect_verified=false`、`write_authority_ready=false`，`session.warm` / `session.status` 也已把 top-level session 真实收口到 resolved write session，并保留 base/effective 分层，不再把旧 bootstrap truth 无痕覆盖掉。

### Command 3

```text
Ran 11 tests in 0.758s

OK
```

关键覆盖点：

- `tests.test_trade_probe_readiness_split.TradeProbeReadinessSplitTests.test_probe_connection_read_only_ready_with_write_permission_blocked`
  - 只读 probe session 可复用，但只要 `same_plan_verdict=false`，顶层仍保持 `session_id=2111` 作为 write-path 主真相，并通过 `observed_probe_session_id=101` 单独暴露观测 session，避免 operator 被 owner shadow 误导。
- `tests.test_trade_probe_readiness_split.TradeProbeReadinessSplitTests.test_probe_connection_owner_managed_broker_verify_can_realign_write_session`
  - owner-managed reuse 可以在 broker fresh verify 后把 write-path 主真相 realign 到 `2101`；payload 中 `session_resolution.resolved_session_id=2101`，`effective_session_plan[0]=2101`。
- `tests.test_trade_probe_readiness_split.TradeProbeReadinessSplitTests.test_probe_connection_owner_managed_fresh_verify_connect_failure_realigns_write_truth_to_attempted_broker_session`
  - 即使 fresh connect 最终失败，payload 仍把 attempted broker session `2101` 收口为当前 write truth，并明确 `write_permission_block_reason=write_connect_failed`，避免把失败归咎到旧 owner shadow session。

## Verification Notes

1. `TradeOpsService` 已把 base/effective/runtime override 三层明确拆开。
   - `xtqmt_mcp/trade_ops.py:250-272`：`base_session_resolution`、`effective_session_resolution()`、`runtime_session_override()` 已独立存在。
   - `xtqmt_mcp/trade_ops.py:375-397`：`realign_session_resolution()` 只写 `_runtime_session_resolution`，不回写 `_base_session_resolution`。
   - `xtqmt_mcp/session_resolution.py:133-192`：runtime realign 会保留 `seed_resolved_session_id`、`resolved_base_session_id`、`previous_resolved_session_id`，并记录 `event_source`、`owner_session_id`、`observed_probe_session_id`、`attempted_broker_session_id`。

2. session-state summary 已按设计要求双轨呈现，不再把 runtime 观测伪装成 bootstrap 真相。
   - `xtqmt_mcp/trade_gateway/session_manager.py:365-388`：`GatewaySessionState.summary()` 同时返回 `session_resolution`、`effective_session_resolution`、`runtime_session_override`。
   - `xtqmt_mcp/trade_gateway/session_manager.py:426-445`：owner mismatch payload 也保持同一三层结构，避免 operator 在非 owner 场景下丢失 truth 分层。
   - `xtqmt_mcp/trade_gateway/session_manager.py:460-467`：运行态 `session_id` 优先跟随 effective session truth，而不是退回 base truth。
   - `xtqmt_mcp/trade_gateway/session_manager.py:112-125`：warm/status trace 会把 runtime realign 后的 resolution 同步到 trace payload，operator 可直接观察当前有效视图与 override 事件。

3. operator-facing write/path 判断没有被兼容字段带偏，仍锚定 effective write truth。
   - `xtqmt_mcp/trade_ops.py:1421-1534`：`probe.connection` 顶层 `session_id` 对齐 `write_session_alignment.resolved_session_id`，`observed_probe_session_id` 单独暴露，只读 probe session 不再占用顶层主语义。
   - 同一段代码明确把 `same_plan_verdict`、`probe_complete_verdict`、`write_permission_precheck_ok`、`write_permission_ready` / `write_authority_ready` 作为 machine-readable gate，符合 `OPERATIONS_RUNBOOK` 与 `ACCEPTANCE_STANDARD` 当前口径。

4. 当前证据足以说明：
   - 已实现 `TG-007` 声称的三层分离。
   - 已完成开发自测之外的首个独立 deterministic EvidencePack。
   - 可以把任务卡推进到 `In Independent Test` 并挂上正式 EvidencePack 入口。

5. 当前证据不足以说明：
   - `TG-007` 已 closeout。
   - `TG-007` 已达到 `G4 pass`。
   - `VAL-003` 的 live blocked posture 可以解除。

## Findings

未发现新的 scoped `fail_design` 或 `fail_env` finding。

需要保留的边界说明：

1. write-adjacent payload 目前仍保留兼容字段 `session_resolution`；operator 必须按 `effective_session_resolution` 与 `runtime_session_override` 联合解读，而不能把兼容字段误读成“唯一当前态”。
2. 本次独立测试完全基于 deterministic 代码/测试/文档检查，尚未补 live packet、fresh runtime capture 或 review authority。

## Failure Classification

- 本次 bounded independent test 未触发新的环境失败或设计失败。
- 未完成的 formal closeout 不记为本次执行失败；它属于明确的 downstream review/live proof 缺口，而不是当前 deterministic evidence 的反证。

## Evidence Paths

- `docs/task_cards/TG-007.md`
- `docs/change_packages/TG-007.md`
- `docs/MCP_DESIGN.md`
- `docs/OPERATIONS_RUNBOOK.md`
- `docs/ACCEPTANCE_STANDARD.md`
- `docs/EXECUTION_AND_ARTIFACT_STANDARD.md`
- `docs/WORKFLOW_AND_BOARD.md`
- `docs/VAL-003_G4_EXECUTION_PLAN.md`
- `xtqmt_mcp/session_resolution.py`
- `xtqmt_mcp/trade_ops.py`
- `xtqmt_mcp/trade_gateway/session_manager.py`
- `tests/test_trade_gateway_bootstrap.py`
- `tests/test_trade_gateway_session_manager.py`
- `tests/test_trade_probe_readiness_split.py`

## Formal Conclusion

`partial`

理由：

1. `TG-007` 所声明的实现边界已经被独立代码检查和 deterministic 测试支持：
   - base truth 未被 runtime realign 原地覆盖；
   - session-state summary 已显式暴露 `session_resolution` / `effective_session_resolution` / `runtime_session_override`；
   - `probe.connection` 继续锚定 resolved write session，并通过 additive 字段单独暴露观测 session，operator 不应再被只读 probe 结果误导。
2. 当前 EvidencePack 足以作为 `In Independent Test` 的正式证据入口。
3. 但本卡最高 gate 仍是 `G4`，且当前没有 live write、没有新的 ReviewPack、也没有 formal authority closeout，因此不能写成 `pass`，更不能宣布 closeout。

## Recommended Next Board Status

- Target Status: `In Independent Test`
- Reason: 首个独立 EvidencePack 已形成，足以支撑测试阶段正式挂证据入口；但 review authority、live G4 proof 与 formal closeout 均未完成，因此不得推进到 `In Review` 或 `Accepted`。
