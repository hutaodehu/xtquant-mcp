# EvidencePack

Task ID: TG-008
Role: test
Date: 2026-04-13T19:13:08+08:00
Acceptance Gate: G3
Stage: independent-test-repo-local
Conclusion: partial

## 测试范围

本次独立测试只覆盖 `TG-008` 当前声明范围内、且本轮补丁直接触达的资源契约：

1. `trade://orders/today`、`trade://fills/today`、`diag://order_*` 继续保持分离缓存，不发生交叉污染。
2. resource cache-miss 返回继续带 `freshness_status`、`resource_authority`，并补齐 `resource_trace_id`、`resource_server_ts` 元数据。
3. 仅做 repo-local deterministic 测试，不触发跨宿主、外部端口或 live gateway 访问。

本次明确不做：

1. 不改 formal authority 生成逻辑。
2. 不创建 EnvSnapshot。
3. 不宣布 closeout、`In Review` 或 `Accepted`。

## 已读取输入

- `AGENTS.md`
- `docs/task_cards/TG-008.md`
- `docs/change_packages/TG-008.md`
- `docs/EXECUTION_AND_ARTIFACT_STANDARD.md`
- `docs/WORKFLOW_AND_BOARD.md`
- `docs/VAL-003_G4_EXECUTION_PLAN.md`
- `xtqmt_mcp/trade_gateway/resources.py`
- `tests/test_trade_gateway_server.py`

## 执行命令

1. red:
   `python -m unittest tests.test_trade_gateway_server.TradeGatewayServerTests.test_read_trade_resource_cache_miss_includes_resource_metadata -v`
2. green:
   `python -m unittest tests.test_trade_gateway_server.TradeGatewayServerTests.test_read_trade_resource_cache_miss_includes_resource_metadata tests.test_trade_gateway_server.TradeGatewayServerTests.test_order_resources_are_cached_to_distinct_uris -v`
3. 回归：
   `python -m unittest tests.test_trade_gateway_server -v`
4. 交叉回归：
   `python -m unittest tests.test_trade_order_submission_contract tests.test_trade_gateway_server -v`

## 原始结果

### red

- `test_read_trade_resource_cache_miss_includes_resource_metadata`
  - 失败前现象：`resource_not_cached` payload 只有
    `available`、`reason`、`state_scope`、`freshness_status`、`resource_authority`
  - 缺失字段：`resource_trace_id`、`resource_server_ts`
  - 失败断言：`'resource_trace_id' not found`

### green

- `test_read_trade_resource_cache_miss_includes_resource_metadata` 通过。
- `test_order_resources_are_cached_to_distinct_uris` 通过。
- `python -m unittest tests.test_trade_gateway_server -v`
  - `13` 个测试全部通过，`OK`
- `python -m unittest tests.test_trade_order_submission_contract tests.test_trade_gateway_server -v`
  - `22` 个测试全部通过，`OK`

## 结论说明

1. `TG-008` 本轮补丁已把 cache-miss payload 补齐为完整资源 metadata 形态：
   - `freshness_status=cached_state_missing`
   - `resource_authority=unavailable`
   - `resource_trace_id=""`
   - `resource_server_ts=""`
2. 资源分离缓存回归仍保持成立：
   - `orders.list -> trade://orders/today`
   - `fills.list -> trade://fills/today`
   - `order.place/order.cancel/order.status -> diag://order_*`
3. 本次证据只证明 repo-local 独立测试已经形成，不等于任务可 closeout。

## 不能 closeout 的原因

1. 当前只形成 `test` 角色的独立测试证据，尚无 `ReviewPack`。
2. 本次未进行 review authority 判定，不允许把任务推进到 `In Review` 或 `Accepted`。
3. 本轮只验证 `TG-008` 的实现与 deterministic 资源契约，不扩大到其他卡的 formal truth。

## 风险与未覆盖点

1. 本次只覆盖 repo-local 资源读写契约；未在外部运行中的 gateway 进程上做 live 读回验证。
2. `trade_ops.py` 的共享写门禁逻辑在本轮同时被修补；若主控认为 `TG-006` 的旧证据依赖旧 no-shadow 旁路假设，可能需要后续人工判断是否刷新相关 EvidencePack。本次未越权修改 `TG-006` 工件。

## Evidence Paths

- `docs/task_cards/TG-008.md`
- `docs/change_packages/TG-008.md`
- `xtqmt_mcp/trade_gateway/resources.py`
- `tests/test_trade_gateway_server.py`

## Formal Conclusion

`partial`

理由：

1. `TG-008` 本轮目标内的资源 metadata 缺口已被独立测试命中并验证修复。
2. 相关资源隔离回归测试保持绿色。
3. 但当前只形成独立测试证据，未进入 review，也未形成 closeout authority。

## Recommended Next Board Status

- Target Status: `In Independent Test`
- Reason: 已形成首个独立 EvidencePack，但当前回合按要求只停留在独立测试阶段，不推进到 `In Review`。
