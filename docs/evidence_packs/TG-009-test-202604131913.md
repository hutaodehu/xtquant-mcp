# EvidencePack

Task ID: TG-009
Role: test
Date: 2026-04-13T19:13:08+08:00
Acceptance Gate: G4
Stage: independent-test-repo-local
Conclusion: partial

## 测试范围

本次独立测试只覆盖 `TG-009` 本轮 reviewer finding 对应的实现与契约：

1. 只有显式 `execution_mode=flow_smoke` 才允许 no-shadow 放宽；`live/no-shadow` 不得再隐式旁路 connect gate / write authority snapshot。
2. `tools/call order.cancel` 在网关层继续走 `require_ready=True` 的 session dispatch，并保留 machine-readable 提交范围字段。
3. 仅做 repo-local deterministic 测试，不触发 live broker 写入，不改变 `VAL-003` 的 blocked posture。

本次明确不做：

1. 不执行真实撤单。
2. 不创建 EnvSnapshot。
3. 不宣布 closeout、`In Review` 或 `Accepted`。

## 已读取输入

- `AGENTS.md`
- `docs/task_cards/TG-009.md`
- `docs/change_packages/TG-009.md`
- `docs/EXECUTION_AND_ARTIFACT_STANDARD.md`
- `docs/WORKFLOW_AND_BOARD.md`
- `docs/VAL-003_G4_EXECUTION_PLAN.md`
- `xtqmt_mcp/trade_ops.py`
- `tests/test_trade_order_submission_contract.py`
- `tests/test_trade_gateway_server.py`

## 执行命令

1. red:
   `python -m unittest tests.test_trade_order_submission_contract.TradeOrderSubmissionContractTests.test_live_no_shadow_connect_gate_uses_probe_connection_instead_of_flow_smoke_bypass tests.test_trade_order_submission_contract.TradeOrderSubmissionContractTests.test_live_no_shadow_write_authority_snapshot_uses_probe_payload tests.test_trade_gateway_server.TradeGatewayServerTests.test_order_cancel_routes_through_gateway_session_dispatch -v`
2. green:
   `python -m unittest tests.test_trade_order_submission_contract.TradeOrderSubmissionContractTests.test_live_no_shadow_connect_gate_uses_probe_connection_instead_of_flow_smoke_bypass tests.test_trade_order_submission_contract.TradeOrderSubmissionContractTests.test_live_no_shadow_write_authority_snapshot_uses_probe_payload tests.test_trade_gateway_server.TradeGatewayServerTests.test_order_cancel_routes_through_gateway_session_dispatch -v`
3. 回归：
   `python -m unittest tests.test_trade_order_submission_contract tests.test_trade_gateway_server -v`

## 原始结果

### red

- `test_live_no_shadow_connect_gate_uses_probe_connection_instead_of_flow_smoke_bypass`
  - 失败前现象：`_run_connect_gate()` 在 `live/no-shadow` 下直接返回 `enabled=False`
  - 失败断言：`False is not true`
- `test_live_no_shadow_write_authority_snapshot_uses_probe_payload`
  - 失败前现象：`_write_authority_snapshot()` 在 `live/no-shadow` 下直接返回 `ready=True/source=flow_smoke`
  - 失败断言：`True is not false`
- `test_order_cancel_routes_through_gateway_session_dispatch`
  - 新增网关集成断言，首次运行即通过，证明现有 server dispatch 已正确把 `order.cancel` 纳入 `require_ready=True` 的正式写工具路径；此前缺的是独立测试覆盖，不是额外实现缺口。

### green

- `test_live_no_shadow_connect_gate_uses_probe_connection_instead_of_flow_smoke_bypass` 通过。
- `test_live_no_shadow_write_authority_snapshot_uses_probe_payload` 通过。
- `test_order_cancel_routes_through_gateway_session_dispatch` 通过。
- `python -m unittest tests.test_trade_order_submission_contract tests.test_trade_gateway_server -v`
  - `22` 个测试全部通过，`OK`

## 结论说明

1. `live/no-shadow` 不再被隐式视为 `flow_smoke`：
   - `_run_connect_gate()` 现在会回到真实 `probe.connection` 语义
   - `_write_authority_snapshot()` 现在会消费真实 probe payload，而不是无条件 `ready=True`
2. `flow_smoke` 放宽边界仍保留，但已显式收敛到 `execution_mode=flow_smoke`。
3. 网关层 `tools/call order.cancel` 已有独立集成断言覆盖：
   - 通过 `GatewaySessionManager.execute(... require_ready=True)`
   - 保留 `broker_submission_attempted`
   - 保留 `local_gate_intercepted`
   - 保留 `submission_scope`
   - 保留 `submission_stage`

## 不能 closeout 的原因

1. 当前只形成 `test` 角色的独立测试证据，尚无 `ReviewPack`。
2. `TG-009` 的 Acceptance Gate 是 `G4`，本次未做 live broker cancel/write-path 验证。
3. `VAL-003` 当前 blocked posture 未改变，本次也未越权修改相关 formal truth。

## 风险与未覆盖点

1. 本次验证是 deterministic repo-local 测试，不等于 live 环境中 broker fresh connect 已闭合。
2. 共享写门禁逻辑改动位于 `trade_ops.py`；若主控判断 `TG-006` 的旧 EvidencePack 对 no-shadow 旁路有隐含依赖，后续可能需要人工判断是否补刷新。本次未越权修改 `TG-006` 工件。

## Evidence Paths

- `docs/task_cards/TG-009.md`
- `docs/change_packages/TG-009.md`
- `xtqmt_mcp/trade_ops.py`
- `tests/test_trade_order_submission_contract.py`
- `tests/test_trade_gateway_server.py`

## Formal Conclusion

`partial`

理由：

1. `TG-009` 本轮 reviewer findings 中的 no-shadow 隐式旁路已被独立测试命中并验证修复。
2. 网关层 `order.cancel` dispatch 已补齐集成断言。
3. 但当前只形成独立测试证据，且未做 `G4` live 验证，因此不能 closeout。

## Recommended Next Board Status

- Target Status: `In Independent Test`
- Reason: 已形成首个独立 EvidencePack，但按当前授权边界只停留在独立测试阶段。
