# EvidencePack

Task ID: TG-001
Role: test
Date: 2026-03-31T15:25:00+08:00
Acceptance Gate: G4
Stage: independent-test-bounded-implementation-scope
Conclusion: partial

## Scope

本次独立测试严格限定为 `TG-001` 的实现范围验证，只检查以下 claim：

1. `order.place` 公共 MCP 入口是否已经收口为服务端唯一正式受控写路径，而不是薄 broker 直提语义。
2. `order.place` 是否带出开发声明中的 governed metadata 与 gate sequence。
3. `order.status`、`order.cancel` 是否在代码与 deterministic 测试层面保持同一 session / primary-account / audit 边界下的非回归形态。
4. 仅用代码检查、文档检查和 deterministic repo-local 测试给出结论。

本次明确不做：

1. 不下真实单，不撤真实单，不触发 live broker 写入。
2. 不创建 EnvSnapshot。
3. 不把 `VAL-003` 的 live G4 链路工作提前执行。
4. 不把 bounded implementation inspection 外推成正式 `G4 pass`。

## Inputs Read

- `AGENTS.md`
- `docs/task_cards/TG-001.md`
- `docs/change_packages/TG-001.md`
- `docs/MCP_DESIGN.md`
- `docs/ACCEPTANCE_STANDARD.md`
- `docs/EXECUTION_AND_ARTIFACT_STANDARD.md`
- `docs/DESIGN_REVIEW_20260327.md`
- `xtqmt_mcp/trade_gateway/server.py`
- `xtqmt_mcp/trade_gateway/capability_v2.py`
- `xtqmt_mcp/trade_gateway/prompts.py`
- `tests/test_trade_gateway_server.py`
- `docs/task_cards/VAL-003.md`

补充代码检查：

- `xtqmt_mcp/trade_ops.py`

## Commands

1. `rg -n "order\.place|order\.status|order\.cancel|require_ready|primary_account|governed_write_path|gate_sequence|thin broker|TradeOpsService\.place_order|place_order_capability" xtqmt_mcp/trade_gateway/server.py xtqmt_mcp/trade_gateway/capability_v2.py xtqmt_mcp/trade_gateway/prompts.py tests/test_trade_gateway_server.py`
2. `rg -n "def place_order|def order_status|def order_cancel|guard_token|kill_switch|risk|audit" xtqmt_mcp/trade_ops.py`
3. `python -m unittest tests.test_trade_gateway_server -v`
4. `Get-Date -Format yyyyMMddHHmm`
5. 代码段复核：
   - `xtqmt_mcp/trade_gateway/server.py:202-225`
   - `xtqmt_mcp/trade_gateway/server.py:478-499`
   - `xtqmt_mcp/trade_gateway/capability_v2.py:12-35`
   - `xtqmt_mcp/trade_ops.py:1024-1098`
   - `xtqmt_mcp/trade_ops.py:1393-1659`

## Raw Results

### Command 1

- `xtqmt_mcp/trade_gateway/capability_v2.py:28-35`
  - `place_order_capability(...)` 的 docstring 明确写为 compatibility adapter，路由 MCP `order.place` 到 governed service path。
  - payload 明确填入：
    - `governed_write_path = True`
    - `write_path = governed_service_order_place`
    - `gate_sequence = [login, session, connectivity, write_permission, risk, kill_switch, audit_persistence]`
- `xtqmt_mcp/trade_gateway/prompts.py:27`
  - `trade-preflight` 明确把 `order.place` 描述为 server-side governed write path，并列出 login/session/connectivity/write-permission/risk/kill-switch/audit gates。
- `xtqmt_mcp/trade_gateway/server.py:212-225`
  - `order.place` 对外描述为 “Place one governed broker order through the server-side write path...”
  - 未再出现 thin-broker direct-submit wording。
- `xtqmt_mcp/trade_gateway/server.py:478-499`
  - `order.status` / `order.cancel` / `order.place` 都通过 `self._session_manager.execute(account_id=\"\", require_ready=True)` 进入同一 owner-managed session 执行路径。
  - `order.place` 的 runner 是 `place_order_capability(context.service, req)`，不是 server 内部直接 submit broker。
- `tests/test_trade_gateway_server.py:364-388`
  - `test_order_place_routes_through_governed_server_write_path` 断言：
    - `require_ready=True`
    - 只调用 `service.place_order(...)`
    - `guard_token == "mcp_server_governed_write_path"`
    - 返回 payload 中存在 `governed_write_path`、`write_path`、`gate_sequence`
- `tests/test_trade_gateway_server.py:236-237`
  - `order.place` schema/description 测试明确断言包含 `governed`，且不再包含 `thin broker order`。

### Command 2

- `xtqmt_mcp/trade_ops.py:1024-1098`
  - `order_status(...)` 通过 `self.broker.query_order(self.cfg.account_id, broker_order_id)` 查询主账户订单状态。
  - `order_cancel(...)` 先查当前订单，再通过 `_attach_persistence_meta(...)` 把撤单结果维持在同一持久化边界。
- `xtqmt_mcp/trade_ops.py:1393-1659`
  - `place_order(...)` 要求 `guard_token`；缺失时直接返回 `guard_token_missing`。
  - `place_order(...)` 内部显式执行：
    - `connect_gate`
    - `session_gate`
    - `risk_engine.evaluate(...)`
    - `kill_switch_on(...)`
    - `risk_decisions.csv` 审计落盘
    - 持久化 meta 回填

### Command 3

```text
test_initialize_login_resource_and_prompt ... ok
test_order_place_routes_through_governed_server_write_path ... ok
test_primary_account_flow_rejects_explicit_account_selectors ... ok
test_primary_account_flow_schemas_hide_explicit_account_inputs ... ok
test_read_trade_resource_blocks_fake_payload_in_prod_scope ... ok

----------------------------------------------------------------------
Ran 5 tests in 0.006s

OK
```

观察：

1. 本次 deterministic 测试全部通过。
2. 其中与 `TG-001` 最直接相关的是：
   - `test_order_place_routes_through_governed_server_write_path`
   - `test_primary_account_flow_schemas_hide_explicit_account_inputs`
   - `test_initialize_login_resource_and_prompt`
3. 测试使用 recording/fake service 与 repo-local temp root，不会触发真实 broker 下单。

### Command 4

- 结果：`202603311525`

## Verification Notes

1. `order.place` 公共入口已按 `TG-001` 的 bounded implementation claim 收口到受控服务端路径。
   - server 不再把公共入口表述成 thin broker order。
   - capability 适配层不再承担 broker 直通语义，而是显式进入 `service.place_order(...)`。
   - `trade_ops.place_order(...)` 自身继续要求 guard token，并保留 connect/session/risk/kill-switch/audit 这组服务端 gate。
2. governed metadata 与 gate sequence 已存在，并且能被 deterministic 测试直接观察。
   - `governed_write_path`
   - `write_path = governed_service_order_place`
   - `gate_sequence = [login, session, connectivity, write_permission, risk, kill_switch, audit_persistence]`
3. `order.status` / `order.cancel` 的 contract-level 非回归风险在本次边界内未发现新增问题。
   - server 路由层对 `order.status`、`order.cancel` 与 `order.place` 一样都要求 `require_ready=True`
   - 三者都走 owner-managed session 执行
   - `order.status`、`order.cancel` 继续绑定主账户上下文
   - `order.cancel` 仍带 persistence meta，未见 `TG-001` 把其移出审计/持久化边界
4. 本次证据只支持代码/契约/测试层面的 bounded 结论。
   - 不能证明真实 broker 写入已经成立
   - 不能证明 `order.status -> orders.list -> order.cancel -> fills.list` 的 live G4 证据链已经完成
   - 这部分仍是 [VAL-003.md](../task_cards/VAL-003.md) 的职责，并且该卡当前仍为 `Blocked / broker_blocked`

## Findings

未发现新的 scoped `fail_design` 或 `fail_env` finding。

需要保留的边界说明：

1. `TG-001` 的 dev claim 在 bounded implementation scope 内已被支持。
2. 但 `TG-001` 的任务卡 `Acceptance Gate` 是 `G4`，而本次没有执行 live broker 验证，因此不能把本 EvidencePack 写成正式 `G4 pass`。

## Failure Classification

- 本次 bounded independent test 未触发新的环境失败或设计失败。
- 未完成的 live proof 不记为本次已执行失败项；它是明确的 out-of-scope downstream work，归 [VAL-003.md](../task_cards/VAL-003.md)。

## Evidence Paths

- `docs/task_cards/TG-001.md`
- `docs/change_packages/TG-001.md`
- `docs/MCP_DESIGN.md`
- `docs/ACCEPTANCE_STANDARD.md`
- `docs/EXECUTION_AND_ARTIFACT_STANDARD.md`
- `docs/DESIGN_REVIEW_20260327.md`
- `xtqmt_mcp/trade_gateway/server.py`
- `xtqmt_mcp/trade_gateway/capability_v2.py`
- `xtqmt_mcp/trade_gateway/prompts.py`
- `xtqmt_mcp/trade_ops.py`
- `tests/test_trade_gateway_server.py`
- `docs/task_cards/VAL-003.md`

## Formal Conclusion

`partial`

理由：

1. `TG-001` 声称的实现范围内目标已被独立代码检查和 deterministic 测试支持：
   - `order.place` 已关闭到受控服务端写路径
   - 不再暴露 thin-broker direct-submit 语义
   - governed metadata 与 gate sequence 已存在
   - `order.status` / `order.cancel` 在当前边界内未见 contract-level 回归
2. 但当前任务卡的最高 gate 是 `G4`，而本次按要求未执行真实 broker 写入验证，因此不能给出正式 `pass`。
3. live proof 仍应由 [VAL-003.md](../task_cards/VAL-003.md) 独立完成；在那之前，本卡只能形成 bounded implementation-scope 的独立测试支持，而不能外推成 live write acceptance。

## Recommended Next Board Status

- Target Status: `In Review`
- Reason: 在 bounded implementation scope 内，`TG-001` 的 dev ChangePack 已得到独立测试支持，且未发现新的 scoped regression；下一步应由 review 角色判断该 bounded `partial` 结论是否足以支撑实现卡收口表述。但这不改变 `VAL-003` 仍为 downstream / out-of-scope 的事实。
