# EvidencePack

Task ID: OPS-001
Role: test
Date: 2026-03-30T02:06:00+08:00
Acceptance Gate: G1
Conclusion: pass

Supersedes:
- D:\xtquant-mcp\repo\docs\evidence_packs\OPS-001-test-202603300200.md

Supersede Reason:
- 上一份 EvidencePack（`...0200.md`）中 `data_gateway_calls.jsonl` 的 `flagged_lines=1` 统计错误。
- 本次已按用户独立复核口径纠偏并回写 manifest，且保留错误版本回溯链。

## Env Snapshot

- Reused Link: D:\xtquant-mcp\repo\docs\env_snapshots\OPS-001-test-202603300200.md
- Reason: 本次为同一主机、同一配置、短时间窗口内的证据纠偏；运行环境未变更，无需新增 EnvSnapshot。

## Test Scope

1. 仅对 OPS-001 二次最小复核的统计错误做测试证据纠偏。
2. 更正 quarantine manifest 中 mixed-content JSONL 项完整性字段，尤其 `flagged_lines`。
3. 保留错误版本 manifest 备份并维持回溯链。
4. 做最小必要运行态复核，确认纠偏动作未破坏 prod 证据链。

## Commands

1. 重新统计 mixed-content 日志（regex + json 双口径）
   - `Select-String -Path <jsonl> -Pattern '"source"\s*:\s*"fake"|mock|synthetic'`
   - `ConvertFrom-Json` 后统计 `envelope.data.source == "fake"`
2. 备份并修正 manifest
   - `Copy-Item manifest.json -> manifest.pre_recheck_correction_20260330020512.json`
   - 更新 mixed-content JSONL 项：
     - `flagged_lines=8`
     - `flagged_lines_count_method=json_envelope_data_source_eq_fake`
     - `regex_flagged_lines_reference=8`
3. 最小运行态复核
   - `GET /healthz` (8766, 8765)
   - `POST /mcp tools/call xtdata.status`

## Raw Results

- Corrected statistics for:
  - `D:\xtquant-mcp\instance\prod\artifacts\quarantine\202603300146\artifacts\data_gateway\20260327\data_gateway_calls.jsonl`
- Corrected values:
  - `total_lines=9`
  - `regex_flagged_lines=8`
  - `json_flagged_lines=8`
- Manifest corrected:
  - mixed-content JSONL item `flagged_lines: 8`（此前错误为 `1`）
  - `reason_rule: temporary_mixed_log_quarantine`
  - `contains_non_flagged_records: true`
  - `flagged_lines_count_method: json_envelope_data_source_eq_fake`
  - `regex_flagged_lines_reference: 8`
- Traceability chain preserved:
  - pre-correction backup exists: `manifest.pre_recheck_correction_20260330020512.json`
  - manifest retains `previous_manifest_backup` and `previous_manifest_backup_correction`
- Runtime spot check:
  - Data/Trade `/healthz` both `ok=true`, `evidence_scope=prod`
  - `xtdata.status` `ok=true`, `ready=true`, audit includes `trace_id/evidence_scope/state_root`

## Artifact Refs

- Updated Manifest:
  - D:\xtquant-mcp\instance\prod\artifacts\quarantine\202603300146\manifest.json
- Backup of incorrect manifest (this correction run):
  - D:\xtquant-mcp\instance\prod\artifacts\quarantine\202603300146\manifest.pre_recheck_correction_20260330020512.json
- Earlier backup (previous recheck run):
  - D:\xtquant-mcp\instance\prod\artifacts\quarantine\202603300146\manifest.pre_recheck_20260330020012.json
- Mixed-content log artifact:
  - D:\xtquant-mcp\instance\prod\artifacts\quarantine\202603300146\artifacts\data_gateway\20260327\data_gateway_calls.jsonl
- Superseded EvidencePack:
  - D:\xtquant-mcp\repo\docs\evidence_packs\OPS-001-test-202603300200.md

## Failure Classification

- fail_env: none in this correction run.
- fail_design: none in this correction run.
- blocked: none.

## Verdict

`pass`（仅限 OPS-001 本轮“测试证据纠偏”范围）。  
纠偏后字段与文件实际内容一致，且未破坏 prod 证据链；本结论不等于任务层 acceptance。

## Board Suggestion

- Suggested Status: remain `In Independent Test` and submit latest evidence to review.
- Suggested Note: `OPS-001-test-202603300206` supersedes `...0200`, corrected `flagged_lines` from `1` to `8`.

禁止自动进入下一卡。
