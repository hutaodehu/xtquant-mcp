# EvidencePack

Task ID: OPS-001
Role: test
Date: 2026-03-30T02:00:00+08:00
Acceptance Gate: G1
Conclusion: pass

## Env Snapshot

- Link: D:\xtquant-mcp\repo\docs\env_snapshots\OPS-001-test-202603300200.md
- Host: CHIYU (Windows 11)
- Shell: PowerShell 7
- Config:
  - D:\xtquant-mcp\instance\prod\config\data_gateway.local.yaml
  - D:\xtquant-mcp\instance\prod\config\trade_gateway.local.yaml

## Test Scope

1. 对 `OPS-001-review-202603300153` 的回流点做最小复核，只验证 mixed-content 日志处置精度与证据保全是否可同时成立。
2. 在不改 repo 代码/设计文档前提下，回溯化更新 quarantine manifest。
3. 做最小必要运行态复核，确认 prod 证据链未被本轮 mixed-log 保全动作破坏。

## Commands

1. 读取策略、review、现有 manifest 与 mixed-content 备份日志
   - `Get-Content docs/OPS-001_ARTIFACT_DISPOSITION.md`
   - `Get-Content docs/reviews/OPS-001-review-202603300153.md`
   - `Get-Content D:\xtquant-mcp\instance\prod\artifacts\quarantine\202603300146\manifest.json`
   - `Get-Content D:\xtquant-mcp\instance\prod\artifacts\quarantine\202603300146\artifacts\data_gateway\20260327\data_gateway_calls.jsonl`
2. 备份并更新 manifest（补完整性字段 + mixed-content reason）
   - `Copy-Item manifest.json -> manifest.pre_recheck_20260330020012.json`
   - PowerShell 脚本计算并写入：
     - `original_sha256`
     - `total_lines`
     - `flagged_lines`
     - `contains_non_flagged_records`
     - mixed-content JSONL 的 `reason_rule=temporary_mixed_log_quarantine`
3. 最小运行态复核
   - `Invoke-RestMethod http://127.0.0.1:8766/healthz`
   - `Invoke-RestMethod http://127.0.0.1:8765/healthz`
   - `POST /mcp tools/call xtdata.status`
   - `POST /mcp resources/read xtdata://service/status`
   - `POST /mcp resources/read trade://session/current`
   - `Test-NetConnection 127.0.0.1:58610/8765/8766`

## Raw Results

- Manifest 回溯链保留：
  - 旧版备份存在：`manifest.pre_recheck_20260330020012.json`
  - 新版 `manifest.json` 已写入 `recheck_updated_at`、`recheck_operator`、`previous_manifest_backup`
- 完整性字段（更新后）：
  - `xtdata_service_status.json`: `original_sha256=B92B0A4A...`, `total_lines=4`, `flagged_lines=1`, `contains_non_flagged_records=false`
  - `data_gateway_calls.jsonl`: `original_sha256=B0A4F330...`, `total_lines=9`, `flagged_lines=1`, `contains_non_flagged_records=true`
- mixed-content 判定与标记：
  - JSONL 项 `reason_rule` 已从 `known_historical_fake_artifact` 更新为 `temporary_mixed_log_quarantine`
- 运行态复核：
  - `/healthz` (8766/8765) 均 `ok=true`
  - 两端 `evidence_scope=prod`
  - `xtdata.status` 返回 `ready=true`，审计字段含 `trace_id/server_ts/evidence_scope/state_root`
  - `resources/read xtdata://service/status` 返回 runtime 非 fake 状态
  - `resources/read trade://session/current` 返回 `session_not_ready`（本卡范围外，不影响本次 mixed-log 最小复核）
  - 端口：`58610/8765/8766` 均 `TcpTestSucceeded=True`

## Artifact Refs

- Updated Manifest:
  - D:\xtquant-mcp\instance\prod\artifacts\quarantine\202603300146\manifest.json
- Previous Manifest Backup:
  - D:\xtquant-mcp\instance\prod\artifacts\quarantine\202603300146\manifest.pre_recheck_20260330020012.json
- Mixed-content Backup Log:
  - D:\xtquant-mcp\instance\prod\artifacts\quarantine\202603300146\artifacts\data_gateway\20260327\data_gateway_calls.jsonl
- Runtime State File:
  - D:\xtquant-mcp\instance\prod\state\data_resources\xtdata_service_status.json
- Fresh Call Logs:
  - D:\xtquant-mcp\instance\prod\artifacts\data_gateway\20260330\data_gateway_calls.jsonl
  - D:\xtquant-mcp\instance\prod\artifacts\trade_gateway\20260330\trade_gateway_calls.jsonl

## Failure Classification

- fail_env: none in this run.
- fail_design: none in this run.
- blocked: none in this run.

## Verdict

`pass`（仅限 OPS-001 本轮最小复核范围）。  
本轮已证明 mixed-content 日志处置可与证据保全并存：manifest 有可回溯备份、完整性字段齐全、JSONL 项不再被标成纯污染文件；同时 prod 运行态证据链保持可用。

## Board Suggestion

- Suggested Status: `In Independent Test` -> 可提交 `In Review`
- Suggested Release Wording: 本卡二次独立测试最小复核 `pass`，待 review 判定任务层是否放行。

禁止自动进入下一卡。
