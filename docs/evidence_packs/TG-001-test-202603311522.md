# EvidencePack

Task ID: TG-001
Role: independent test
Date: 2026-03-31 15:22:38 +08:00
Acceptance Standard Scope: `docs/ACCEPTANCE_STANDARD.md` target gate remains `G4`, but this pack only covers the user-authorized bounded deterministic validation of TG-001 implementation claims. No `G4` pass or `VAL-003` pass is claimed.
Change Package: `docs/change_packages/TG-001.md`
Task Card: `docs/task_cards/TG-001.md`

## Test Scope

1. Validate only TG-001 implementation-scope claims by repo inspection and deterministic repo-local checks.
2. Confirm public `order.place` is closed to the governed server-side write path and no longer presents thin-broker direct-submit semantics.
3. Confirm governed metadata and gate sequence are present as claimed.
4. Check contract-level non-regression risk around `order.status` and `order.cancel` boundaries only by code/docs/test inspection or deterministic fake-service execution.
5. Confirm TG-001 does not drift into `VAL-003` live validation scope.

## Inputs Read

- `D:\xtquant-mcp\repo\AGENTS.md`
- `D:\xtquant-mcp\repo\docs\task_cards\TG-001.md`
- `D:\xtquant-mcp\repo\docs\change_packages\TG-001.md`
- `D:\xtquant-mcp\repo\docs\MCP_DESIGN.md`
- `D:\xtquant-mcp\repo\docs\ACCEPTANCE_STANDARD.md`
- `D:\xtquant-mcp\repo\docs\EXECUTION_AND_ARTIFACT_STANDARD.md`
- `D:\xtquant-mcp\repo\docs\DESIGN_REVIEW_20260327.md`
- `D:\xtquant-mcp\repo\xtqmt_mcp\trade_gateway\server.py`
- `D:\xtquant-mcp\repo\xtqmt_mcp\trade_gateway\capability_v2.py`
- `D:\xtquant-mcp\repo\xtqmt_mcp\trade_gateway\prompts.py`
- `D:\xtquant-mcp\repo\tests\test_trade_gateway_server.py`
- `D:\xtquant-mcp\repo\docs\task_cards\VAL-003.md`
- Additional inspection used for contract boundary confirmation:
  - `D:\xtquant-mcp\repo\xtqmt_mcp\trade_ops.py`
  - `D:\xtquant-mcp\repo\xtqmt_mcp\trade_gateway\session_manager.py`

## Executed Commands

1. Repo-local deterministic unit tests:

```powershell
python -m unittest tests.test_trade_gateway_server -v
```

2. Repo-local routing/boundary probe with fake session manager and fake trade service. This command only instantiated local Python objects and did not connect to broker, place orders, or cancel orders:

```powershell
@'
from pathlib import Path
from types import SimpleNamespace
import sys

ROOT = Path(r'D:\xtquant-mcp\repo')
if str(ROOT) not in sys.path:
    sys.path.insert(0, str(ROOT))

from xtqmt_mcp.trade_gateway.server import TradeGatewayServer
from xtqmt_mcp.trade_gateway.config import TradeAuditConfig, TradeGatewayConfig, TradeLoginConfig, TradeOpsGatewayConfig
from xtqmt_mcp.settings import QmtInstallConfig, ServiceIdentity, ServiceRuntimePaths, TransportConfig, XtquantBundleConfig

root = ROOT / '.tmp' / 'tg001_test_probe'
for sub in ('config','logs','state','artifacts','vendor'):
    (root / sub).mkdir(parents=True, exist_ok=True)

config = TradeGatewayConfig(
    identity=ServiceIdentity(server_name='xtqmtTradeGateway', server_version='test'),
    runtime_paths=ServiceRuntimePaths(
        config_root=str(root / 'config'),
        logs_root=str(root / 'logs'),
        state_root=str(root / 'state'),
        artifact_root=str(root / 'artifacts'),
    ),
    bundle=XtquantBundleConfig(bundle_root=str(root / 'vendor')),
    qmt=QmtInstallConfig(account_id='ACC001'),
    transport=TransportConfig(bind_port=0),
    audit=TradeAuditConfig(enabled=False, call_log_root=str(root / 'artifacts' / 'trade_gateway')),
    trade_ops=TradeOpsGatewayConfig(
        account_id='ACC001',
        output_dir=str(root / 'artifacts' / 'trade_ops'),
        state_dir=str(root / 'state' / 'trade_ops'),
    ),
    login=TradeLoginConfig(account_id='ACC001'),
)
server = TradeGatewayServer(config, login_handler=lambda cfg: SimpleNamespace(as_payload=lambda: {'ok': True}))

class FakeService:
    def __init__(self):
        self.calls = []
    def order_status(self, broker_order_id):
        self.calls.append(('order.status', broker_order_id))
        from xtqmt_mcp.trade_ops import TradeOpsResult
        return TradeOpsResult(command='order.status', ok=True, payload={'ok': True, 'broker_order_id': broker_order_id, 'status': 'submitted'})
    def order_cancel(self, broker_order_id):
        self.calls.append(('order.cancel', broker_order_id))
        from xtqmt_mcp.trade_ops import TradeOpsResult
        return TradeOpsResult(command='order.cancel', ok=True, payload={'ok': True, 'broker_order_id': broker_order_id, 'status': 'cancel_requested'})

class RecordingSessionManager:
    def __init__(self, service):
        self.service = service
        self.execute_calls = []
    def execute(self, *, account_id='', runner=None, require_ready=False):
        self.execute_calls.append({'account_id': account_id, 'require_ready': require_ready})
        context = SimpleNamespace(service=self.service, wake_report={})
        state = SimpleNamespace(context=context)
        return state, runner(context)

service = FakeService()
manager = RecordingSessionManager(service)
server._session_manager = manager

for tool_name in ('order.status', 'order.cancel'):
    payload = server.dispatch({
        'jsonrpc': '2.0',
        'id': tool_name,
        'method': 'tools/call',
        'params': {'name': tool_name, 'arguments': {'broker_order_id': '123456'}}
    })
    structured = payload['result']['structuredContent']
    print({'tool': tool_name, 'ok': structured['ok'], 'data': structured['data']})

print({'execute_calls': manager.execute_calls, 'service_calls': service.calls})
'@ | python -
```

3. Focused code/reference inspection:

```powershell
rg -n "order\.place|order\.status|order\.cancel|require_ready = tool_name|governed_write_path|gate_sequence|governed server-side write path" `
  D:\xtquant-mcp\repo\xtqmt_mcp\trade_gateway\server.py `
  D:\xtquant-mcp\repo\xtqmt_mcp\trade_gateway\capability_v2.py `
  D:\xtquant-mcp\repo\xtqmt_mcp\trade_gateway\prompts.py `
  D:\xtquant-mcp\repo\tests\test_trade_gateway_server.py `
  D:\xtquant-mcp\repo\docs\MCP_DESIGN.md `
  D:\xtquant-mcp\repo\docs\task_cards\VAL-003.md
```

## Observed Results

1. `python -m unittest tests.test_trade_gateway_server -v` ran 5 tests and all 5 passed.
2. Existing deterministic test coverage confirms:
   - `order.place` tool description now says `governed broker order through the server-side write path` in `xtqmt_mcp/trade_gateway/server.py`.
   - `tests/test_trade_gateway_server.py` asserts the public `order.place` path no longer exposes `thin broker order` wording and that the request reaches `service.place_order(...)` with server-injected guard token `mcp_server_governed_write_path`.
   - The structured payload includes `governed_write_path=true`, `write_path=governed_service_order_place`, and the explicit gate sequence from `xtqmt_mcp/trade_gateway/capability_v2.py`.
3. Code inspection confirms the public MCP entry still routes `order.place` through `place_order_capability(...)`, while `place_order_capability(...)` is now only a compatibility adapter that calls `TradeOpsService.place_order(...)` and annotates governed metadata.
4. Prompt inspection confirms `trade-preflight` now describes `order.place` as a governed server-side write path with login/session/connectivity/write-permission/risk/kill-switch/audit gates and does not treat prompt text as the safety boundary.
5. The fake-service routing probe confirmed both `order.status` and `order.cancel` dispatch through `session_manager.execute(account_id=\"\", require_ready=True)` and return `primary_account` contract metadata, so the TG-001 write-path refactor did not introduce an obvious contract-level boundary regression for those tools.
6. `docs/MCP_DESIGN.md` and `docs/task_cards/VAL-003.md` remain aligned that live `G4` proof is downstream work: the minimum real-order evidence chain stays in `VAL-003`, and this TG-001 bounded validation does not claim it.

## Findings

1. No `fail_design` finding was observed within the bounded deterministic scope executed here.
2. TG-001 implementation-scope claims are supported by code, docs, and deterministic tests: public `order.place` is closed to the governed server-side write path, explicit governed metadata is present, and thin-broker direct-submit wording is removed from the public tool contract.
3. `order.status` and `order.cancel` remain on the same owner-session ready-gated boundary by inspection and fake-service execution, which reduces immediate contract-level regression risk around the shared session/audit boundary described in `docs/MCP_DESIGN.md`.
4. Remaining unclosed risk is intentionally out of scope for this pack: no live broker `G4` proof was attempted, and `VAL-003` still owns the minimum real-order chain.

## Evidence Paths

- EvidencePack: `docs/evidence_packs/TG-001-test-202603311522.md`
- ChangePack: `docs/change_packages/TG-001.md`
- TaskCard: `docs/task_cards/TG-001.md`
- Downstream live-validation boundary: `docs/task_cards/VAL-003.md`
- Design contract:
  - `docs/MCP_DESIGN.md:217`
  - `docs/MCP_DESIGN.md:231`
  - `docs/MCP_DESIGN.md:233`
  - `docs/MCP_DESIGN.md:481`
  - `docs/MCP_DESIGN.md:483`
- Implementation and prompt boundary:
  - `xtqmt_mcp/trade_gateway/server.py:212`
  - `xtqmt_mcp/trade_gateway/server.py:478`
  - `xtqmt_mcp/trade_gateway/server.py:482`
  - `xtqmt_mcp/trade_gateway/server.py:486`
  - `xtqmt_mcp/trade_gateway/server.py:493`
  - `xtqmt_mcp/trade_gateway/capability_v2.py:29`
  - `xtqmt_mcp/trade_gateway/capability_v2.py:33`
  - `xtqmt_mcp/trade_gateway/capability_v2.py:35`
  - `xtqmt_mcp/trade_gateway/prompts.py:27`
  - `xtqmt_mcp/trade_gateway/prompts.py:37`
- Deterministic repo-local test references:
  - `tests/test_trade_gateway_server.py:236`
  - `tests/test_trade_gateway_server.py:237`
  - `tests/test_trade_gateway_server.py:383`
  - `tests/test_trade_gateway_server.py:386`
  - `tests/test_trade_gateway_server.py:388`

## Failure Classification

`partial`

Reason: bounded deterministic validation passed, but TG-001's task-level highest gate remains `G4` and no live governed-write proof was executed here. That remaining gap is downstream `VAL-003` scope, not a failure of the bounded checks performed in this pack.

## Formal Conclusion

`partial`

Within the bounded non-live scope authorized for this independent test pass, TG-001 implementation claims are validated and no contract-level regression was found around the inspected `order.status` / `order.cancel` boundaries. This EvidencePack does not claim `G4 pass`, does not claim `VAL-003 pass`, and does not claim broker-live readiness.

## Recommended Next Board Status

`In Review`

Reason: TG-001 has completed bounded independent test coverage required for implementation-scope review. Review should assess whether the deterministic evidence is sufficient for code/contract closure while keeping live governed-write acceptance in `VAL-003`.
