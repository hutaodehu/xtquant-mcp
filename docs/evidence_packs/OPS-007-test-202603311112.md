# EvidencePack

Task ID: OPS-007
Role: test
Date: 2026-03-31T11:12:00+08:00
Stage: independent-test-doc-contract
Acceptance Standard: `docs/ACCEPTANCE_STANDARD.md` (`G0`, governance/doc-contract scope only)
ChangePack: `docs/change_packages/OPS-007.md`

## Test Scope

本次仅执行文档/契约级独立核对，不推断 harness 运行行为，不验证脚本实现，不修改任何文档。

核对目标：

1. 确认 adapter mapping 没有把 board、聊天摘要、`Board Export`、`Board Sync`、controller judgment 升格成 truth carrier、validation evidence 或 release source。
2. 确认 `task_truth_carriers`、`change_evidence_carriers`、`validation_evidence_carriers`、`final_release_sources`、`env_snapshot_sources` 与当前仓库执行/协作规则一致。
3. 确认未闭合 adapter gap 被诚实登记，而不是伪装成已结构化解决。

## Inputs Read

- `docs/task_cards/OPS-007.md`
- `docs/change_packages/OPS-007.md`
- `docs/universal_skills/contracts/ADAPTER_CONTRACT.md`
- `docs/universal_skills/README.md`
- `docs/EXECUTION_AND_ARTIFACT_STANDARD.md`
- `docs/WORKFLOW_AND_BOARD.md`
- `AGENTS.md`
- `docs/ACCEPTANCE_STANDARD.md`
- `docs/evidence_packs/README.md`

## Executed Commands

1. `Get-Content -Path docs/task_cards/OPS-007.md`
   - 结果：确认本卡为 `governance`，当前角色为 `test`，目标是收口 TaskCard/ChangePack-style adapter 映射，并要求显式暴露未闭合缺口。
2. `Get-Content -Path docs/change_packages/OPS-007.md`
   - 结果：确认变更范围限定为 `ADAPTER_CONTRACT.md`、`README.md`、`EXECUTION_AND_ARTIFACT_STANDARD.md`、`WORKFLOW_AND_BOARD.md`、`AGENTS.md` 中的 adapter mapping、ledger boundary 与 gap discipline。
3. `Get-Content -Path docs/universal_skills/contracts/ADAPTER_CONTRACT.md`
4. `Get-Content -Path docs/universal_skills/README.md`
5. `Get-Content -Path docs/EXECUTION_AND_ARTIFACT_STANDARD.md`
6. `Get-Content -Path docs/WORKFLOW_AND_BOARD.md`
7. `Get-Content -Path AGENTS.md`
8. `Get-Content -Path docs/ACCEPTANCE_STANDARD.md`
9. `Get-Content -Path docs/evidence_packs/README.md`
   - 结果：获取 repo-local 规则、验收结论词、EvidencePack 命名约束。
10. `rg -n "task_truth_carriers|change_evidence_carriers|validation_evidence_carriers|final_release_sources|env_snapshot_sources|Board Export|Board Sync|controller judgment|聊天摘要|RunLedger|Review Result|Accepted" docs/universal_skills/contracts/ADAPTER_CONTRACT.md docs/universal_skills/README.md docs/EXECUTION_AND_ARTIFACT_STANDARD.md docs/WORKFLOW_AND_BOARD.md AGENTS.md`
    - 结果：定位 carrier 映射与 board/control-plane 边界条目。
11. `rg -n "machine-readable|typed schema|外部 approval|gap|缺口|未闭合" docs/universal_skills/contracts/ADAPTER_CONTRACT.md docs/universal_skills/README.md docs/EXECUTION_AND_ARTIFACT_STANDARD.md docs/WORKFLOW_AND_BOARD.md AGENTS.md`
    - 结果：定位 gap 声明与 gap discipline。
12. `rg -n "结论词汇|pass|partial|blocked|fail_env|fail_design|Accepted|ReviewPack|EnvSnapshot" docs/ACCEPTANCE_STANDARD.md docs/EXECUTION_AND_ARTIFACT_STANDARD.md docs/WORKFLOW_AND_BOARD.md AGENTS.md`
    - 结果：确认正式结论词、ReviewPack/EnvSnapshot/Accepted 约束。
13. `$lines = Get-Content docs/universal_skills/contracts/ADAPTER_CONTRACT.md; $start=60; $end=75; for($i=$start; $i -le $end; $i++){ '{0}:{1}' -f $i, $lines[$i-1] }`
14. `$lines = Get-Content docs/universal_skills/contracts/ADAPTER_CONTRACT.md; $start=112; $end=133; for($i=$start; $i -le $end; $i++){ '{0}:{1}' -f $i, $lines[$i-1] }`
15. `$lines = Get-Content docs/EXECUTION_AND_ARTIFACT_STANDARD.md; $start=44; $end=76; for($i=$start; $i -le $end; $i++){ '{0}:{1}' -f $i, $lines[$i-1] }`
    - 结果：补取关键上下文行号，用于比对 generic example 与 repo-local 正式规则。

## Observed Results

### Check 1: board / control-plane 未在 repo-local mapping 中被提升为正式 carrier

通过的证据：

- `docs/universal_skills/contracts/ADAPTER_CONTRACT.md:115-120`
  - 明确写出 `ReviewPack` 才是正式 release / rollback judgment 主载体。
  - 明确写出 `Board Export` 不是 `task_truth_carriers`、`change_evidence_carriers` 或 `validation_evidence_carriers`。
  - 明确写出 `Board Sync` 与 controller judgment 属于 control-plane，不升级为角色工件或 `final_release_sources`。
- `docs/EXECUTION_AND_ARTIFACT_STANDARD.md:64-70`
  - 将 `RunLedger` / 外部看板、`Board Export`、`Board Sync`、controller judgment、聊天摘要全部列入“非 authority / 非工件对象”。
- `docs/WORKFLOW_AND_BOARD.md:26-38`
  - 明确看板字段只做索引或 ledger mirror；`Review Result`、`Accepted` 等不能单独生成 authority。
- `AGENTS.md:68-71`
  - 明确 repo-local formal carriers 仅为 `TaskCard`、`ChangePack`、`EvidencePack`、`ReviewPack`、`EnvSnapshot` 及显式回链 artifact；board/control-plane 不替代这些工件。

结论：repo-local 正式映射层面，这一项基本满足测试目标。

### Check 2: 关键 carrier 映射与现行仓库规则一致

通过的证据：

- `docs/universal_skills/contracts/ADAPTER_CONTRACT.md:83-107`
  - `task_truth_carriers` 绑定 `docs/task_cards/<TaskID>.md` 与 `Repo Spec Link` 目标。
  - `change_evidence_carriers` 绑定 `docs/change_packages/<TaskID>.md` 与被 ChangePack 回链的文件/artifact。
  - `validation_evidence_carriers` 绑定 `docs/evidence_packs/...`、必要时的 `docs/env_snapshots/...` 以及被 EvidencePack 回链的原始 artifact。
  - `env_snapshot_sources` 绑定 `docs/env_snapshots/...`。
  - `final_release_sources` 绑定 `docs/reviews/...`、`docs/ACCEPTANCE_STANDARD.md` 中适用 gate 和任务要求的外部 approval artifact。
- `docs/EXECUTION_AND_ARTIFACT_STANDARD.md:44-53`
  - 与上面的 repo-local mapping 一致。
- `docs/WORKFLOW_AND_BOARD.md:26-31`
  - 看板字段仅作为这些 carrier 的链接入口或 ledger mirror，与 repo-local carrier 分工一致。
- `AGENTS.md:69-70`
  - 再次确认 board status / review result 不生成 `validation_authority` 或 `release_authority`。

结论：repo-local carrier 映射与现行执行规范、协作规则和验收规则一致，未发现把 `EnvSnapshot`、`EvidencePack`、`ReviewPack` 的职责混写为看板状态的情况。

### Check 3: unresolved adapter gaps 是否被诚实登记

通过的证据：

- `docs/universal_skills/contracts/ADAPTER_CONTRACT.md:122-127`
  - 显式登记当前缺少 machine-readable adapter manifest。
  - 显式登记 `runtime_health_sources`、`runtime_state_sources`、`resource_sources` 尚无 repo-wide typed schema。
  - 显式登记 `ReviewPack` 之外 external approval artifact 尚未统一命名或目录。
  - 显式声明 repo-only sync payload / board mirror 不承载最终 release authority。
- `docs/EXECUTION_AND_ARTIFACT_STANDARD.md:72-76`
  - 规定缺口必须显式登记，不能把 board mirror 当独立证据，也不能把没有 artifact 落点的 external approval 当成已满足。
- `docs/universal_skills/README.md:48-49`
  - 明确 adapter gap 必须写回 repo adapter 文档，不能在通用抽象层假装闭合。
- `AGENTS.md:71`
  - 明确 runtime/resource/external approval 若无稳定 carrier，必须显式登记 gap。

结论：未闭合 adapter gap 的暴露是诚实的，没有被伪装成已结构化解决。

## Finding

### Finding 1

- Severity: medium
- Type: design documentation inconsistency
- Evidence:
  - `docs/universal_skills/contracts/ADAPTER_CONTRACT.md:62-70`
    - 通用“示例 3：TaskCard/ChangePack-style adapter”仍写有 `final_release_sources`：`ReviewPack`、`board/review gate`。
  - `docs/universal_skills/contracts/ADAPTER_CONTRACT.md:118-120`
    - 同一文件的 repo-local 正式约束又明确声明 board 状态只能镜像 `ReviewPack` 结论，`Board Sync` 与 controller judgment 不会升级为 `final_release_sources`。
  - `docs/EXECUTION_AND_ARTIFACT_STANDARD.md:53,64-70`
    - 当前仓库正式规则明确 `final_release_sources` 为 `docs/reviews/...`、适用 gate 与外部 approval artifact，并把 `RunLedger` / board / controller judgment 列为非 authority / 非工件对象。
- Impact:
  - repo-local 映射本身是正确的，但该 generic example 仍可能误导后续读者，以为 TaskCard/ChangePack-style adapter 可以把 board/review gate 作为 release source 组成部分。
- Required fix:
  - 收紧 `ADAPTER_CONTRACT.md` 的 generic example 3，使其不再把 `board/review gate` 写入 `final_release_sources`，或明确标注该示例不适用于当前仓库的 authority 边界。

## Evidence Paths

- `docs/task_cards/OPS-007.md`
- `docs/change_packages/OPS-007.md`
- `docs/universal_skills/contracts/ADAPTER_CONTRACT.md`
- `docs/universal_skills/README.md`
- `docs/EXECUTION_AND_ARTIFACT_STANDARD.md`
- `docs/WORKFLOW_AND_BOARD.md`
- `AGENTS.md`
- `docs/ACCEPTANCE_STANDARD.md`
- `docs/evidence_packs/README.md`

## Failure Classification

- 发现的问题属于文档契约口径不一致，归类为设计侧问题，不是环境问题。

## Formal Conclusion

`partial`

理由：

- repo-local 正式 mapping、ledger/control-plane 边界与 gap discipline 基本一致，核心目标大部分满足。
- 但 `ADAPTER_CONTRACT.md` 的 generic example 3 仍保留 `board/review gate` 作为 `final_release_sources` 的表述，和当前仓库正式规则存在可误读冲突，因此不宜判为 `pass`。

## Recommended Next Board Status

`In Dev`

理由：

- 需要先修正文档契约示例层面的残留歧义，再回到独立测试确认收口结果。
