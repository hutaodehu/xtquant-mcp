# EvidencePack

Task ID: OPS-008
Role: test
Date: 2026-04-13T18:57:20+08:00
Acceptance Gate: G4
Stage: independent-test-first-evidence
Conclusion: partial

## Scope

本次独立测试严格限定为 `OPS-008` 的实现范围验证，只检查以下 claim：

1. `trade_write_authority_latest.json` 的机器输入已经切到 packet-bound typed carrier，不再依赖 `CURRENT_STATUS.md` Markdown 解析。
2. authority 生成逻辑在 typed carrier 缺字段、缺关键 ref、缺 judgment 内容或 `trace_id` 与 `diag_probe.resource_trace_id` 不一致时会 fail closed。
3. `trace mismatch` 不会把 authority 拼绿，`current_truth_source` 仍固定为 typed source。
4. `scripts/run_controller_direct_test.ps1` 已负责落 typed authority source 与 latest pointer，`scripts/run_trade_write_authority_check.ps1` 已改为消费该 typed source。

本次明确不做：

1. 不执行 live `controller direct test`，不生成新的 runtime packet。
2. 不修改 `VAL-003` 的 blocked posture，不改 formal truth，不改 `CURRENT_STATUS.md`。
3. 不创建 `ReviewPack`，不宣告 closeout，不把本次独立测试外推成 `G4 pass` 或 release authority。

## Inputs Read

- `AGENTS.md`
- `docs/task_cards/OPS-008.md`
- `docs/change_packages/OPS-008.md`
- `docs/MCP_DESIGN.md`
- `docs/EXECUTION_AND_ARTIFACT_STANDARD.md`
- `docs/WORKFLOW_AND_BOARD.md`
- `docs/ACCEPTANCE_STANDARD.md`
- `docs/VAL-003_G4_EXECUTION_PLAN.md`
- `docs/evidence_packs/TG-001-test-202603311525.md`
- `docs/env_snapshots/TG-005-test-202603311916.md`
- `xtqmt_mcp/trade_write_authority.py`
- `tests/test_trade_write_authority.py`
- `scripts/run_trade_write_authority_check.ps1`
- `scripts/run_controller_direct_test.ps1`

## Commands

1. `python -m unittest tests.test_trade_write_authority -v`
2. `rg -n "CURRENT_STATUS|current_status|trade_write_authority_source_latest|authority_source_incomplete|authority_source_trace_mismatch|typed_authority_source|formal_trade_write_lane_not_closed" xtqmt_mcp/trade_write_authority.py scripts/run_trade_write_authority_check.ps1 scripts/run_controller_direct_test.ps1 tests/test_trade_write_authority.py docs/MCP_DESIGN.md docs/change_packages/OPS-008.md docs/task_cards/OPS-008.md`
3. `pwsh -NoLogo -NoProfile -Command "[void][scriptblock]::Create((Get-Content -LiteralPath 'D:\\xtquant-mcp\\repo\\scripts\\run_controller_direct_test.ps1' -Raw)); 'run_controller_direct_test.ps1 OK'"`
4. `pwsh -NoLogo -NoProfile -Command "[void][scriptblock]::Create((Get-Content -LiteralPath 'D:\\xtquant-mcp\\repo\\scripts\\run_trade_write_authority_check.ps1' -Raw)); 'run_trade_write_authority_check.ps1 OK'"`
5. `pwsh -NoLogo -NoProfile -Command '$PSVersionTable.PSVersion.ToString(); Resolve-Path "D:\xtquant-mcp\repo"; Resolve-Path "D:\xtquant-mcp\instance\prod"; Resolve-Path "D:\xtquant-mcp\repo\scripts\run_controller_direct_test.ps1"; Resolve-Path "D:\xtquant-mcp\repo\scripts\run_trade_write_authority_check.ps1"'`
6. 离线结构化脚本级探针：
   在 `%TEMP%\ops008-authority-probe2-b022a5dbbd114e248e8c8a0f48f5919c` 下构造临时 `InstanceRoot`，
   复制 `diag_probe_latest.json` 到 `state\trade_resources\diag_probe_latest.json`，
   按 `scripts/run_controller_direct_test.ps1:1730-1760` 的 carrier 字段形状写入
   `state\trade_resources\trade_write_authority_source_latest.json`，
   然后实际调用
   `pwsh -NoLogo -NoProfile -File D:\xtquant-mcp\repo\scripts\run_trade_write_authority_check.ps1 -RepoRoot D:\xtquant-mcp\repo -InstanceRoot C:\Users\Yun\AppData\Local\Temp\ops008-authority-probe2-b022a5dbbd114e248e8c8a0f48f5919c`
7. 代码复核：
   `xtqmt_mcp/trade_write_authority.py:18-34`
   `xtqmt_mcp/trade_write_authority.py:93-114`
   `xtqmt_mcp/trade_write_authority.py:154-216`
   `xtqmt_mcp/trade_write_authority.py:290-320`
   `tests/test_trade_write_authority.py:72-179`
   `scripts/run_trade_write_authority_check.ps1:40-56`
   `scripts/run_controller_direct_test.ps1:1812-1816`
   `scripts/run_controller_direct_test.ps1:2199-2218`
   `docs/MCP_DESIGN.md:255-273`
   `docs/ACCEPTANCE_STANDARD.md:175-205`

## Raw Results

### Command 1

`python -m unittest tests.test_trade_write_authority -v` 共执行 `7` 个测试，结果全部 `OK`。

覆盖到的关键断言：

1. `tests/test_trade_write_authority.py:72-84`
   `build_trade_write_authority_report(...)` 在 typed source 驱动下返回 `current_truth_source="typed_authority_source"`，且 formal closeout 未闭合时保持 `blocking_reason="formal_trade_write_lane_not_closed"`，没有回退到 `CURRENT_STATUS.md` Markdown 解析。
2. `tests/test_trade_write_authority.py:86-97`
   `review_ref` 缺失时返回 `authority_source_incomplete`。
3. `tests/test_trade_write_authority.py:98-108`
   `trace_id` 与 `diag_probe.resource_trace_id` 不一致时返回 `authority_source_trace_mismatch`，不会拼绿。
4. `tests/test_trade_write_authority.py:110-143`
   只有 typed source formal closeout 为绿、`fresh_connect_verified=true`、`write_permission_ready=true` 且 trace 对齐时才返回 `pass`。
5. `tests/test_trade_write_authority.py:145-179`
   authority source 缺失、`packet_id` 缺失、`controller_judgment_content` 缺失都返回 `authority_source_incomplete`。

测试输出摘要：

```text
Ran 7 tests in 0.001s

OK
```

### Command 2

`rg` 命中结果与本卡 claim 一致：

1. `docs/MCP_DESIGN.md:255-273`
   formal contract 已明确：
   `trade_write_authority_latest.json` 必须来自 `trade_write_authority_source_latest.json`，若关键字段缺失或 trace mismatch，`MUST fail closed`，不得回退去猜 `CURRENT_STATUS.md`。
2. `xtqmt_mcp/trade_write_authority.py:15`
   默认 authority source latest pointer 固定为 `D:/xtquant-mcp/instance/prod/state/trade_resources/trade_write_authority_source_latest.json`。
3. `xtqmt_mcp/trade_write_authority.py:164-184`
   fail-closed 分支按顺序覆盖：
   `authority_source_incomplete`
   `authority_source_trace_mismatch`
   `formal_trade_write_lane_not_closed`
   `fresh_connect_not_verified`
   `trade_write_authority_failed`
4. `xtqmt_mcp/trade_write_authority.py:215`
   当前 truth source 明确写死为 `typed_authority_source`。
5. `scripts/run_trade_write_authority_check.ps1:43-55`
   检查脚本默认把 instance root 下的 `state\\trade_resources\\trade_write_authority_source_latest.json` 传给 Python 模块。
6. `scripts/run_controller_direct_test.ps1:1812-1816,2199-2218`
   live runner 已为 fresh packet 生成 `trade_write_authority_source`、latest pointer，并把 EvidencePack / EnvSnapshot / runtime capture 一并回链。
7. `docs/change_packages/OPS-008.md:15-18,40,44-45`
   开发声明与独立测试关注点一致，未发现 claim 漂移。

### Command 3

PowerShell 解析烟测返回：

```text
run_controller_direct_test.ps1 OK
```

### Command 4

PowerShell 解析烟测返回：

```text
run_trade_write_authority_check.ps1 OK
```

### Command 5

最小环境与路径事实：

```text
7.6.0

Path
----
D:\xtquant-mcp\repo
D:\xtquant-mcp\instance\prod
D:\xtquant-mcp\repo\scripts\run_controller_direct_test.ps1
D:\xtquant-mcp\repo\scripts\run_trade_write_authority_check.ps1
```

对应最小环境快照见：[OPS-008-test-202604131857.md](../env_snapshots/OPS-008-test-202604131857.md)。

### Command 6

离线结构化脚本级探针在不执行 live `controller direct test` 的前提下，实际证明了
`run_trade_write_authority_check.ps1` 会从 `InstanceRoot` 默认 latest pointer 取 typed source，
并生成 authority 输出文件。

探针输入与落位：

1. 临时 `InstanceRoot`：
   `C:\Users\Yun\AppData\Local\Temp\ops008-authority-probe2-b022a5dbbd114e248e8c8a0f48f5919c`
2. typed latest pointer：
   `C:\Users\Yun\AppData\Local\Temp\ops008-authority-probe2-b022a5dbbd114e248e8c8a0f48f5919c\state\trade_resources\trade_write_authority_source_latest.json`
   - `mtime_utc=2026-04-13T11:13:19.395038+00:00`
   - `size=2759`
3. authority 输出：
   `C:\Users\Yun\AppData\Local\Temp\ops008-authority-probe2-b022a5dbbd114e248e8c8a0f48f5919c\state\trade_resources\trade_write_authority_latest.json`
   - `mtime_utc=2026-04-13T11:13:19.657616+00:00`
   - `size=4427`
4. `diag_probe` 副本：
   `C:\Users\Yun\AppData\Local\Temp\ops008-authority-probe2-b022a5dbbd114e248e8c8a0f48f5919c\state\trade_resources\diag_probe_latest.json`
   - `mtime_utc=2026-04-13T07:03:03.376476+00:00`
   - `trace_id=04003498-621d-4e0f-a7dd-5608b93fe389`

authority 输出关键字段如下：

```json
{
  "status": "fail",
  "blocking_reason": "formal_trade_write_lane_not_closed",
  "current_truth_source": "typed_authority_source",
  "current_truth_ref": "C:\\Users\\Yun\\AppData\\Local\\Temp\\ops008-authority-probe2-b022a5dbbd114e248e8c8a0f48f5919c\\state\\trade_resources\\trade_write_authority_source_latest.json",
  "packet_id": "ops008-offline-probe",
  "trace_id": "04003498-621d-4e0f-a7dd-5608b93fe389",
  "resource_trace_id": "04003498-621d-4e0f-a7dd-5608b93fe389",
  "controller_direct_executed_test": true,
  "controller_direct_summary": "offline authority probe only",
  "warnings": []
}
```

该结果与脚本入口逻辑一致：

1. `scripts/run_trade_write_authority_check.ps1:43-48` 在未传 `-AuthoritySourcePath` 时，默认从
   `<InstanceRoot>\state\trade_resources\trade_write_authority_source_latest.json` 取 source，
   并把输出写到 `<InstanceRoot>\state\trade_resources\trade_write_authority_latest.json`。
2. 本次 authority 输出里的 `current_truth_ref` 正是上述临时 latest pointer 路径，不是 packet id、
   不是 `CURRENT_STATUS.md`、也不是 prod instance 路径。
3. 临时 carrier 保持了 `scripts/run_controller_direct_test.ps1:1730-1760` 的字段骨架与 open formal posture，
   只把 `review_ref` 填成临时占位 artifact，以避免顶层 blocker 先退化成 `authority_source_incomplete`，
   从而把脚本对 latest pointer 的真实消费路径单独暴露出来。

## Verification Notes

1. typed carrier 已成为 authority 的单一机器输入主路径。
   `xtqmt_mcp/trade_write_authority.py:301-315` 的 `main(...)` 只把 `authority_source_path` 与可选 `diag_probe_path` 送入 `build_from_paths(...)`，不再从 `CURRENT_STATUS.md` 读取机器 truth。
2. fail-closed 已有独立测试证据，而不是只停留在 ChangePack 声明。
   缺字段、缺 ref、缺 judgment 内容都返回 `authority_source_incomplete`；
   trace mismatch 返回 `authority_source_trace_mismatch`；
   formal lane 未闭合时返回 `formal_trade_write_lane_not_closed`。
3. `trace mismatch` 不拼绿这一点已被测试和代码双重支持。
   代码在 `xtqmt_mcp/trade_write_authority.py:167-169` 先于任何 green claim 判定 mismatch；
   测试在 `tests/test_trade_write_authority.py:98-108` 明确断言 fail。
4. `run_controller_direct_test.ps1` 与 `run_trade_write_authority_check.ps1` 的串接已形成 typed source 闭环。
   前者负责写 `trade_write_authority_source_latest.json` 与回链元数据；
   后者默认从 instance latest pointer 取 source 并调用 `xtqmt_mcp.trade_write_authority`。
   新增的离线结构化脚本级探针进一步证明：
   只要 `InstanceRoot` 指向临时目录，脚本就会消费该临时目录内的 latest pointer，
   并把 authority 输出也落到同一临时 `state\trade_resources` 下。
5. authority 机器输入不再依赖 `CURRENT_STATUS.md`，但 CLI 仍保留了 `--current-status-path` 参数。
   `xtqmt_mcp/trade_write_authority.py:295` 仍声明该参数；
   `xtqmt_mcp/trade_write_authority.py:301-315` 未实际消费它。
   这不构成当前实现 blocker，但属于 operator 可读性残留。
6. 本次不能 closeout。
   `docs/ACCEPTANCE_STANDARD.md:178,205` 明确规定：
   `write_authority_ready=true` 不能等价于 `trade_write_authority` 或 `G4 pass`；
   formal authority green 只能出现在 fresh packet 完成、独立 `ReviewPack` 产出并完成 state truth refresh 之后。
   本次只形成 bounded independent-test evidence，且按任务边界没有创建 ReviewPack，也没有改动 `VAL-003` 或 formal truth。

## Findings

本次 scoped 独立验证未发现新的 `fail_design` 或 `fail_env` finding。

需要保留的残留风险：

1. `--current-status-path` 兼容参数仍暴露在 CLI 上，虽然实现已不消费它，后续可能继续误导 operator 以为 `CURRENT_STATUS.md` 仍参与机器路径。
2. `scripts/run_controller_direct_test.ps1:1740,1756` 当前仍把 `review_ref` / `artifact_refs.review_pack` 写成空字符串。
   代码评审已指出这会让 authority 顶层 blocker 退化成 `authority_source_incomplete`；
   本次离线 probe 为了验证 latest pointer 消费路径，使用了临时 placeholder review artifact 绕开该退化，
   因此这条风险仍保留，且本次未修复。
3. 本次没有执行 live fresh packet，因此没有验证真实 `trade_write_authority_source_latest.json` 产物在实例目录中的端到端落位，只验证了 deterministic 测试、PowerShell 解析，以及临时 instance root 下的脚本真实消费路径。

## Failure Classification

- 本次不归类为新的 `fail_env` 或 `fail_design`。
- 当前正式结论是 `partial`：
  已形成首个独立测试证据，但尚未形成 review authority，也没有 live packet 级 formal truth refresh。

## Evidence Paths

- [OPS-008 TaskCard](../task_cards/OPS-008.md)
- [OPS-008 ChangePack](../change_packages/OPS-008.md)
- [OPS-008 EnvSnapshot](../env_snapshots/OPS-008-test-202604131857.md)
- [MCP_DESIGN.md](../MCP_DESIGN.md)
- [EXECUTION_AND_ARTIFACT_STANDARD.md](../EXECUTION_AND_ARTIFACT_STANDARD.md)
- [WORKFLOW_AND_BOARD.md](../WORKFLOW_AND_BOARD.md)
- [ACCEPTANCE_STANDARD.md](../ACCEPTANCE_STANDARD.md)
- `xtqmt_mcp/trade_write_authority.py`
- `tests/test_trade_write_authority.py`
- `scripts/run_trade_write_authority_check.ps1`
- `scripts/run_controller_direct_test.ps1`

## Formal Conclusion

`partial`

理由：

1. `OPS-008` 声称的三个核心实现点已经得到独立测试支持：
   typed carrier 主路径、fail-closed、防止 trace mismatch 拼绿。
2. `run_controller_direct_test.ps1 -> trade_write_authority_source_latest.json -> run_trade_write_authority_check.ps1 -> xtqmt_mcp.trade_write_authority` 的入口链已在代码与 PowerShell 解析层面形成一致证据。
   新增离线结构化脚本级探针后，已补上“脚本真实消费 latest pointer 并写出 authority 文件”的一手运行证据。
3. 但当前只形成首个独立测试 EvidencePack，不能据此宣布 closeout。
   本次未生成 fresh live packet；
   未创建 `ReviewPack`；
   未执行 state truth refresh；
   `VAL-003` 的 blocked posture 也按要求保持不变。

## Recommended Next Board Status

- Target Status: `In Independent Test`
- Reason: `OPS-008` 已从 `In Self-Test` 进入正式独立测试阶段，并有首个 EvidencePack 与最小 EnvSnapshot 可回链；但按当前边界不得推进到 `In Review` 或 `Accepted`。
