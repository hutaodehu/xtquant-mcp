# ReviewPack

Task ID: TG-003
Role: review
Date: 2026-03-30T15:06:00+08:00
Change Package Link: D:\xtquant-mcp\repo\docs\change_packages\TG-003.md
Evidence Pack Link: D:\xtquant-mcp\repo\docs\evidence_packs\TG-003-test-202603300140.md
Env Snapshot Link: D:\xtquant-mcp\repo\docs\env_snapshots\TG-003-test-202603300140.md
Supersedes: D:\xtquant-mcp\repo\docs\reviews\TG-003-review-202603300150.md
Supporting Validation Evidence: D:\xtquant-mcp\repo\docs\evidence_packs\VAL-002-test-20260330-142745-live-gateway-rerun-orders-fallback.md; D:\xtquant-mcp\repo\docs\reviews\VAL-002-review-20260330-143606-live-gateway-rerun-orders-fallback.md

## Findings

1. 旧 blocker 已关闭。`TG-003-review-202603300150.md` 明确写明唯一剩余阻断是“缺少真实 MiniQMT/xtquant 的完整 `G3` live 只读链路证据”；`VAL-002-test-20260330-142745-live-gateway-rerun-orders-fallback.md` 与其 review 已正式补齐这条链，按顺序覆盖了 `miniqmt.ensure_logged_in -> session.warm -> session.status -> probe.connection -> account.show -> positions.list -> orders.list -> snapshot.l1`，且 `/healthz`、MCP 原始响应、`trace_id`、`server_ts`、artifact 路径均已留档。
2. 新 live `G3` 证据与 `TG-003` 的目标契约一致。`probe.connection` 与 `diag://probe/latest` 在真实运行态中同时暴露 `readiness_layers.read_only`、`readiness_layers.write_permission` 和 `write_permission_probe.implies_write_permission=false`，说明只读 readiness 与写权限探测已经拆层表达，而不是把写路径前置条件混写成总体只读不可用。
3. `orders.list` 在这轮 live rerun 中以 `ok=true` 的 public degraded fallback 完成 `G3` 只读链路，同时显式保留 `broker_read.error=xttrader connect failed: -1 ...`。这对 `TG-003` 是正向 closure，不是 blocker：它证明只读消费面可以成立且不会把 broker 子路径失败伪装成写路径 ready；同一证据也明确说明这不构成 `G4` 放行。

## Severity

- highest: low

## Impact

`TG-003` 的验收门槛是 `G3`，不是 `G4`。现在该卡已经同时具备：

1. 本卡自有的代码级/定向回归证据；
2. `VAL-002` 提供的完整 live `G3` 运行态证据；
3. 没有新的 `fail_design` 被证明。

因此，旧 review 的唯一阻断条件已经消失，`TG-003` 可以正式从 `In Independent Test` 收口到 `Accepted`。但这不改变 higher gate 的写路径阻断状态。

## Required Fix

1. 对 `TG-003` 本卡范围：无新的 blocking fix。
2. 对更高 gate：继续单独保持 `VAL-003/G4` 阻断，不能把这次 `G3` 通过外推成 broker 写路径已就绪。

## Release Decision

- Decision: pass
- Summary: `VAL-002` 的最新 live `G3` EvidencePack 与通过的 ReviewPack 已关闭 `TG-003` 旧 review 中“仅缺完整 live G3 证据”的唯一 blocker。`TG-003` 现可 `pass`，且目标状态可更新为 `Accepted`；本结论不包含任何 `G4` 放行含义。

## State Suggestion

- Target Status: Accepted
- Reason: `TG-003` 的目标 gate 是 `G3`，而最新 `VAL-002` 证据已在真实运行态中完成完整有序的 `G3` 链路，并验证只读/写权限 readiness 拆层对 agent 仍保持 operational truth。
