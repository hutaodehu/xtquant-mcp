# ReviewPack

Task ID: DG-002
Role: review
Date: 2026-03-30T08:05:00+08:00
Change Package Link: D:\xtquant-mcp\repo\docs\change_packages\DG-002.md
Evidence Pack Link: D:\xtquant-mcp\repo\docs\evidence_packs\DG-002-test-202603300750.md
Env Snapshot Link: D:\xtquant-mcp\repo\docs\env_snapshots\DG-002-test-202603300750.md
Supersedes: D:\xtquant-mcp\repo\docs\reviews\DG-002-review-202603300735.md

## Findings

1. 新增的 process-restart-residue 场景已经补到了上一轮 review 的核心语义缺口：它独立证明了持久化残留的 `status=running` 会在服务启动后被改写为 `lease_state=stopped`、`stop_reason=process_restart`、`needs_rebuild=true`、`rebuild_reason=process_restart`，且 `resolved_runtime_endpoint` 仍会落入 lease health 视图。就“重启残留是否被正确降级并提示重建”这一剩余风险而言，这轮场景覆盖是充分的。
2. 但这份正式 EvidencePack 仍不足以让 DG-002 通过 review，因为它没有闭合上一轮 review 明确要求保留的 MCP 层证据。`DG-002-test-202603300750.md` 明确写明本轮是 `HarmlessBackend` 直接实例化 `DataGatewayService`，并且“does not emit MCP trace_id or server_ts”；证据包中也没有 gateway `/healthz` 原始响应，亦没有 `xtdata://leases/active` 或等价 MCP tool/resource 调用的原始返回。
3. 因此，DG-002 当前的精确残余缺口已经不再是“缺少 restart-residue 语义验证”，而是“缺少可追溯到 gateway/MCP 消费面的正式证据体”。如果现在放行为 `pass`，等于把服务内实例化观察误当成 agent 可消费契约已经独立成立。

## Severity

- highest: medium

## Impact

本卡的剩余问题已经从状态机语义收敛到证据层级。继续维持 `blocked` 会把问题误写成环境或场景未覆盖；直接写 `pass` 则会绕过 `ACCEPTANCE_STANDARD.md` 对 `/healthz`、MCP 原始响应、`trace_id`、`server_ts` 的最小留证要求。

## Required Fix

1. 由测试角色补一轮 gateway/MCP 面的独立证据，至少保留本卡要求的 `/healthz` 原始响应。
2. 在同一轮证据中，通过 `xtdata://leases/active` 或等价 MCP 消费面保留原始响应，并包含 `trace_id`、`server_ts` 与 artifact 路径。
3. 新证据必须继续证明本轮已验证的 restart-residue 分类结果，即 `process_restart -> stopped + needs_rebuild=true`，避免回退成只剩摘要结论。

## Release Decision

- Decision: needs_fix
- Summary: restart-residue 场景本身已足够覆盖上一轮剩余语义风险，但正式 EvidencePack 仍缺 MCP 层最小留证，因此 DG-002 不能 `pass`，应按 `needs_fix` 回流补证。

## State Suggestion

- Target Status: In Independent Test
- Reason: 需要测试角色补正式证据体，不是开发返工，也不是环境阻断。
