# 审查结论

Task ID: TG-001
Role: review
Date: 2026-03-31T15:28:00+08:00
Change Package Link: D:\xtquant-mcp\repo\docs\change_packages\TG-001.md
Evidence Pack Link: D:\xtquant-mcp\repo\docs\evidence_packs\TG-001-test-202603311525.md

## Findings

1. `TG-001` 的核心设计偏差已经在实现范围内闭合。公共 `order.place` 的对外描述已从薄 broker 语义改为 governed write path，服务端入口仍经由统一 session manager 调度，但实际执行已被收口到 `TradeOpsService.place_order(...)` 所代表的受控服务端写路径；相关契约和实现在 [docs/MCP_DESIGN.md](../MCP_DESIGN.md)、[xtqmt_mcp/trade_gateway/server.py](../../xtqmt_mcp/trade_gateway/server.py) 和 [xtqmt_mcp/trade_gateway/capability_v2.py](../../xtqmt_mcp/trade_gateway/capability_v2.py) 之间保持一致。
2. 独立 EvidencePack 提供了足够的 bounded deterministic 证据来支持本卡收口，而不需要越权进入 live `G4`。其中既验证了 `order.place` 公共入口经过 `require_ready=True` 的 owner-session 边界，也验证了输出中存在 `governed_write_path`、`write_path=governed_service_order_place`、显式 gate sequence，以及 `order.status` / `order.cancel` 未脱离同一 ready-gated session boundary。
3. 现存剩余风险不再是 `TG-001` 的实现阻断，而是下游 live 验证边界。`docs/MCP_DESIGN.md` 与 [docs/task_cards/VAL-003.md](../task_cards/VAL-003.md) 仍明确：`G4` 真单证据链、broker readiness、live write-path release 属于 `VAL-003`，不能从本 ReviewPack 外推。

## Severity

- highest: medium

## Impact

在“实现范围共享变更收口”这个有界目标内，`TG-001` 已具备完整的 ChangePack、独立 EvidencePack 和一致的规范口径，可以结束本卡的 dev/test/review 流转，而不必继续把“尚未做 live `G4`”误记为当前实现卡的未完事项。这样可以把后续真正高风险的 live 写路径证明继续留在 `VAL-003`，避免任务边界混写。

## Required Fix

1. `TG-001` 当前有界实现范围内无阻断性 required fix。
2. 后续若有人尝试把本卡 review 结论解释成 live write path 已放行，必须回退该解释，并按 [docs/task_cards/VAL-003.md](../task_cards/VAL-003.md) 单独执行最小真单验证。
3. 若未来再次调整 `order.place`、`order.status`、`order.cancel` 的会话或审计边界，应以新变更重新进入 `dev -> test -> review`，不得复用本次 ReviewPack 直接覆盖新行为。

## Release Decision

- Decision: pass
- Summary: `TG-001` 可以在 bounded implementation scope 内通过 review 并收口到 `Accepted`。该结论只表示“唯一受控写路径的实现与契约收口已成立”，不表示 `G4` 已通过，不表示 live broker readiness 已成立，也不表示 `VAL-003` 已通过。
- Target Status: Accepted
- Recommended next board status: Accepted

## Residual Risks

1. 本次 `pass` 依赖的是 deterministic code/contract 证据，而非真实 broker 写入链路；因此它不能替代 `VAL-003` 的 live 真单验证。
2. `TradeOpsService.place_order(...)` 的真实 broker/runtime 行为仍可能在后续 `VAL-003` 中暴露环境或集成问题；这些问题应归属于 live validation lane，而不是 retroactively 否定本卡的 bounded implementation closure。
3. 如果后续文档或看板把 `TG-001 Accepted` 误写成“Trade Gateway live write path released”，会再次制造 agent 心智模型错误；主控同步时必须保留本 ReviewPack 的 scope boundary。
