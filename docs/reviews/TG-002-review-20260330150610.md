# ReviewPack

Task ID: TG-002
Role: review
Date: 2026-03-30T15:06:10+08:00
Change Package Link: [TG-002.md](../change_packages/TG-002.md)
Evidence Pack Link: [TG-002-test-202603300011.md](../evidence_packs/TG-002-test-202603300011.md)
Prior Review Link 1: [TG-002-review-202603300111.md](./TG-002-review-202603300111.md)
Supporting Evidence Link 1: [VAL-002-test-20260330-142745-live-gateway-rerun-orders-fallback.md](../evidence_packs/VAL-002-test-20260330-142745-live-gateway-rerun-orders-fallback.md)
Supporting Review Link 1: [VAL-002-review-20260330-143606-live-gateway-rerun-orders-fallback.md](./VAL-002-review-20260330-143606-live-gateway-rerun-orders-fallback.md)
Design Context Link: [MCP_DESIGN.md](../MCP_DESIGN.md)
Acceptance Standard Link: [ACCEPTANCE_STANDARD.md](../ACCEPTANCE_STANDARD.md)

## Findings

1. High: `TG-002` 之前唯一剩余阻断“缺少完整 live `G3` 证据”现已关闭，本卡可正式通过 review gate。
   - 旧 ReviewPack 明确说明阻断点不是新增实现缺陷，而是缺少真实 MiniQMT/xtquant 运行窗口下完整 `G3` 只读链路证据。
   - 新的 `VAL-002` EvidencePack 已在 fresh repo-loaded gateway 上按 [ACCEPTANCE_STANDARD.md](../ACCEPTANCE_STANDARD.md) 的 `G3` 顺序完成：
     - `miniqmt.ensure_logged_in`
     - `session.warm`
     - `session.status`
     - `probe.connection`
     - `account.show`
     - `positions.list`
     - `orders.list`
     - `snapshot.l1`
   - 这次 live 证据同时保留了与 `TG-002` 直接相关的单账户契约信号：
     - `/healthz` 返回 `account_contract=single_account_primary`
     - `/healthz` 返回 `account_input_mode=service_context_only`
     - `session.warm` 与 `trade://session/current` 都落在同一主账户上下文：`account_id=8883884325`、`owner_account_id=8883884325`
   - 结合 `TG-002` 自身独立测试里对 schema 隐藏 `account_id`、显式旧参数被 validation reject、resource/health 输出带单账户契约元数据的验证，可以确认 `TG-002` 的单账户主契约已经同时具备代码级和 live `G3` 级证据。

2. Medium: 本轮 closure 只覆盖 `TG-002` 的 `G3` 目标，不外推为 `G4` 或 broker 写路径已恢复。
   - 最新 `VAL-002` ReviewPack 已接受 `public orders.list` 的 truthful degraded fallback 作为 `G3 pass`。
   - 同一 payload 内仍显式保留 `broker_read.error=xttrader connect failed: -1 after 3 attempts (...)`。
   - 这说明 `TG-002` 现在可以按交易侧只读 gate 接受，但不能把本次通过误写成 broker fresh connect 已恢复，更不能据此启动 `VAL-003/G4`。

3. Medium: 当前没有新的 `fail_design` 被证明。
   - [MCP_DESIGN.md](../MCP_DESIGN.md) 对 `session/account` 的目标契约要求是单账户优先、服务端主账户上下文、schema 不混入每次调用显式账户切换。
   - 现有 `TG-002` 本地独立测试与最新 `VAL-002` live `G3` 证据共同表明，这些要求在当前只读链路上已经闭环。
   - `VAL-002` 中观察到的 session identifier split 属于 session owner / shadow reuse 观察项，不构成 `TG-002` 账户契约的 reopen blocker。

## Severity

- highest: high

## Impact

`TG-002` 现在具备两层互补证据：

1. 本地独立测试证明 schema、resource、validation reject 和 metadata 口径已经收口到 `single_account_primary` / `service_context_only`。
2. 最新 accepted `VAL-002` live `G3` 证据证明同一契约已在真实 gateway 运行态生效，并支撑完整交易侧只读链路。

因此，本卡已经满足当前定义的最高 gate `G3`，可以从先前的 `blocked` 收口为 `pass`，目标状态可进入 `Accepted`。

## Required Fix

1. `TG-002` 本卡范围内无新的 blocking fix。
2. 主控不得把本次 `TG-002` closeout 解释为 `VAL-003/G4` 可推进；higher gate 仍需独立 broker/write-path 证据。
3. 若外部看板或依赖链对 `TG-004` 仍保留旧的 blocked 记录，主控应单独同步该卡的 review closeout，而不要把其 stale 状态反向当作 `TG-002` 的新阻断。

## Release Decision

- Decision: pass
- Evidence Conclusion Carried Forward: `pass`
- Prior Blocker Closed By Latest `VAL-002` Live `G3` Evidence: `yes`
- `TG-002` May Move To `Accepted`: `yes`
- `G4` Or Write-Path Readiness Proven: `no`
- Summary: `TG-002` 的旧阻断只剩 live `G3` 证据缺口。最新 accepted `VAL-002` 已在 fresh repo-loaded gateway 上完成完整 `G3` 只读链路，并给出与本卡目标一致的单账户主契约运行态证据，因此本卡现在可以正式 `pass`。但该结论严格停留在 `G3`；broker subpath 的 `xttrader connect=-1` 仍被显式保留，不能据此推进 `VAL-003/G4`。

## State Suggestion

- Target Status: `Accepted`
- Current Role: `review`
- Reason: `TG-002` 的最高目标 gate 是 `G3`，且先前唯一未闭环项已被最新 accepted `VAL-002` live 证据关闭。
