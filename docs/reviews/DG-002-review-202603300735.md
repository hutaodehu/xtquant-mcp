# 审查结论

Task ID: DG-002
Role: review
Date: 2026-03-30T07:35:00+08:00
Change Package Link: D:\xtquant-mcp\repo\docs\change_packages\DG-002.md
Evidence Pack Link: D:\xtquant-mcp\repo\docs\evidence_packs\DG-002-test-202603300010.md
Env Snapshot Link: D:\xtquant-mcp\repo\docs\env_snapshots\DG-002-test-202603300010.md
Supersedes: D:\xtquant-mcp\repo\docs\reviews\DG-002-review-202603300110.md
Supporting Validation Evidence: D:\xtquant-mcp\repo\docs\evidence_packs\VAL-001-test-202603300307.md; D:\xtquant-mcp\repo\docs\reviews\VAL-001-review-202603300646.md

## Findings

1. `VAL-001` 已经补齐了 `DG-002` 旧 review 中“缺完整 live G2 只读窗口”的一部分关键缺口：同窗证据证明 `xtdata.status`、`xtdata.history.get_bars`、`xtdata.snapshot.batch` 在真实 prod scope 下成立，且 `xtdata://leases/active` 可读并与 `resolved_runtime_endpoint=127.0.0.1:58610` 对齐。旧 review 中“完全缺 live G2 运行窗证据”的表述已不再准确。
2. 但 `VAL-001` 没有把 `DG-002` 推进到可放行状态。它只证明了“当前窗口 lease 视图可读且无残留 lease 污染”，没有覆盖本卡真正仍待闭环的恢复语义：断连、端口恢复、进程重启残留后的 `lease_state`、`needs_rebuild`、`rebuild_reason`、`stop_reason` 是否仍然稳定且不误导 agent。
3. 现有 `DG-002` 独立测试与 `VAL-001` live 证据合并后，仍未形成一个能支持 `pass` 的最小证据包。按 `MCP_DESIGN.md`，subscription lease 仍是 experimental capability；按 `ACCEPTANCE_STANDARD.md`，若证据尚未证明运行态语义稳定成立，就不能把同窗可读误写成任务已放行。

## Severity

- highest: medium

## Impact

`DG-002` 当前真实状态已从“缺少 live G2 同窗观测”收窄为“恢复语义缺少独立证据”。如果继续沿用旧 review，不足以准确表达当前剩余风险；如果直接宣告 `pass`，又会把“当前窗口 lease 可读”误判为“lease recovery 模型已被验证”。

## Required Fix

1. 由测试角色补一轮针对 `DG-002` 本卡语义的独立证据，至少覆盖断连、恢复或进程重启残留之一，验证 `lease_state`、`needs_rebuild`、`rebuild_reason`、`stop_reason` 的分类稳定性。
2. 新 EvidencePack 必须继续保留 `/healthz`、相关 MCP 原始响应中的 `trace_id` / `server_ts` 以及 artifact 路径，避免把状态摘要当作运行态证据本体。
3. 在 recovery-scenario 证据补齐前，不得把 `DG-002` 写成 `pass` 或 `Accepted`。

## Release Decision

- Decision: blocked
- Summary: `VAL-001` 使旧 review 的阻断理由收窄，但没有消除 `DG-002` 的核心剩余风险；当前正式结论仍是 `blocked`。
- Target Status: `In Independent Test`
- Blocking Reason: ``
- Recommended next board status: `In Independent Test`

## Residual Risks

1. 若 agent 只看到当前窗口 `count=0`、`running_count=0`、`needs_rebuild_count=0`，仍可能误把“当前没有活跃 lease”理解成“恢复路径已经被验证”；本卡还需要 recovery-scenario evidence 去消除这个心智模型风险。
2. `needs_rebuild` 与相关 stop/rebuild 字段当前仍依赖实现侧启发式；没有断连或重启后的独立证据，就无法证明这些字段在异常窗口下不会产生误导。
