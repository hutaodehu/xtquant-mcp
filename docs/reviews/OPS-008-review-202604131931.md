# ReviewPack

Task ID: OPS-008
Role: review
Date: 2026-04-13T19:31:36+08:00
Change Package Link: [OPS-008.md](../change_packages/OPS-008.md)
Evidence Pack Link: [OPS-008-test-202604131857.md](../evidence_packs/OPS-008-test-202604131857.md)
Env Snapshot Link: [OPS-008-test-202604131857.md](../env_snapshots/OPS-008-test-202604131857.md)
Acceptance Standard Link: [ACCEPTANCE_STANDARD.md](../ACCEPTANCE_STANDARD.md)
Design Context Link: [MCP_DESIGN.md](../MCP_DESIGN.md)
Supporting Context Link 1: [EXECUTION_AND_ARTIFACT_STANDARD.md](../EXECUTION_AND_ARTIFACT_STANDARD.md)
Supporting Context Link 2: [VAL-003_G4_EXECUTION_PLAN.md](../VAL-003_G4_EXECUTION_PLAN.md)

## Findings

1. `OPS-008` 的 bounded implementation 目标已经闭合，足以支持本卡在当前 scope 内通过 review。
   - `xtqmt_mcp/trade_write_authority.py:262-320` 的正式入口只从 typed `authority_source_path` 与 `diag_probe_path` 构建 authority，不再消费 `CURRENT_STATUS.md`。
   - `docs/MCP_DESIGN.md:255-270` 已把 formal contract 固定为 `trade_write_authority_source_latest.json` + `diag_probe` 的 packet-bound typed carrier，且缺字段或 trace mismatch 时 `MUST fail closed`。
   - `tests/test_trade_write_authority.py:72-84` 明确锁定了“typed source 驱动且 formal lane 未闭合时返回 `formal_trade_write_lane_not_closed`”这一基础语义，证明本卡主目标“去 Markdown 拼装化”已经真正落到代码和测试。

2. fail-closed 与 latest-pointer 消费链已经有独立证据，不是只停留在 ChangePack 声明。
   - `tests/test_trade_write_authority.py:86-179` 覆盖了 `review_ref` 缺失、source 缺失、`packet_id` 缺失、`controller_judgment_content` 缺失、trace mismatch 以及 formal/runtime 同时转绿六类关键分支，说明 authority 不会因为链路缺口或错配被误拼成绿灯。
   - `scripts/run_trade_write_authority_check.ps1:40-56` 明确把 `<InstanceRoot>\\state\\trade_resources\\trade_write_authority_source_latest.json` 作为默认 source，并把输出写回同一实例目录下的 `trade_write_authority_latest.json`。
   - 现有 [OPS-008-test-202604131857.md](../evidence_packs/OPS-008-test-202604131857.md) 与配套 EnvSnapshot 已额外补上离线结构化 probe，证明 `run_trade_write_authority_check.ps1` 会真实消费临时 instance 下的 typed latest pointer 并输出 authority json，而不是只靠代码阅读推断。

3. 保留一个非阻断 residual risk：`run_controller_direct_test.ps1` 当前仍把 `review_ref` / `artifact_refs.review_pack` 写成空字符串，导致 fresh live packet 的 authority 顶层 blocker 可能先退化成 `authority_source_incomplete`。
   - `scripts/run_controller_direct_test.ps1:1730-1763` 当前生成的 typed carrier 固定写入 `review_ref = ""` 与 `artifact_refs.review_pack = ""`。
   - `xtqmt_mcp/trade_write_authority.py:18-26,156-166` 又把 `review_ref` 视为必填字段，因此 live writer 当前产出的 source 一旦直接进入 authority 生成，顶层分类会先落到 `authority_source_incomplete`，而不是更语义化的 `formal_trade_write_lane_not_closed`。
   - 这条风险不会把 authority 错拼成 formal green，反而保持 fail closed，因此不阻断 `OPS-008` 在“去 Markdown 拼装化 + typed carrier fail-closed”这个 bounded scope 内通过 review；但它确实意味着当前 live writer 还没有把“同 packet typed carrier + 后续独立 review 回链”闭成最终 operator 友好的 blocker 语义。

## Severity

- highest: medium

## Impact

当前代码、测试与独立证据已经足以证明：`trade_write_authority_latest.json` 的机器输入主路径已切换到 packet-bound typed carrier，且缺链路、错链路、trace mismatch 都会 fail closed，不再存在通过 Markdown / cache 拼装把 authority 假绿的路径。因此 `OPS-008` 可以在 bounded implementation scope 内结束 `review` 并进入 `Accepted`。

这一定义明确小于 formal closeout。它不代表 `VAL-003` 已解阻，不代表 `G4 pass`，也不代表 `trade_write_authority` 已经 formal green。`VAL-003` 的 blocked posture 仍应保持现状，任何写路径放行仍需要新的 fresh packet、独立 review 以及 state truth refresh。

## Required Fix

1. `OPS-008` 当前 bounded scope 内无阻断性 required fix。
2. 若后续需要让 fresh live packet 的 authority 在 review 之前呈现更稳定的 open-lane blocker 语义，而不是 `authority_source_incomplete`，应单独补一张后续卡处理 `review_ref` / `artifact_refs.review_pack` 的同 packet refresh 机制或更清晰的阶段化 carrier contract。
3. `xtqmt_mcp/trade_write_authority.py` 仍保留未消费的 `--current-status-path` 兼容参数；这不是本卡 blocker，但后续若继续保留，应在 operator 文档中避免把它误读成机器路径仍依赖 `CURRENT_STATUS.md`。

## Release Decision

- Decision: pass
- Scope: bounded implementation review only
- Typed carrier migration proven: `yes`
- Fail-closed behavior proven: `yes`
- `CURRENT_STATUS.md` no longer authority machine input: `yes`
- `VAL-003` unblocked: `no`
- `G4 pass` / formal green / closeout proven: `no`
- Recommended task status: `Accepted`
- Summary: `OPS-008` 可以在当前 bounded implementation scope 内通过 review，因为它已经把 authority 机器输入收口到同 packet typed carrier，并用独立测试证明缺链路和错链路只会 fail closed，不会再把 Markdown/cache 错配拼成绿灯。本结论不外推到 `VAL-003`、`G4` 或任何 formal release authority。

## Residual Risks

1. `run_controller_direct_test.ps1` 目前写出的空 `review_ref` 会让 live authority 顶层 blocker 偏向 `authority_source_incomplete`；这不产生假绿，但会削弱 open packet 阶段的 blocker 可读性。
2. 本轮 review 依据的是 deterministic tests、PowerShell 解析烟测和离线 instance-root probe，没有新增 fresh live packet，因此没有证明真实实例目录里已经形成可被后续 review 回填的 end-to-end refresh 闭环。
3. 只要后续有人把本 ReviewPack 解读成 `VAL-003` 可 reopen、`G4 ready`、formal green 或 closeout，该结论就是越权；必须回到当前 ReviewPack 的 bounded scope 表述。
