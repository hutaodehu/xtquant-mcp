# 审查结论

Task ID: OPS-001
Role: review
Date: 2026-03-30T01:53:00+08:00
Change Package Link: D:\xtquant-mcp\repo\docs\change_packages\OPS-001.md
Evidence Pack Link: D:\xtquant-mcp\repo\docs\evidence_packs\OPS-001-test-202603300146.md
Env Snapshot Link: D:\xtquant-mcp\repo\docs\env_snapshots\OPS-001-test-202603300146.md

## Findings

1. 上一轮审查要求的两项缺口已基本闭合：
   - 已补真实 gateway 进程下 `G0/G1` 运行态证据，`/healthz` 与 call log 可见 `evidence_scope/evidence_state_root/evidence_artifact_root`。
   - 已新增正式策略工件 `docs/OPS-001_ARTIFACT_DISPOSITION.md`，覆盖历史污染识别规则与处置流程。
2. 新增风险：本次隔离把 `D:\xtquant-mcp\instance\prod\artifacts\data_gateway\20260327\data_gateway_calls.jsonl` 整个文件迁移到 quarantine，但该文件包含混合内容（既有 `source=fake` 行，也有 `xtdata_port_not_ready` 非 fake 行）。这种“整文件按单一理由归类”为 `known_historical_fake_artifact` 的方式，会降低原始证据时间线的可解释性，并可能造成后续回放误判。
3. 当前阻断不属于环境问题：运行态端口、进程、gateway health 均可复核；问题属于证据处置策略在“混合日志”场景下的精度不足（设计/流程风险）。

## Severity

- highest: medium

## Impact

`OPS-001` 主目标（隔离 fake 状态并恢复 `G0/G1` 证据可信度）已显著推进，但如果不修正“混合日志整文件隔离”的规则，后续审计可能无法准确区分“污染条目”与“真实历史条目”，会损伤证据完整性与可追溯性。

## Required Fix

1. 在 `docs/OPS-001_ARTIFACT_DISPOSITION.md` 补充“混合日志处置规则”：
   - 禁止默认整文件按单一污染理由隔离。
   - 对 JSONL 类日志优先做条目级筛分，或先做原文件哈希与全量备份再分流。
2. 在隔离 manifest 增加最小完整性字段（至少 `original_sha256`、`total_lines`、`flagged_lines`、`contains_non_flagged_records`），避免把混合日志误标成纯污染文件。
3. 依据修订后的规则，补一轮最小复核 EvidencePack（可沿用 OPS-001 卡内范围），证明“污染隔离”与“历史证据保全”可同时成立。

## Release Decision

- Decision: needs_fix
- Summary: 上一轮 required fix 已基本关闭，但本轮发现新的设计/流程风险（混合日志整文件隔离导致证据完整性受损）。建议状态回退到 `In Dev` 完成策略修订后，再回到 `In Independent Test` 做最小复核；当前不建议标记为任务层 `Accepted`。

## Classification

- Design status: residual risk exists (evidence-disposition precision)
- Environment status: no blocking issue observed in this run

禁止自动进入下一卡。
