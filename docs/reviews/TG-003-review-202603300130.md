# 审查结论

Task ID: TG-003
Role: review
Date: 2026-03-30T01:31:00+08:00
Change Package Link: D:\xtquant-mcp\repo\docs\change_packages\TG-003.md
Evidence Pack Link: D:\xtquant-mcp\repo\docs\evidence_packs\TG-003-test-202603300120.md
Env Snapshot Link: D:\xtquant-mcp\repo\docs\env_snapshots\TG-003-test-202603300120.md

## Findings

1. `probe.connection` 的不可用错误文案仍写有 “explicit account_id”，与当前主契约不一致：
   - 当前 schema 与网关文案已是单账户主上下文（无显式 `account_id` 输入）。
   - 但 `xtqmt_mcp/trade_ops.py` 在 `probe_connection_unavailable` 分支仍返回 `"probe.connection requires server-side qmt_userdata and explicit account_id"`，会误导排查与调用方心智。
2. `G3` 证据链仍未闭环：
   - 当前 EvidencePack 结论为 `partial`，仅覆盖代码级测试和定向脚本；
   - 尚未覆盖真实 MiniQMT/xtquant 的完整 live 只读链路，因此不能宣称 `G3 pass`。

## Severity

- highest: medium

## Impact

`TG-003` 的核心拆层实现方向基本正确，但错误文案与契约不一致会继续污染 agent/调用方认知；且在缺少 live `G3` 证据前，任务不能被视为可放行。

## Required Fix

1. 修正 `xtqmt_mcp/trade_ops.py` 中 `probe_connection_unavailable` 文案，去掉“explicit account_id”表述，统一为 server-side primary account context 语义。
2. 为该分支增加/更新针对性测试，防止后续再次出现描述-实现漂移。
3. 在真实 MiniQMT/xtquant 环境补齐 `G3` 只读链路证据，并产出新的独立测试 EvidencePack 后再复审。

## Release Decision

- Decision: needs_fix
- Summary: 当前存在可修复的契约文案不一致，且 live `G3` 证据未闭环；建议回退 `In Dev` 完成修复后，再回到 `In Independent Test` 补齐 live 证据。
