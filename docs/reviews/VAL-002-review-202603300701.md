# 审查结论

Task ID: VAL-002
Role: review
Date: 2026-03-30T07:01:00+08:00
Change Package Link: D:\xtquant-mcp\repo\docs\change_packages\VAL-002.md
Evidence Pack Link: D:\xtquant-mcp\repo\docs\evidence_packs\VAL-002-test-202603300306.md
Env Snapshot Link: D:\xtquant-mcp\repo\docs\env_snapshots\VAL-002-test-202603300306.md
Supersedes: D:\xtquant-mcp\repo\docs\reviews\VAL-002-review-202603300430.md

## Findings

1. 现有 EvidencePack 明确记录 `miniqmt.ensure_logged_in` 返回 `miniqmt_not_logged_in/login_window_not_found`，`session.warm` 报 `xttrader connect failed: -1`，随后 `probe.connection`、`account.show`、`positions.list`、`orders.list`、`snapshot.l1` 全部停在 `session_not_ready`。这说明本次 live `G3` 只读 smoke 在环境层硬停止，没有形成可放行的只读链路证据。
2. `fail_env` 是本次测试证据的结论词，不是 review pack 的 release decision 词汇。若审查继续写成 `Decision: fail_env`，controller harness 会把本卡误判成 `review_needed`，从而偏离仓库约定的 review gate 语义。
3. 当前任务仍然阻断 `VAL-003`。在 `miniqmt.ensure_logged_in -> session.warm -> session.status` 不能建立 owner-managed session 之前，`G4` 写路径验证不能启动。

## Severity

- highest: high

## Impact

当前 `VAL-002` 的真实状态是环境阻断而非设计放行。`TG-004`、`TG-002`、`TG-003` 仍缺少成功的 live `G3` 只读闭环证据，`VAL-003` 也继续保持不可启动。

## Required Fix

1. 修复本机 MiniQMT 登录窗口不可消费和 `xttrader connect=-1` 的环境问题。
2. 在环境恢复后由测试角色重新执行 `VAL-002` 的 live `G3` 只读 smoke，产出新的 EvidencePack 和必要的 EnvSnapshot。
3. 待 rerun 证据成立后，再进入新的 review gate；在此之前不得把当前任务或其依赖卡宣称为 `pass`。

## Release Decision

- Decision: blocked
- Summary: 当前正式结论是环境阻断。保留测试 EvidencePack 的 `fail_env` 分类，但 review gate 的 release decision 归一为 `blocked`，以表达任务仍停留在阻断状态且未放行。
- Target Status: `Blocked`
- Blocking Reason: `env_blocked`
- Recommended next board status: `Blocked`

## Residual Risks

1. 如果环境恢复后仍出现 UI 焦点丢失、session owner 抖动或 `xttrader connect=-1` 间歇复现，`VAL-002` 可能继续多轮失败；后续 rerun 必须继续保留完整 trace 和 artifact 路径。
2. 若 controller 或 board 仅看见旧 review pack 文案，可能把 `fail_env` 误当成 review decision 而不是测试结论；因此本 superseding ReviewPack 必须作为新的审查入口使用。
