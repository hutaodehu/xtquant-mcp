# 审查结论

Task ID: DG-002
Role: review
Date: 2026-03-30T08:15:00+08:00
Change Package Link: D:\xtquant-mcp\repo\docs\change_packages\DG-002.md
Evidence Pack Link: D:\xtquant-mcp\repo\docs\evidence_packs\DG-002-test-202603300808.md
Env Snapshot Link: D:\xtquant-mcp\repo\docs\env_snapshots\DG-002-test-202603300808.md
Supersedes: D:\xtquant-mcp\repo\docs\reviews\DG-002-review-202603300805.md

## Findings

1. 上一轮 review 的唯一剩余 required fix 是“formal MCP/gateway 证据不足”，不是 restart-residue 语义本身未验证。`DG-002-test-202603300808.md` 现在已经补齐同一场景下的 `health_payload()` 原始输出、`tools/call` `xtdata.status` 原始 envelope、`resources/read` `xtdata://leases/active` 原始返回，以及 post-start 持久化文件改写结果。
2. 这组新增证据在 MCP 消费面上完整保留了上一轮要求的最小留证链：`xtdata.status` 原始响应中存在 `trace_id=dg002-trace-202603300808`、`server_ts=2026-03-30T08:13:11` 与审计 artifact 路径；`xtdata://leases/active` 原始响应与 post-start 文件改写结果共同证明 restart residue 会被降级为 `lease_state=stopped`、`stop_reason=process_restart`、`needs_rebuild=true`、`rebuild_reason=process_restart`，且 lease 视图继续关联当前 `resolved_runtime_endpoint`。
3. 因此，`DG-002` 现在已经同时满足本卡的设计目标和上一轮 review 明确要求的形式证据门槛。剩余的“live reconnect 尚未证明”仍属于本卡 `Scope Out` / `Not Implemented`，不是当前 review gate 的残余缺口，不能继续作为拒绝放行的理由。

## Severity

- highest: medium

## Impact

`DG-002` 的 review 阻断点已经从“证据层级不足”收敛为“范围内无残余阻断项”。当前 formal evidence 已足以支撑 agent 消费 `xtdata://leases/active` 时不再把重启残留误判为活跃订阅，并且能够在 MCP 层追溯对应 endpoint、trace 与审计 artifact。

## Required Fix

1. `DG-002` 当前范围内无新增 blocking fix。
2. 后续若要宣告 subscription lease 的 live reconnect / auto-rebuild 已被真实环境证明，必须以新任务卡或新一轮独立变更重新进入 `dev -> test -> review`，不得复用本次放行结论。

## Release Decision

- Decision: pass
- Summary: 新增的 MCP-layer 证据已经闭合上一轮 review 要求的 formal evidence 缺口，因此 `DG-002` 可以正式 `pass` 并进入 `Accepted`。
- Target Status: `Accepted`
- Recommended next board status: `Accepted`
