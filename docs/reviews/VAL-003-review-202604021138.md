# ReviewPack

Task ID: VAL-003
Role: review
Date: 2026-04-02T11:38:18+08:00
Change Package Link: [VAL-003.md](../change_packages/VAL-003.md)
Evidence Pack Link: [VAL-003-test-202604021132-round1-preflight.md](../evidence_packs/VAL-003-test-202604021132-round1-preflight.md)
Env Snapshot Link: [VAL-003-202604021132-round1-preflight.md](../env_snapshots/VAL-003-202604021132-round1-preflight.md)
Supporting Evidence Link 1: [VAL-003.md](../task_cards/VAL-003.md)
Supporting Evidence Link 2: [ACCEPTANCE_STANDARD.md](../ACCEPTANCE_STANDARD.md)
Supporting Evidence Link 3: [OPERATIONS_RUNBOOK.md](../OPERATIONS_RUNBOOK.md)
Supporting Evidence Link 4: [VAL-003_G4_EXECUTION_PLAN.md](../VAL-003_G4_EXECUTION_PLAN.md)
Supporting Evidence Link 5: [VAL-003-review-202604021049.md](./VAL-003-review-202604021049.md) (previous review baseline)

## Findings

1. High: the fresh rerun still cannot move beyond Round 1 because the observed window is no longer a live trading window and the runtime readiness chain is still not closed.
   - The new [EvidencePack](../evidence_packs/VAL-003-test-202604021132-round1-preflight.md) places the rerun at `2026-04-02T11:30:43+08:00` through `2026-04-02T11:32:17+08:00`.
   - The paired [EnvSnapshot](../env_snapshots/VAL-003-202604021132-round1-preflight.md) explicitly records `market_window_closed=true` for that observed wall-clock window under the repo's `VAL-003/G4` CN A-share timing rules.
   - The same rerun also records `probe.connection -> session_not_ready`, so the live read-only readiness shape still does not support escalation toward Round 2.
   - Review impact: even with healthier endpoints, this rerun remains a no-go Round 1 snapshot only.

2. High: governance posture still independently blocks progression.
   - [VAL-003 task card](../task_cards/VAL-003.md) still records `Status: Blocked`, `Blocking Reason: broker_blocked`, `Risk Class: high`, and `Automation Policy: manual_gate`.
   - The controller judgment used for this rerun explicitly restricted scope to fresh Round 1 evidence capture only and did not authorize Round 2, Round 3, or any write-path action.
   - [VAL-003_G4_EXECUTION_PLAN.md](../VAL-003_G4_EXECUTION_PLAN.md) requires a stop when formal posture still forbids continuation.
   - Review impact: the card remains `blocked` even if host-side transport health improved.

3. Medium: the prior runtime-versus-doc contract drift identified in the 10:40 retry is resolved in this rerun and should no longer be treated as an active blocker.
   - The fresh [EvidencePack](../evidence_packs/VAL-003-test-202604021132-round1-preflight.md) shows `http://127.0.0.1:8766/healthz` returning repo-backed `xtqmtDataGateway` health with HTTP `200`.
   - The same evidence set shows `resources/read diag://probe/latest` succeeding instead of returning JSON-RPC `-32601 unknown method`.
   - The fresh trace `68143b3c-d581-48f2-9614-d91e345892e1` is back-linked in `D:\xtquant-mcp\instance\prod\artifacts\trade_gateway\20260402\trade_gateway_calls.jsonl`.
   - Review impact: the old endpoint-contract drift is closed for this rerun; future controller judgment should not keep citing the old `8766/healthz` or `resources/read` mismatch as the current stop.

4. Medium: `diag://probe/latest` currently returns only a minimal contract-scoped payload, so this rerun still does not provide a richer live readiness snapshot beyond the failing tool call.
   - The fresh [EvidencePack](../evidence_packs/VAL-003-test-202604021132-round1-preflight.md) records the resource payload as `{\"account_contract\":\"single_account_primary\",\"account_input_mode\":\"service_context_only\",\"account_scope\":\"service_context\"}`.
   - This is materially different from a fuller live readiness payload, but the evidence also classifies it as an observation rather than the primary blocker.
   - Review impact: not a separate release blocker for this rerun, but worth keeping explicit so later runs do not over-read the resource output.

## Separation Of Findings

- Environment:
  - `market_window_closed=true` for the observed `11:30-11:32 +08` window.
  - `probe.connection` returned `session_not_ready`.
- Governance and control-plane:
  - `VAL-003` remains formally `Blocked / broker_blocked`.
  - This rerun stayed confined to Round 1 only.
- Design and contract:
  - The prior `8766/healthz` and `resources/read diag://probe/latest` drift from the 10:40 retry is resolved.
  - `diag://probe/latest` now works but returns only a minimal contract-scoped payload in this window.

## Impact

The fresh `202604021132` rerun is materially better than the `202604021040` retry on endpoint-contract truth: both repo-backed `/healthz` endpoints are live, `resources/read diag://probe/latest` works again, and same-day audit back-linking is restored. That improvement does not change the release posture for `VAL-003`, because the observed window was already closed for live trading, runtime readiness still reports `session_not_ready`, and formal posture remains `Blocked / broker_blocked`. The correct review result therefore remains `blocked`, but for a cleaner blocker set than the previous review.

## Required Fix

1. Keep `VAL-003` at `Blocked / broker_blocked`; do not authorize Round 2 or Round 3 from this rerun.
2. Wait for a fresh live Beijing trading window before attempting another Round 1 governed preflight.
3. Re-run Round 1 only when the live window is open and capture whether `probe.connection` still returns `session_not_ready`.
4. Treat the earlier `8766/healthz` and `resources/read` mismatch as resolved for current runtime; the next controller handoff should focus on window timing and runtime readiness, not on the old drift.

## Release Decision

- Decision: blocked
- Current Retry Scope: Round 1 only
- Prior Contract Drift Resolved: yes
- `market_window_closed` In This Window: yes
- Runtime Readiness Closed: no
- Write-path Release: no
- Summary: The fresh `202604021132` rerun fixes the earlier endpoint-contract drift but still lands outside the live trading window and still reports `probe.connection -> session_not_ready`. Combined with the standing `Blocked / broker_blocked` manual-gate posture, that leaves the correct review result at `blocked` and does not authorize movement beyond Round 1.

## State Suggestion

- Target Status: Blocked
- Recommended Review Result: blocked
- Recommended Next State: Blocked
- Reason: Return control to the controller for closeout of this Round 1 packet and wait for a fresh live-window judgment before any further delegated execution.

## Residual Risks

1. A later operator could keep citing the old `8766/healthz` or `resources/read` drift even though this rerun shows those checks are now restored.
2. If the next rerun again starts after `11:30 +08` or outside another live window, the task will keep generating blocked packets without advancing readiness truth.
3. There is still no new governed-write evidence for `order.place`, `order.status`, `orders.list` in write-followup context, `order.cancel`, or `fills.list`.
