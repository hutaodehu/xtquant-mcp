# ReviewPack

Task ID: TG-004
Role: review
Date: 2026-03-30T15:08:00.6654473+08:00
Change Package Link: [TG-004.md](../change_packages/TG-004.md)
Primary Task Evidence Link: [TG-004-test-202603292358.md](../evidence_packs/TG-004-test-202603292358.md)
Primary Task Env Snapshot Link: [TG-004-test-202603292358.md](../env_snapshots/TG-004-test-202603292358.md)
Superseded Review Link: [TG-004-review-202603300000.md](./TG-004-review-202603300000.md)
Accepted Upstream Evidence Link: [VAL-002-test-20260330-142745-live-gateway-rerun-orders-fallback.md](../evidence_packs/VAL-002-test-20260330-142745-live-gateway-rerun-orders-fallback.md)
Accepted Upstream Review Link: [VAL-002-review-20260330-143606-live-gateway-rerun-orders-fallback.md](./VAL-002-review-20260330-143606-live-gateway-rerun-orders-fallback.md)
Acceptance Standard Link: [ACCEPTANCE_STANDARD.md](../ACCEPTANCE_STANDARD.md)
Design Spec Link: [MCP_DESIGN.md](../MCP_DESIGN.md)

## Findings

1. High: `TG-004` 的上一轮唯一 formal blocker 已被关闭，当前可以按本卡目标 gate `G3` 放行。
   - 被 supersede 的旧审查明确写明阻断点仅是“缺少真实环境下的完整 `G3` 只读链路独立证据”，而不是新增实现缺陷或未关闭的 `fail_design`。
   - 最新已接受的 `VAL-002` live rerun 现在按 [ACCEPTANCE_STANDARD.md](../ACCEPTANCE_STANDARD.md) 定义的 `G3` 顺序完整跑通：`miniqmt.ensure_logged_in -> session.warm -> session.status -> probe.connection -> account.show -> positions.list -> orders.list -> snapshot.l1`。
   - 该 rerun 直接验证到了 `TG-004` 相关 contract，而不是旁路验证：
     - `session.warm` / `session.status` 维持 owner-managed session 语义。
     - `probe.connection` 以 `owner_managed_session_reuse` 成功返回，证明只读探测已经在 owner session 语义下闭合。
     - public `orders.list` 不再是 formal failure，而是 `ok=true` 的 explicit degraded fallback，且同一 payload 内保留 `broker_read.error=xttrader connect failed: -1 ...`。
   - 这意味着 `TG-004` 在 `G3` 范围内要求的“同一 owner 模型下完成预热、状态查询和只读探测”已经有真实 live evidence 支撑，不再停留在窄自测或文案一致性层面。

2. High: 这次通过只覆盖 `TG-004` 的 `G3` / read-only contract closure，不外推到 `G4` 或真实写路径 readiness。
   - [ACCEPTANCE_STANDARD.md](../ACCEPTANCE_STANDARD.md) 将 `xttrader connect=-1` 列为更高 gate 的硬停止条件。
   - 最新 `VAL-002` 证据虽然让 public `orders.list` 以 truthful degraded success 通过 `G3`，但 broker subpath failure 仍被显式保留在 payload 中。
   - 因此本次 review 的正确结论是：
     - `TG-004`：`pass`
     - `VAL-003` / `G4`：仍不得据此启动
   - 不能把 public read contract 的 closure 误写成 broker fresh connect 已恢复，更不能误写成 `order.place` 前置 gate 已闭合。

3. Medium: `MCP_DESIGN.md` 中仍存在 session/account 统一性与只读/写权限彻底拆离的后续差距，但这些差距没有被本轮 live evidence 证明为当前 `TG-004` 的 blocker。
   - 设计文档仍把“`session/account` 契约未完全统一”和“写权限尚未彻底从只读 readiness 中拆离”列为 Current Gaps。
   - 最新 live rerun 证明的是：在当前实现形态下，owner-managed session 与 public `orders.list` explicit fallback contract 已足以 truthful 地通过 `G3`。
   - 所以这些 gap 仍应保留为后续任务债务，而不是继续作为 `TG-004` 的 release blocker。

4. Low: session identifier split 仍是 residual observation，但不构成当前放行阻断。
   - 最新 evidence 里，`session.status` / `trade://session/current` 仍报告 `session_id=100`，而 `probe.connection` / `diag://probe/latest` / `orders.list.shadow_fallback` 观察到的 reused shadow session 为 `111`。
   - 这次 ordered chain 没有被该 split 打断，public contract 也没有因此失真。
   - 该 observation 需要在后续 contract 收口中继续跟踪，但不足以推翻当前 `G3 pass`。

## Severity

- highest: high

## Impact

`TG-004` 的旧阻断是证据缺口，不是新的设计失败。现在这条证据缺口已经由最新接受的 `VAL-002` live `G3` rerun 补齐，而且补齐的是本卡最关键的真实运行态 contract：

1. owner-managed session 已在真实链路中闭合。
2. `probe.connection` 的 owner-session reuse 已在 live runtime 生效。
3. public `orders.list` 已从 formal failure 变成 truthful degraded success，并保留 broker failure 作为 machine-readable metadata。

因此，`TG-004` 现在具备进入 `Accepted` 的依据。与此同时，这个结论仍然严格停留在 `G3` 边界内，不改变 higher gate 仍受 `xttrader connect=-1` 阻断的事实。

## Required Fix

1. 对 `TG-004` 当前 release decision：无新的 blocking fix。
2. 对控制面后续推进：继续将 `VAL-003` 保持为 `blocked`，直到 higher-gate broker/write path 前置条件得到独立 evidence closure。
3. 对后续设计收口：将 `session/account` 统一性与只读/写权限进一步拆离留给后续任务，不回流为当前 `TG-004` 的 blocker。

## Release Decision

- Decision: `pass`
- Scope Of Pass: `G3` / read-only contract closure only
- Prior Blocker Closed: `yes`
- Formal `fail_design` Proven Open: `no`
- May Move To Accepted: `yes`
- May Start `VAL-003`: `no`
- Summary: 旧审查中 `TG-004` 唯一未闭合的问题是“缺少完整 live G3 evidence”。最新已接受的 `VAL-002` rerun 现在在 fresh repo-loaded gateway 上完整跑通 `G3` 顺序链，并直接验证了 owner-managed session、`probe.connection` owner-session reuse，以及 public `orders.list` truthful degraded fallback contract。该 blocker 已关闭，因此 `TG-004` 可以正式 `pass` 并进入 `Accepted`。但 payload 内仍保留 `xttrader connect=-1` 的 broker subpath failure，这个事实继续阻断 higher gate，不得据此推进 `VAL-003` / `G4`。

## State Suggestion

- Target Status: `Accepted`
- Current Role: `review`
- Blocking Reason: clear for `TG-004`; higher-gate broker blocker remains downstream only
