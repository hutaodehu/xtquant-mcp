# ReviewPack

Task ID: VAL-003
Role: review
Date: 2026-04-02T10:49:00+08:00
Change Package Link: [VAL-003.md](../change_packages/VAL-003.md)
Evidence Pack Link: [VAL-003-test-202604021040-round1-preflight.md](../evidence_packs/VAL-003-test-202604021040-round1-preflight.md)
Env Snapshot Link: [VAL-003-202604021040-round1-preflight.md](../env_snapshots/VAL-003-202604021040-round1-preflight.md)
Supporting Evidence Link 1: [VAL-003.md](../task_cards/VAL-003.md)
Supporting Evidence Link 2: [ACCEPTANCE_STANDARD.md](../ACCEPTANCE_STANDARD.md)
Supporting Evidence Link 3: [OPERATIONS_RUNBOOK.md](../OPERATIONS_RUNBOOK.md)
Supporting Evidence Link 4: [VAL-003_G4_EXECUTION_PLAN.md](../VAL-003_G4_EXECUTION_PLAN.md)
Supporting Evidence Link 5: [VAL-003-review-202603312314.md](./VAL-003-review-202603312314.md) (baseline only, not current result)

## Findings

1. High: this retry stays formally blocked by governance and control-plane posture even though the host-side preflight is materially better than the 2026-03-31 late-night baseline.
   - [VAL-003 task card](../task_cards/VAL-003.md) still records `Status: Blocked`, `Blocking Reason: broker_blocked`, `Risk Class: high`, and `Automation Policy: manual_gate`.
   - [VAL-003_G4_EXECUTION_PLAN.md](../VAL-003_G4_EXECUTION_PLAN.md) keeps Round 1 as a read-only preflight and requires a stop when formal posture still forbids continuation.
   - The submitted [EvidencePack](../evidence_packs/VAL-003-test-202604021040-round1-preflight.md) explicitly records that controller judgment still restricted this retry to fresh Round 1 only and that the task was still not auto-dispatchable.
   - Review impact: the new evidence does not authorize Round 2 or Round 3, and it does not justify moving `VAL-003` out of `Blocked / broker_blocked`.

2. High: `market_window_closed` does not apply in this new evidence window, but Round 1 still does not pass because the environment-side gateway readiness chain is incomplete.
   - The new [EvidencePack](../evidence_packs/VAL-003-test-202604021040-round1-preflight.md) and [EnvSnapshot](../env_snapshots/VAL-003-202604021040-round1-preflight.md) both place the observed window at `2026-04-02 10:44-10:46 +08:00` and explicitly state that `market_window_closed=false`.
   - This means the blocker shape changed relative to the 2026-03-31 baseline review: market hours are no longer the active stop in this retry.
   - Even so, [ACCEPTANCE_STANDARD.md](../ACCEPTANCE_STANDARD.md) and [VAL-003_G4_EXECUTION_PLAN.md](../VAL-003_G4_EXECUTION_PLAN.md) require trade and data gateway `/healthz` to be available during Round 1.
   - The fresh [EvidencePack](../evidence_packs/VAL-003-test-202604021040-round1-preflight.md) shows `127.0.0.1:8766` reachable by TCP, but `http://127.0.0.1:8766/healthz` returned `404 Not Found`, so this retry still cannot claim Round 1 pass.

3. Medium: the required Round 1 MCP resource-read contract is not satisfied on the observed live endpoint, which is a design and contract issue that must stay separate from the environment blockers.
   - [VAL-003_G4_EXECUTION_PLAN.md](../VAL-003_G4_EXECUTION_PLAN.md) requires `resources/read diag://probe/latest` in Round 1.
   - [OPERATIONS_RUNBOOK.md](../OPERATIONS_RUNBOOK.md) also requires resource read-back as part of the formal evidence chain.
   - The new [EvidencePack](../evidence_packs/VAL-003-test-202604021040-round1-preflight.md) and [EnvSnapshot](../env_snapshots/VAL-003-202604021040-round1-preflight.md) show that `resources/read diag://probe/latest` returned JSON-RPC `-32601 unknown method: resources/read`.
   - Review impact: this retry surfaces a current runtime-versus-documented-contract gap, but because the task is already stopped by formal posture and incomplete Round 1 readiness, the overall release decision remains `blocked` rather than being reclassified wholesale to `fail_design`.

4. Medium: advertised audit back-links are still incomplete for the current-day trace, so the posture cannot be upgraded toward governed write readiness.
   - The fresh [EvidencePack](../evidence_packs/VAL-003-test-202604021040-round1-preflight.md) records a successful `probe.connection` response with trace id `b05bcd80-cefa-43a9-9225-593dcbd33e53`.
   - The same evidence set records that no current-day `trade_gateway_calls.jsonl` or advertised `output/mcp_gateway/20260402/gateway_calls.jsonl` file was found at the documented locations.
   - [ACCEPTANCE_STANDARD.md](../ACCEPTANCE_STANDARD.md) requires audit artifact paths, `trace_id`, and `server_ts` as formal evidence, and [VAL-003_G4_EXECUTION_PLAN.md](../VAL-003_G4_EXECUTION_PLAN.md) requires back-linkable calls/log/state artifacts before any `G4` release can be considered.
   - Review impact: this is not a basis to attempt `order.place`; it is additional evidence that the current run remains a no-go packet only.

## Separation Of Findings

- Environment:
  - The observed window is live, processes are present, and `58610/8765/8766` are reachable, but the documented data gateway `/healthz` endpoint returned `404 Not Found`.
  - Current-day audit back-link files for the advertised trace were not found at the documented locations.

- Governance and control-plane:
  - `VAL-003` remains formally `Blocked / broker_blocked`.
  - This retry was still bounded to Round 1 only and requires controller reconcile before any further step.

- Design and contract:
  - The documented Round 1 call `resources/read diag://probe/latest` is not available on the observed live endpoint.
  - This is a contract gap, but it is not the only active stop in the current window.

## Market Window Assessment

`market_window_closed` does not apply to this review window. The formal evidence places the retry at `2026-04-02 10:44:09+08:00` through `2026-04-02 10:46:14+08:00`, and both the [EvidencePack](../evidence_packs/VAL-003-test-202604021040-round1-preflight.md) and [EnvSnapshot](../env_snapshots/VAL-003-202604021040-round1-preflight.md) explicitly mark that window as inside the CN A-share morning session. Compared with the 2026-03-31 baseline review, this is a real posture improvement in timing, but it does not change the overall task status because other hard stops still remain active.

## Impact

The new 2026-04-02 Round 1 retry improves the observable host posture and removes `market_window_closed` from the active blocker set for this specific window. It does not, however, clear `broker_blocked`, complete the documented Round 1 readiness chain, or supply the audit and contract evidence required to move toward governed write. The net posture therefore stays `blocked`, with the active blocker shape now split across governance/control-plane, environment readiness, and design/contract completeness.

## Required Fix

1. Keep `VAL-003` at `Blocked / broker_blocked` until a new formal artifact set proves that controller posture, Round 1 readiness, and higher-gate broker prerequisites are all independently closed.
2. Resolve the documented data gateway health expectation for Round 1 so that the required `http://127.0.0.1:8766/healthz` check returns an acceptable live response or the formal contract is corrected.
3. Resolve the runtime-versus-doc contract gap for `resources/read diag://probe/latest` before any future review treats Round 1 as fully passing.
4. Restore a current-day audit back-link path for the observed trace so the repo can prove advertised runtime artifacts actually exist for the same window.

## Release Decision

- Decision: blocked
- Current Retry Scope: Round 1 only
- `market_window_closed` In This Window: no
- New Evidence Changes Overall Posture: no
- Posture Delta Versus 2026-03-31 Baseline: market-window blocker cleared, but task remains blocked by governance/control-plane plus environment and contract gaps
- Write-path Release: no
- Summary: The 2026-04-02 Round 1 retry was executed during a live Beijing trading window and therefore is not blocked by `market_window_closed`. Even so, the task remains formally `Blocked / broker_blocked`, the documented data gateway `/healthz` check did not pass, the required `resources/read diag://probe/latest` contract did not hold on the observed endpoint, and current-day audit back-links were not found. The correct review result for this retry remains `blocked`.

## State Suggestion

- Target Status: Blocked
- Recommended Review Result: blocked
- Recommended Next State: Blocked
- Reason: The blocker shape changed, but the safety posture did not. This retry should return to controller judgment as a fresh no-go packet for the live 2026-04-02 morning window, not as permission to continue into Round 2 or Round 3.

## Residual Risks

1. A later operator could incorrectly treat the improved `probe.connection` output as equivalent to write-path authorization unless the current review keeps the governance and contract stops explicit.
2. Because this retry remained Round 1 only, there is still no new broker-side evidence for `order.place`, `order.status`, `orders.list` in governed-write context, `order.cancel`, or `fills.list`.
3. If the `resources/read` contract drift is not resolved, future runs may continue to mix environment readiness with endpoint-contract mismatch and make the blocker harder to diagnose cleanly.
