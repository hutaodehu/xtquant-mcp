# ReviewPack

Task ID: TG-008
Role: review
Date: 2026-04-13T19:30:56+08:00
Change Package Link: [TG-008.md](../change_packages/TG-008.md)
Evidence Pack Link: [TG-008-test-202604131913.md](../evidence_packs/TG-008-test-202604131913.md)
Env Snapshot Link:
Acceptance Standard Link: [ACCEPTANCE_STANDARD.md](../ACCEPTANCE_STANDARD.md)
Execution Standard Link: [EXECUTION_AND_ARTIFACT_STANDARD.md](../EXECUTION_AND_ARTIFACT_STANDARD.md)
Workflow Link: [WORKFLOW_AND_BOARD.md](../WORKFLOW_AND_BOARD.md)
Design Context Link: [MCP_DESIGN.md](../MCP_DESIGN.md)

## Findings

1. 未发现阻断当前 `TG-008` bounded implementation scope 通过 review 的缺口；代码、ChangePack、EvidencePack 与当前 repo-local 复核结果彼此一致。
   - [TG-008.md](../change_packages/TG-008.md) 把范围限定为 trade resource cache 语义拆分与防污染，不涉及 formal authority carrier 改造，也不扩大工具能力面。
   - [resources.py](../../xtqmt_mcp/trade_gateway/resources.py) 已把 `trade://orders/today`、`trade://fills/today`、`diag://order_*` 作为稳定资源集合暴露，并在 cache miss 场景统一补齐 `resource_authority`、`resource_trace_id`、`resource_server_ts` 等资源元数据。
   - [server.py](../../xtqmt_mcp/trade_gateway/server.py) 的 `_cache_resource_from_tool` 已把 `orders.list`、`fills.list`、`order.place`、`order.cancel`、`order.status` 分流到各自专用 URI，没有再把异构工具结果回写到 `trade://orders/today`。

2. 已知的 cache-miss metadata 缺口在当前代码与测试中均已闭合，没有残留“分离缓存通过但 miss payload 丢字段”的 review blocker。
   - [TG-008-test-202604131913.md](../evidence_packs/TG-008-test-202604131913.md) 已记录 red -> green 过程：最初 `resource_not_cached` 缺失 `resource_trace_id` / `resource_server_ts`，随后补丁修复并完成独立测试。
   - 当前代码路径中，`read_trade_resource()` 的 missing payload 通过 `resource_truth_metadata(... authority=\"unavailable\")` 回填资源 truth 字段；本轮复核再次确认 miss payload 含空字符串形态的 `resource_trace_id` 与 `resource_server_ts`。
   - 对应测试 [test_trade_gateway_server.py](../../tests/test_trade_gateway_server.py) 中的 `test_read_trade_resource_cache_miss_includes_resource_metadata` 当前复跑通过，证明该缺口未回归。

3. 现有证据足以支撑 `TG-008` 在 bounded implementation scope 内通过 review，并把任务卡推进到 `Accepted`；但该结论只覆盖 repo-local 资源契约收口，不外推到 live gateway 或更高 gate。
   - 当前复核复跑了 `test_order_resources_are_cached_to_distinct_uris` 与 `test_read_trade_resource_cache_miss_includes_resource_metadata`，以及整份 `tests.test_trade_gateway_server`，结果均为 `OK`。
   - 这些测试直接覆盖了本卡 `Scope In`：`trade://orders/today` 仅由 `orders.list` 写入、新增 `trade://fills/today` 与 `diag://order_*` 专用资源、所有资源带齐 freshness 与 authority 元数据。
   - 在本仓规范下，review 是否可放行取决于 task scope、ChangePack、EvidencePack 与独立复核是否闭合；对 `TG-008` 这张 repo-local refactor 卡，当前证据链已经足够，不需要额外补出 live EnvSnapshot 才能完成 bounded implementation review。

4. 本次 review `pass` 不代表 `VAL-003` 解阻，不代表 formal green，不代表任何 `G4` 写路径通过或 closeout authority 已建立。
   - `TG-008` 的任务边界只处理 trade resource cache 语义拆分与 metadata 完整性，不处理 governed write truth。
   - [ACCEPTANCE_STANDARD.md](../ACCEPTANCE_STANDARD.md) 明确要求 formal green / `G4` 写路径 authority 必须依赖独立写路径证据与后续 review；本卡没有也不应替代这条证据链。
   - 因此本 ReviewPack 的 `pass` 只能解释为：`TG-008` 在 bounded implementation review scope 内完成收口，可进入 `Accepted`；不能把它扩写成 `VAL-003 pass`、`G4 ready` 或正式解阻。

## Severity

- highest: low

## Impact

`TG-008` 当前已经把 trade 资源缓存从“最后一次任意交易工具结果”收口为按语义分流的稳定资源集合，并补齐了 cache-miss 场景的统一资源 metadata。基于现有 ChangePack、独立 EvidencePack 与本轮 repo-local 复核，当前卡片可以在 bounded implementation review scope 内结束 `dev -> test -> review` 流转并进入 `Accepted`。

## Required Fix

1. `TG-008` 当前 bounded implementation scope 内无阻断性 required fix。
2. 若后续有人把本卡 review `pass` 误写成 `VAL-003` 解阻、formal green 或 `G4` 写路径 ready，必须按本 ReviewPack 的边界口径纠正。
3. 若后续资源 URI、resource truth metadata 字段或 cache 分流策略再次变动，必须重新进入新的 dev/test/review 工件闭环，不能直接复用本 ReviewPack 覆盖新行为。

## Release Decision

- Decision: pass
- Bounded Scope Only: yes
- Cache Separation Proven: yes
- Cache-Miss Metadata Gap Closed: yes
- Live Gateway Readback Proven: no
- `VAL-003` Unblocked: no
- Formal Green / `G4` Ready Proven: no
- Summary: `TG-008` 可以在 bounded implementation review scope 内通过 review 并推进到 `Accepted`。该 `pass` 仅表示 repo-local 资源分流与 cache-miss metadata 修复已经由 ChangePack、EvidencePack 和当前代码/测试闭合；它不代表 `VAL-003` 解阻，不代表 formal green，也不代表任何 `G4` 写路径通过。
- Target Status: Accepted
- Recommended next board status: Accepted

## Residual Risks

1. 本次 review 只证明 repo-local 资源契约与 deterministic 测试闭合，不等于 live gateway 进程上的读回行为已额外验证。
2. 后续 consumers 若仍假定所有交易结果都可从 `trade://orders/today` 读取，可能需要在独立卡片中继续做文档或调用方 reconcile；这不构成当前卡的阻断。
3. `TG-008` 的 review `pass` 不应被上游流程误升格为 write authority 或 release authority；若发生这种叙事漂移，会重新引入 formal truth 混写风险。
