# ReviewPack

Task ID: TG-009
Role: review
Date: 2026-04-13T19:32:12+08:00
Change Package Link: [TG-009.md](../change_packages/TG-009.md)
Evidence Pack Link: [TG-009-test-202604131913.md](../evidence_packs/TG-009-test-202604131913.md)
Env Snapshot Link:
Acceptance Standard Link: [ACCEPTANCE_STANDARD.md](../ACCEPTANCE_STANDARD.md)
Execution Standard Link: [EXECUTION_AND_ARTIFACT_STANDARD.md](../EXECUTION_AND_ARTIFACT_STANDARD.md)
Workflow Link: [WORKFLOW_AND_BOARD.md](../WORKFLOW_AND_BOARD.md)
VAL-003 Plan Link: [VAL-003_G4_EXECUTION_PLAN.md](../VAL-003_G4_EXECUTION_PLAN.md)

## Findings

1. 无阻断性 findings：`TG-009` 的核心 bounded implementation 已闭合，`order.cancel` 现在与 `order.place` 共享同级 write gate，且 `live/no-shadow` 不再被隐式视为 `flow_smoke`。

   当前实现中，[xtqmt_mcp/trade_ops.py](../../xtqmt_mcp/trade_ops.py) 的 `_run_connect_gate()` 与 `_write_authority_snapshot()` 只在显式 `execution_mode=flow_smoke` 时走放宽分支；`order_cancel()` 先执行同一 `connect_gate`，失败时以 `local_gate_intercepted=true`、`submission_scope=local_gate`、`submission_stage=connect_gate` 本地拦截，不再存在未声明的撤单旁路。独立测试 [TG-009-test-202604131913.md](../evidence_packs/TG-009-test-202604131913.md) 已命中并记录 `live/no-shadow` 旧旁路失败现象与修复后通过结果；本轮 review 复核中，`test_live_no_shadow_connect_gate_uses_probe_connection_instead_of_flow_smoke_bypass`、`test_live_no_shadow_write_authority_snapshot_uses_probe_payload`、`test_order_cancel_respects_same_write_authority_gate_as_order_place`、`test_order_cancel_green_path_marks_broker_cancel_scope` 均再次通过。

2. 无阻断性 findings：网关层 contract 与可观测面已对齐到本卡 scope，足以支撑 bounded implementation review 收口。

   [xtqmt_mcp/trade_gateway/server.py](../../xtqmt_mcp/trade_gateway/server.py) 已把 `order.cancel` 同时纳入 `/healthz` 的 `readiness_layers.write_permission.write_tools` 和 `GatewaySessionManager.execute(... require_ready=True)` 的正式写工具路径。测试 [tests/test_trade_gateway_server.py](../../tests/test_trade_gateway_server.py) 中，`test_initialize_login_resource_and_prompt` 覆盖 `/healthz` 写工具列表，`test_order_cancel_routes_through_gateway_session_dispatch` 覆盖 `tools/call order.cancel` 的网关层派发与 machine-readable 字段保留；本轮 review 复核时两项均再次通过。结合现有 EvidencePack 中两模块整套 `22` 个测试 `OK` 的记录，可以确认当前 ChangePack 与代码实现一致，没有遗漏本卡 scope in 的第三项。

3. Medium：本次可通过的是 bounded implementation review，不是 `VAL-003` 解阻，不是 `G4 pass`，也不是 formal green。

   [TG-009-test-202604131913.md](../evidence_packs/TG-009-test-202604131913.md) 把结论写成 `partial`，原因是测试角色没有越权宣布 `Accepted`，且未做 live broker cancel/write-path 验证；这一点与 [ACCEPTANCE_STANDARD.md](../ACCEPTANCE_STANDARD.md) 和 [VAL-003_G4_EXECUTION_PLAN.md](../VAL-003_G4_EXECUTION_PLAN.md) 的边界一致，不构成 `TG-009` 在 bounded implementation scope 内的阻断。当前 review 只能确认“撤单同级强门禁的实现与 deterministic contract 已闭合”，不能把该结论外推为 `VAL-003` 已解阻、`G4` 已通过或 trade write lane 已正式放行。

## Severity

- highest: medium

## Impact

`TG-009` 的目标是把 `order.cancel` 纳入与 `order.place` 同级的强门禁和 machine-readable 写路径 contract，并消除 `live/no-shadow` 的隐式旁路。现有 ChangePack、独立 EvidencePack、当前代码实现以及本轮 review 复核测试，已经足够证明这三个 scoped 目标都已闭合。因此，本卡可以在 bounded implementation scope 内完成 `dev -> test -> review` 流转并进入 `Accepted`。同时，这个 `Accepted` 只表示实现卡本身收口，不改变 `VAL-003` 的 blocked posture，也不替代任何 `G4` live 证据链。

## Required Fix

1. `TG-009` 当前 bounded implementation scope 内无新的阻断性 required fix。
2. 若后续有人把本 ReviewPack 解读成 `VAL-003` 解阻、`G4 pass`、formal green 或 live trade release，必须回退该解释，并继续以 [VAL-003_G4_EXECUTION_PLAN.md](../VAL-003_G4_EXECUTION_PLAN.md) 和下游正式工件为准。
3. 若后续再次修改 `order.cancel`、`order.place`、`probe.connection` 或网关写工具派发边界，必须重新进入新的 `dev -> test -> review` 闭环，不得复用本次 ReviewPack 覆盖新行为。

## Release Decision

- Decision: pass
- Scope Of Pass: bounded implementation review only
- May Move To Accepted: yes
- VAL-003 Unblocked: no
- G4 Pass Proven: no
- Formal Green Proven: no
- Summary: 当前 `TG-009` 的 ChangePack、独立 EvidencePack、代码实现和复核测试已经足以证明：`order.cancel` 已纳入与 `order.place` 同级的 write gate，`live/no-shadow` 不再隐式旁路为 `flow_smoke`，网关层 `tools/call order.cancel` 也保持 `require_ready=True` 与 machine-readable 提交范围字段。因此本卡可以在 bounded implementation scope 内 `pass` 并进入 `Accepted`。该结论不代表 `VAL-003` 解阻，不代表 `G4` 通过，也不代表 formal green。

## State Suggestion

- Target Status: `Accepted`
- Recommended next board status: `Accepted`
- Reason: `TG-009` 的 scoped goal 是实现与 deterministic contract 收口，而不是 live `G4` 验收。现有正式工件已经足以支撑本卡收口；higher-gate live blocker 继续留在 `VAL-003`，不应回写到本卡状态。

## Residual Risks

1. 当前证据仍以 repo-local deterministic 测试为主，不等价于 live broker fresh connect、真实撤单成功或完整 `G4` 写路径证据链。
2. 本次 review 依赖 `trade_ops.py` 与 gateway server 的当前 contract；如果后续在未补新工件的情况下继续调整写门禁或 session dispatch，旧 ReviewPack 不能自动覆盖新实现。
3. 最大剩余风险不是代码缺口，而是 scope 被误读；主控或后续同步若把 `TG-009 Accepted` 外推成 higher-gate green，会重新制造 formal truth 漂移。
