# ReviewPack

Task ID: TG-006
Role: review
Date: 2026-04-13T19:30:04+08:00
Change Package Link: [TG-006.md](../change_packages/TG-006.md)
Evidence Pack Link: [TG-006-test-202604131856.md](../evidence_packs/TG-006-test-202604131856.md)

## Findings

1. 未发现会阻断 `TG-006` 在 bounded implementation scope 内收口的 scoped finding。当前实现仍满足任务卡声明的核心目标：`order.place` 的写前 gate 直接复用 `probe.connection` 同源 truth，不再维持第二套独立 gate 推导；`write_authority_snapshot` 也继续从同一 `probe.connection` payload 归一生成。对应代码位于 [xtqmt_mcp/trade_ops.py](../../xtqmt_mcp/trade_ops.py) 第 586-704 行，关键独立断言位于 [tests/test_trade_order_submission_contract.py](../../tests/test_trade_order_submission_contract.py) 第 268-365 行。
2. 现有 EvidencePack 虽然把正式结论保守写为 `partial`，但其未升级为 `pass` 的原因是明确保留 `G4` / live / formal authority 边界，而不是因为 `TG-006` 实现本身仍存在未闭合设计缺口。结合本次 review 在当前代码快照上重新执行 `tests.test_trade_order_submission_contract` 与 `tests.test_trade_gateway_server`，可以支持把本卡作为 bounded implementation review 收口到 `Accepted`。
3. 当前代码快照已包含后续共享写门禁补丁带来的 `live/no-shadow` 防回退测试，[tests/test_trade_order_submission_contract.py](../../tests/test_trade_order_submission_contract.py) 第 470-569 行验证 live/no-shadow 路径不再隐式退化成 `flow_smoke`。这组后续补丁没有削弱 `TG-006` 的“写前 authority 单一真相”结论，反而说明当前 `_run_connect_gate()` / `_write_authority_snapshot()` 在无 shadow 的 live 场景下也继续锚定 `probe.connection`。但这仍不构成 `VAL-003` 解阻、`G4 pass`、formal green 或 closeout 依据。

## Severity

- highest: low

## Impact

`TG-006` 现在具备完整的 bounded implementation 工件链：任务卡、ChangePack、独立 EvidencePack，以及当前 review 重新执行的 repo-local deterministic 验证。按仓库规则，这已经足以支持本卡结束 `dev -> test -> review` 流转并进入 `Accepted`，同时把 live runtime authority、formal truth refresh 和 `VAL-003` reopen plan 继续留在各自的正式工件中处理，避免再次把“实现收口”与“live 放行”混成同一结论。

## Required Fix

1. `TG-006` 当前 bounded implementation scope 内无阻断性 required fix。
2. 若后续再次调整 `probe.connection`、`order.place`、`order.cancel` 之间的写前 authority carrier 或 `write_authority_snapshot` 字段，应以新变更重新进入 `dev -> test -> review`，不得复用本 ReviewPack 覆盖新行为。
3. 若任何看板或口头总结把本 ReviewPack 解读为 `VAL-003` 已 unblock、`G4` 已 pass、formal authority 已转绿，必须回退该表述并以 [docs/VAL-003_G4_EXECUTION_PLAN.md](../VAL-003_G4_EXECUTION_PLAN.md) 为准。

## Verification

本次 review 只做 repo-local 只读复核与确定性测试重跑，不改 formal truth，不做 live 执行：

1. `nl -ba xtqmt_mcp/trade_ops.py | sed -n '586,704p'`
2. `nl -ba tests/test_trade_order_submission_contract.py | sed -n '268,569p'`
3. `nl -ba tests/test_trade_gateway_server.py | sed -n '679,740p'`
4. `python -m unittest tests.test_trade_order_submission_contract -v`
5. `python -m unittest tests.test_trade_gateway_server -v`
6. `date '+%Y%m%d%H%M %Y-%m-%dT%H:%M:%S%z'`

验证结果：

1. `tests.test_trade_order_submission_contract` 当前快照共 9 项，全部通过；其中包含 `TG-006` 原有 contract 用例，以及后续 `live/no-shadow` 两条防回退用例。
2. `tests.test_trade_gateway_server` 当前快照共 13 项，全部通过；`order.place` 仍通过 governed server write path 暴露 `connect_gate_failed` 与既有 envelope。
3. 本次 review 使用的时间戳为 `202604131930 / 2026-04-13T19:30:04+0800`。

## Release Decision

- Decision: pass
- Summary: `TG-006` 可以在 bounded implementation scope 内通过 review，并把任务卡推进到 `Accepted`。该结论只表示“写前 authority 单一真相收口”这张实现卡已经闭合，不表示 `VAL-003` 已解阻，不表示 `G4` 已通过，不表示 formal green，不表示 live broker 写路径已放行。
- Target Status: Accepted
- Recommended next board status: Accepted

## Residual Risks

1. 当前 `pass` 依赖的是 repo-local deterministic contract 证据，不是 live broker / gateway runtime 证据；因此它不能替代 `VAL-003` 的正式 reopen 与 `G4` 验收。
2. 当前代码快照已包含 `TG-006` 之后的共享写门禁补丁；虽然该补丁不构成 TG-006 的 blocker，但后续做审计归因时应避免把新增 `live/no-shadow` 防回退测试误记成原始 EvidencePack 已覆盖范围。
3. formal authority refresh、`trade_write_authority_latest.json`、`CURRENT_STATUS.md` 与其他 formal carrier 的绿色收口仍不在本 ReviewPack 覆盖范围内。
