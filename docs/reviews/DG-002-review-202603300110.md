# 审查结论

Task ID: DG-002
Role: review
Date: 2026-03-30T01:10:00+08:00
Change Package Link: D:\xtquant-mcp\repo\docs\change_packages\DG-002.md
Evidence Pack Link: D:\xtquant-mcp\repo\docs\evidence_packs\DG-002-test-202603300010.md
Env Snapshot Link: D:\xtquant-mcp\repo\docs\env_snapshots\DG-002-test-202603300010.md

## Findings

1. 本卡实现与契约方向基本一致：`xtdata://leases/active` 已暴露 experimental lease 健康结构，并覆盖 `lease_state`、`needs_rebuild`、`resolved_runtime_endpoint` 等核心字段；当前证据内未看到明确 `fail_design` 信号。
2. 当前仍是 gate 证据未闭环：现有 EvidencePack 以代码级独立测试和定向脚本为主，未包含真实 MiniQMT/xtdata 运行窗下的完整 `G2` live 只读证据链，不能据此宣称 `G2 pass`。
3. 风险仍需实证：`needs_rebuild` 与 `lease_state` 依赖启发式判定，尚未在断连恢复、长时间无事件、进程重启残留等真实场景下形成稳定证据。

## Severity

- highest: medium

## Impact

`DG-002` 的代码与文档已经显著降低了“把订阅句柄残留误判为健康租约”的风险，但在缺少 live `G2` 证据前，任务不应被标记为 `pass` 或 `Accepted`。若越级放行，会把“代码级验证通过”误写成“运行态验收通过”。

## Required Fix

1. 补一轮真实环境 `G2` 证据：至少包含 `xtdata.status`、`xtdata.history.get_bars`、`xtdata.snapshot.batch` 以及 `xtdata://leases/active` 的同窗观测结果。
2. 在真实连接波动场景补证据：验证 `lease_state` / `needs_rebuild` / `stop_reason` 在断连、恢复、重启后的分类稳定性。
3. 新增独立测试 EvidencePack 时补齐运行态最小证据要素（`/healthz`、MCP 原始响应中的 `trace_id` / `server_ts`、artifact 路径）。

## Release Decision

- Decision: blocked
- Summary: 当前阻断点是 `G2` live 证据未闭环，不是已确认的新增实现缺陷；建议保持 `In Independent Test`，补齐 live 证据后再复审。
