# EnvSnapshot

Task ID: OPS-008
Role: test
Date: 2026-04-13T18:57:20+08:00
Host: `CHIYU`
Shell: `PowerShell 7.6.0`
Repo Root: `D:\xtquant-mcp\repo`
Instance Root: `D:\xtquant-mcp\instance\prod`
Scope: `OPS-008` 的 PowerShell 解析、WSL -> Windows 路径事实，以及临时 instance root 离线 authority probe 快照；不执行 live gateway/test

## Purpose

本快照只记录与 `OPS-008` 独立测试直接相关的最小环境事实，用于回答三件事：

1. WSL 当前工作目录映射到的 Windows repo / instance 路径是否与脚本默认值一致。
2. PowerShell 7 下两条脚本入口是否能被正常解析，避免把路径或 shell 问题误判成实现问题。
3. 在不触发 live packet 的前提下，`run_trade_write_authority_check.ps1` 是否能在临时 `InstanceRoot` 上真实消费 typed latest pointer 并落 authority 输出。

本快照明确不做：

1. 不执行 `run_controller_direct_test.ps1`。
2. 不执行 `run_trade_write_authority_check.ps1` 的 live authority 生成。
3. 不读取或修改 `CURRENT_STATUS.md`、`VAL-003` formal truth 或任何 review artifact。

## Commands

1. `hostname`
2. `pwsh -NoLogo -NoProfile -Command '$env:COMPUTERNAME'`
3. `pwsh -NoLogo -NoProfile -Command '$PSVersionTable.PSVersion.ToString(); Resolve-Path "D:\xtquant-mcp\repo"; Resolve-Path "D:\xtquant-mcp\instance\prod"; Resolve-Path "D:\xtquant-mcp\repo\scripts\run_controller_direct_test.ps1"; Resolve-Path "D:\xtquant-mcp\repo\scripts\run_trade_write_authority_check.ps1"'`
4. `pwsh -NoLogo -NoProfile -Command "[void][scriptblock]::Create((Get-Content -LiteralPath 'D:\\xtquant-mcp\\repo\\scripts\\run_controller_direct_test.ps1' -Raw)); 'run_controller_direct_test.ps1 OK'"`
5. `pwsh -NoLogo -NoProfile -Command "[void][scriptblock]::Create((Get-Content -LiteralPath 'D:\\xtquant-mcp\\repo\\scripts\\run_trade_write_authority_check.ps1' -Raw)); 'run_trade_write_authority_check.ps1 OK'"`
6. 离线 authority probe：
   在 `%TEMP%\ops008-authority-probe2-b022a5dbbd114e248e8c8a0f48f5919c` 下创建临时 `InstanceRoot`，
   复制 `diag_probe_latest.json`，
   写入 controller-compatible `trade_write_authority_source_latest.json`，
   然后执行
   `pwsh -NoLogo -NoProfile -File D:\xtquant-mcp\repo\scripts\run_trade_write_authority_check.ps1 -RepoRoot D:\xtquant-mcp\repo -InstanceRoot C:\Users\Yun\AppData\Local\Temp\ops008-authority-probe2-b022a5dbbd114e248e8c8a0f48f5919c`

## Host And Path Truth

`hostname` 与 `$env:COMPUTERNAME` 都返回：

```text
CHIYU
```

PowerShell 版本与路径解析返回：

```text
7.6.0

Path
----
D:\xtquant-mcp\repo
D:\xtquant-mcp\instance\prod
D:\xtquant-mcp\repo\scripts\run_controller_direct_test.ps1
D:\xtquant-mcp\repo\scripts\run_trade_write_authority_check.ps1
```

## Parse Smoke Truth

脚本解析结果：

```text
run_controller_direct_test.ps1 OK
run_trade_write_authority_check.ps1 OK
```

## Offline Authority Probe Truth

临时 probe 使用的 Windows 路径：

```text
C:\Users\Yun\AppData\Local\Temp\ops008-authority-probe2-b022a5dbbd114e248e8c8a0f48f5919c
```

关键 artifact 与时间：

```text
trade_write_authority_source_latest.json
mtime_utc=2026-04-13T11:13:19.395038+00:00
size=2759

trade_write_authority_latest.json
mtime_utc=2026-04-13T11:13:19.657616+00:00
size=4427

diag_probe_latest.json
mtime_utc=2026-04-13T07:03:03.376476+00:00
size=9596
trace_id=04003498-621d-4e0f-a7dd-5608b93fe389
```

authority 输出摘要：

```json
{
  "status": "fail",
  "blocking_reason": "formal_trade_write_lane_not_closed",
  "current_truth_source": "typed_authority_source",
  "current_truth_ref": "C:\\Users\\Yun\\AppData\\Local\\Temp\\ops008-authority-probe2-b022a5dbbd114e248e8c8a0f48f5919c\\state\\trade_resources\\trade_write_authority_source_latest.json",
  "trace_id": "04003498-621d-4e0f-a7dd-5608b93fe389",
  "resource_trace_id": "04003498-621d-4e0f-a7dd-5608b93fe389",
  "controller_direct_executed_test": true,
  "controller_direct_summary": "offline authority probe only",
  "warnings": []
}
```

这说明：

1. `run_trade_write_authority_check.ps1` 在该 probe 中真实消费的是临时 `InstanceRoot` 下的 latest pointer，而不是 prod instance 或 `CURRENT_STATUS.md`。
2. authority 输出与 source 文件都落在同一个临时 `state\trade_resources` 目录下，落位行为与 `scripts/run_trade_write_authority_check.ps1:43-55` 一致。
3. 本 probe 使用的是 controller-compatible typed source 字段骨架，只是把 `review_ref` 填成了临时 placeholder，目的是把“latest pointer 真消费”与“当前 live writer 的 review 缺口”拆开观察。

## Notes

1. 这份快照只证明当前独立测试使用的 shell 与 Windows 路径事实成立，不证明 live gateway 已运行，也不证明实例目录下已有 fresh typed authority artifact。
2. `OPS-008` 的实现与测试都以 `D:\xtquant-mcp\repo` / `D:\xtquant-mcp\instance\prod` 为默认路径，本快照确认该默认路径在当前宿主可解析。
3. 离线 authority probe 只在系统临时目录下写入 probe artifact，没有改动 `D:\xtquant-mcp\instance\prod`、`CURRENT_STATUS.md`、`VAL-003` formal truth 或任何 repo 内代码/测试文件。
4. 本快照没有触发 `order.place`、`order.cancel`、`fills.list` 或 controller-direct live packet；`controller_direct_executed_test=true` 仅来自离线 judgment 占位文本，不代表 live test 已执行。
