# ChangePack

Task ID: OPS-006
Role: dev
Date: 2026-03-31T10:34:48.0574723+08:00
Branch/Commit: N/A (`D:\xtquant-mcp\repo` 当前不是 git worktree)
Stage: implemented-dev-selftest

## Goal

收口通用交付技能套件的核心 schema、authority matrix 和场景级 acceptance contract，使后续 adapter 与实现工作可以引用稳定上游 contract，而不是继续沿用草案口径。

## Implemented

1. 将 [ADAPTIVE_DELIVERY_SUITE_DESIGN.md](../universal_skills/ADAPTIVE_DELIVERY_SUITE_DESIGN.md) 从“设计草案”收口为更明确的 Phase 1 core contract baseline：
   - 明确 Phase 1 只收口 core contract、authority vocabulary 与 examples 对齐
   - 明确 repo adapter、运行时 skill 行为、CI/eval runner、plugin packaging 仍属于后续阶段
   - 新增 docs-only 与 safety-boundary docs 的明确分界，避免把高风险文档任务误降到 `fast_loop`
2. 更新 [ADAPTIVE_DELIVERY_SUITE_MANUAL.md](../universal_skills/ADAPTIVE_DELIVERY_SUITE_MANUAL.md)：
   - 把 `mode-fast-loop` 的 docs-only 前提收窄为“纯说明性、无 safety / approval / release / permission boundary 变化”
   - 把 runbook / template / policy / permission boundary 文档变化显式纳入 `mode-gated-change`
   - 新增 “docs-only 不是天然 `fast_loop`” 的使用规则
3. 收口 contract 规则：
   - 在 [ROUTE_DECISION_SCHEMA.md](../universal_skills/contracts/ROUTE_DECISION_SCHEMA.md) 增加 `gated_change` / `live_rollout` 的 authority 与 handoff 非空约束，以及 docs safety-boundary 任务不得停留在 `fast_loop`
   - 在 [ACCEPTANCE_CONTRACT_SCHEMA.md](../universal_skills/contracts/ACCEPTANCE_CONTRACT_SCHEMA.md) 增加 docs/policy 高风险边界任务与 agent/tool routing 任务的 scenario-level acceptance 约束
   - 在 [AUTHORITY_MATRIX.md](../universal_skills/contracts/AUTHORITY_MATRIX.md) 明确高风险文档任务继承 `gated_change` 级别 authority separation
4. 补齐 examples 与 schema 的对齐：
   - 在 [examples/README.md](../universal_skills/examples/README.md) 增加 `safety_boundary_docs_gated_change.yaml`
   - 新增 [safety_boundary_docs_gated_change.yaml](../universal_skills/examples/safety_boundary_docs_gated_change.yaml)，与 `docs_only_fast_loop.yaml` 形成对照
   - 给两个 worked examples 补上 `gate_ladder`、`hard_stop_conditions`、`scope_limited_success_rules`、`runtime_truth_rules`、`gate_statuses`、`degraded_but_truthful`，消除 schema 与样例之间的最小结构漂移

## Not Implemented

1. 不包含 `.agents/skills/*` 的运行逻辑、prompt、tool 选择或权限实现改动。
2. 不包含 repo-specific adapter 映射；`OPS-007` 仍负责当前仓库工件链映射。
3. 不包含 plugin packaging、分发策略切换、CI/eval runner 或 release readiness 宣告。

## Files Changed

- `docs/universal_skills/ADAPTIVE_DELIVERY_SUITE_DESIGN.md`
- `docs/universal_skills/ADAPTIVE_DELIVERY_SUITE_MANUAL.md`
- `docs/universal_skills/contracts/ROUTE_DECISION_SCHEMA.md`
- `docs/universal_skills/contracts/ACCEPTANCE_CONTRACT_SCHEMA.md`
- `docs/universal_skills/contracts/AUTHORITY_MATRIX.md`
- `docs/universal_skills/examples/docs_only_fast_loop.yaml`
- `docs/universal_skills/examples/safety_boundary_docs_gated_change.yaml`
- `docs/universal_skills/examples/deterministic_quant_factor_fast_loop_worked_example.yaml`
- `docs/universal_skills/examples/stale_runtime_evidence_live_rollout_worked_example.yaml`
- `docs/universal_skills/examples/README.md`
- `docs/task_cards/OPS-006.md`
- `docs/change_packages/OPS-006.md`

## Self-check

1. `rg -n "Phase 1：core contract baseline|docs-only 边界|实现顺序建议|plugin" docs\\universal_skills\\ADAPTIVE_DELIVERY_SUITE_DESIGN.md`
   - 结果：设计文档已明确 Phase 1 / Phase 2 边界、docs safety-boundary 规则和 contract-first 的推进顺序。
2. `rg -n "docs-only 不是天然|runbook、template、policy|mode-gated-change" docs\\universal_skills\\ADAPTIVE_DELIVERY_SUITE_MANUAL.md`
   - 结果：手册已明确普通 docs-only 与高风险 policy/runbook 文档的分界。
3. `rg -n "gated_change|manual_gate|docs-only|human_review_required|scope_limited_success_rules|gate_ladder" docs\\universal_skills\\contracts\\ROUTE_DECISION_SCHEMA.md docs\\universal_skills\\contracts\\ACCEPTANCE_CONTRACT_SCHEMA.md docs\\universal_skills\\contracts\\AUTHORITY_MATRIX.md docs\\universal_skills\\examples\\README.md docs\\universal_skills\\examples\\safety_boundary_docs_gated_change.yaml docs\\universal_skills\\examples\\deterministic_quant_factor_fast_loop_worked_example.yaml docs\\universal_skills\\examples\\stale_runtime_evidence_live_rollout_worked_example.yaml`
   - 结果：schema 约束、authority 规则与 examples 已能覆盖 docs safety-boundary 对照，以及 worked examples 的最小结构字段。
4. `Test-Path docs\\universal_skills\\examples\\safety_boundary_docs_gated_change.yaml`
   - 结果：`True`

## Known Risks

1. 本次仍未修改 `.agents/skills/*` 的真实行为，因此当前“contract 已稳定”不等于“运行时实现已经证明完全遵循 contract”。
2. `OPS-007` 尚未完成 repo adapter 映射；当前仓库使用者仍需同时理解通用 contract 与本仓库专用 artifact chain。
3. 目前新增的是 docs safety-boundary route fixture，而不是完整 worked example；若后续要验证文档高风险场景的完整 `acceptance_contract` / `gate_result`，仍建议再补一份 worked example。

## Needs Independent Test

1. review 需要确认本次没有发明新的 `base_mode`、overlay、authority role 或 blocker 枚举，只是在现有 schema 上收口约束。
2. review 需要确认 docs safety-boundary 任务被正确升格到 `gated_change`，且未误伤普通低风险 docs-only `fast_loop` 场景。
3. review 需要确认 worked examples 新增字段与当前 schema 一致，不再出现 schema 最小结构比样例更严、导致使用者误读的情况。

## Recommended Next Board Status

- 建议状态：`In Independent Test`
- 理由：`OPS-006` 的 dev 范围 contract 文档与 examples 已完成收口并通过本地一致性检视，下一步应由独立 review 检查是否存在 schema 漂移、枚举越权或 docs-only boundary 误判。
