# ChangePack

Task ID: TG-009
Role: dev
Date: 2026-04-13
Branch/Commit: /tmp/xtquant-mcp-repo (staging copy)
Stage: implemented-dev-selftest

## Goal

让 `order.cancel` 与 `order.place` 共享同级写前门禁和写路径 contract，不再保留隐式写旁路。

## Implemented

1. `order.cancel` 在进入 broker adapter 前先执行与 `order.place` 同源的 write gate。
2. `order.cancel` 返回 `broker_submission_attempted`、`local_gate_intercepted`、`submission_scope`、`submission_stage`。
3. `/healthz` 的 `write_tools` 把 `order.cancel` 纳入正式写工具。

## Not Implemented

1. 不引入新的紧急撤单旁路。
2. 不关闭 `VAL-003`，也不处理 broker `connect=-1` 环境根因。

## Files Changed

- `xtqmt_mcp/trade_ops.py`
- `xtqmt_mcp/trade_gateway/server.py`
- `tests/test_trade_order_submission_contract.py`
- `tests/test_trade_gateway_server.py`

## Self-check

1. `python -m unittest tests.test_trade_order_submission_contract`
2. `python -m unittest tests.test_trade_gateway_server`

## Known Risks

1. 这次只把 `order.cancel` 纳入同级 gate，formal authority carrier 仍未改造。

## Needs Independent Test

1. 独立验证 authority 未闭合时 `order.cancel` 会在本地 gate 被拦截。
2. 独立验证 authority 闭合后 `order.cancel` 的 broker 提交路径仍工作正常。
