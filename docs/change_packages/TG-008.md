# ChangePack

Task ID: TG-008
Role: dev
Date: 2026-04-13
Branch/Commit: /tmp/xtquant-mcp-repo (staging copy)
Stage: implemented-dev-selftest

## Goal

把 trade 资源缓存按语义拆开，消除 `trade://orders/today` 被异构交易工具结果覆盖的问题。

## Implemented

1. 为 `fills.list`、`order.place`、`order.cancel`、`order.status` 分配独立资源 URI。
2. 让 `trade://orders/today` 仅由 `orders.list` 写入。
3. 增加服务端测试，验证资源缓存不再互相污染。

## Not Implemented

1. 不包含 formal authority carrier 改造。
2. 不扩大工具能力面，只调整资源落位。

## Files Changed

- `xtqmt_mcp/trade_gateway/config.py`
- `xtqmt_mcp/trade_gateway/resources.py`
- `xtqmt_mcp/trade_gateway/server.py`
- `tests/test_trade_gateway_server.py`

## Self-check

1. `python -m unittest tests.test_trade_gateway_server`

## Known Risks

1. 当前 consumers 若写死只读 `trade://orders/today` 之外的资源，还需要后续文档 reconcile。

## Needs Independent Test

1. 独立验证新资源 URI 在默认配置下可读。
2. 独立验证 `trade://orders/today` 不再被 `order.place` / `fills.list` 覆盖。
