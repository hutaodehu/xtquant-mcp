# ChangePack

Task ID: VAL-004
Role: test
Date: 2026-03-31T18:31:13.9557860+08:00
Branch/Commit: TBD
Stage: post-login-evidence-captured

## Goal

隔离当时 `broker_blocked` 叙事下的 higher-gate blocker，输出可执行的根因取证结论。

## Implemented

1. 已形成登录前的 formal 测试工件，用于证明当时 first blocker 是“未完成 broker/login 会话导致 broker/session 未就绪”。
2. 用户完成真实登录后，已再次完成 current Windows repo 下的 fresh trade-gateway rerun，并固定新的 listener、`/healthz`、日志和 state/artifact 路径。
3. 已形成最新 formal [EvidencePack](../evidence_packs/VAL-004-test-202603311831-post-login-rerun.md) 和 [EnvSnapshot](../env_snapshots/VAL-004-test-202603311831-post-login-rerun.md)。
4. 已把当前 first blocker 更新为测试结论：登录/会话未就绪问题已解除，新的 first blocker 是 public `orders.list` 路径上的实现错误，报错为 `'NoneType' object has no attribute 'query_open_orders'`。

## Not Implemented

1. 尚未产出新的 `ReviewPack`。
2. 尚未形成任何 `VAL-003` / `G4` 放行结论。
3. 未启动 `VAL-003`，也未进入任何 `order.place` 或其他真实写路径动作。

## Files Expected to Change

- `docs/evidence_packs/VAL-004-test-202603311806-live-warm-fastfail-rerun.md`
- `docs/env_snapshots/VAL-004-test-202603311806-live-warm-fastfail-rerun.md`
- `docs/evidence_packs/VAL-004-test-202603311831-post-login-rerun.md`
- `docs/env_snapshots/VAL-004-test-202603311831-post-login-rerun.md`
- `docs/reviews/VAL-004-review-*.md`
- `docs/task_cards/VAL-004.md`
- `docs/change_packages/VAL-004.md`

## Self-check

1. 确认所有新证据都落在同一 host、同一 bundle、同一实例目录对比窗口下。
2. 确认结论明确拆开“已证实现象”与“未证实根因假设”。
3. 确认登录前与登录后的两轮证据没有混写。
4. 确认没有把当时的 probe / precheck success 误写成 `G4` ready；当前回读本工件时，应按主入口文档中的最新字段语义解释，而不是把这里的旧字段名直接当成现行 contract。

## Known Risks

1. 若继续沿用旧的“未登录”口径，会掩盖登录后已经暴露出的 `orders.list` 设计错误。
2. 若把 probe / precheck success 误写成 `G4 ready`，会直接越过仍未闭合的 public `orders.list` 问题和 `VAL-003` 边界。

## Historical Semantics Note

本 ChangePack 保留的是 `2026-03-31` 当时的字段口径。当前 contract 已进一步收口：

1. `probe.connection` 顶层 `session_id` 现在对齐 write-path resolved session，而不是单纯 probe 观测 session。
2. `write_permission_ready` 现在表示更强的 write authority 闭合，不再等价于当时的 precheck-like 信号。
3. 当前解释优先级以 [MCP_DESIGN.md](../MCP_DESIGN.md)、[OPERATIONS_RUNBOOK.md](../OPERATIONS_RUNBOOK.md) 和 [CURRENT_STATUS.md](../CURRENT_STATUS.md) 为准。

## Needs Independent Test

1. review 需要明确给出分支结论：旧的 `fail_env / broker_blocked` 已不再代表当前真相，最新 first blocker 应按 `fail_design` 评估。
2. review 需要确认 public `orders.list` 的 `NoneType.query_open_orders` 属于实现缺陷，而不是环境回退。
3. review 需要确认当前证据仍不足以放行 `VAL-003` 或任何 `G4` 写路径动作。
