# ChangePack

Task ID: TG-006
Role: dev
Date: 2026-04-13
Branch/Commit: /tmp/xtquant-mcp-repo (staging copy)
Stage: implemented-dev-selftest

## Goal

把 `probe.connection` 与 `order.place` 的写前 gate 收口到同一套 machine-readable authority truth，避免继续双轨判断。

## Implemented

1. `order.place` 的 `connect_gate` 改为消费 `probe.connection` 同源 truth，不再独立采样 shadow gate。
2. 为 `order.place` 增加 additive `write_authority_snapshot`，明确当前 packet 的写前阻断原因与 resolved/observed session。
3. 调整相关 contract 测试，验证 `order.place` 与 `probe.connection` 对同一 fixture 给出一致的 gate 结论。

## Not Implemented

1. 不包含 Markdown/formal authority carrier 的去拼装化。
2. 不包含 session resolution “显式重建、不可原地漂移”的结构重构。
3. 不关闭 `VAL-003`，不修改其 formal truth。

## Files Changed

- `xtqmt_mcp/trade_ops.py`
- `tests/test_trade_order_submission_contract.py`

## Self-check

1. `python -m unittest tests.test_trade_order_submission_contract`
2. `python -m unittest tests.test_trade_gateway_server`

## Known Risks

1. 本卡把 `order.place` 与 `probe.connection` 的写前 gate 收口到单一 truth，但 formal authority 生成链仍未改造。
2. live broker `connect=-1` 环境根因不在本卡范围内。

## Needs Independent Test

1. 独立验证 `order.place.connect_gate` 与 `probe.connection` 在同一 fixture 下的阻断原因一致。
2. 独立验证新增 `write_authority_snapshot` 仅为 additive contract，不破坏既有 envelope。
