# ChangePack 目录

本目录存放每张正式任务卡的 `ChangePack`。

当前首轮文件先以 skeleton 形式建立，作用是预留稳定路径和变更边界。

使用规则：

1. skeleton 不代表已经完成实现。
2. 进入 `In Dev` 后，开发角色必须把对应文件更新为真实变更说明。
3. `Implemented` 与 `Not Implemented` 必须持续维护，不能只在聊天上下文里说明。
