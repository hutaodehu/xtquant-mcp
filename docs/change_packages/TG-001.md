# ChangePack

Task ID: TG-001
Role: dev
Date: 2026-03-31
Branch/Commit: git metadata unavailable in current workspace (`D:\xtquant-mcp\repo` is not a detected git worktree)
Stage: dev_complete_self_checked

## Goal

把 `order.place` 公共 MCP 入口收口为唯一正式受控写路径，并在不进入 live 验证的前提下补齐最小 deterministic code/contract 自检。

## Route Decision

- `base_mode`: `gated_change`
- `overlays`: `manual_gate`, `human_review_required`
- 开发边界：本次只处理 `TG-001`，不触碰 `VAL-003`，不做真实下单验证、真实 `order.place` 执行、live smoke、broker blocker 取证。

## Implemented

1. 将 [xtqmt_mcp/trade_gateway/capability_v2.py](../../xtqmt_mcp/trade_gateway/capability_v2.py) 从薄 broker 提交实现改为兼容适配层，只负责把 MCP `order.place` 路由到 `TradeOpsService.place_order(...)` 代表的受控服务端写路径。
2. 在 `order.place` 兼容适配层内补入服务端 guard token，并显式回填 `governed_write_path=true`、`write_path=governed_service_order_place`、`gate_sequence=[login, session, connectivity, write_permission, risk, kill_switch, audit_persistence]`，让 deterministic 自检可以直接验证公共入口契约。
3. 更新 [xtqmt_mcp/trade_gateway/server.py](../../xtqmt_mcp/trade_gateway/server.py) 的 `order.place` 对外描述，去掉 “thin broker order” 语义，改成 “governed broker order through the server-side write path”。
4. 更新 [xtqmt_mcp/trade_gateway/prompts.py](../../xtqmt_mcp/trade_gateway/prompts.py) 的 `trade-preflight` 提示，明确 `order.place` 属于服务端 gate 执行路径，而不是 prompt 代替安全边界。
5. 在 [tests/test_trade_gateway_server.py](../../tests/test_trade_gateway_server.py) 增加 deterministic 测试，验证公共 `order.place` 入口：
   - 通过 session manager 的 `require_ready=True` 路径执行；
   - 调用的是 `service.place_order(...)`，而不是 capability 内部直通 broker；
   - 不需要真实 broker 或 live 会话即可断言受控路径标记。
6. 顺手修复了同文件测试的 repo-local 临时目录 helper，避免 Python 3.13 / Windows 下 `TemporaryDirectory(...)` 目录 ACL 异常导致本地 deterministic 测试失败；该修复仅影响测试隔离目录，不改变生产逻辑。
7. 同步更新 [docs/MCP_DESIGN.md](../MCP_DESIGN.md)：
   - 正文明确 `order.place` 公共入口必须进入 `TradeOpsService.place_order(...)`；
   - `capability_v2.py` 仅允许作为兼容适配层存在，不得重新引入未受控写路径；
   - 将 `Current Gaps` 从“写路径尚未收口”收敛为“写路径已代码收口，但 `G4` live 证明仍缺失”。

## Not Implemented

1. 未进行真实下单、真实撤单、真实 `fills.list` 追踪，也未生成任何 live broker 证据。
2. 未声明 `G4 pass`、未声明 `VAL-003 ready`、未声明 live broker readiness。
3. 未修改 [docs/task_cards/TG-001.md](../task_cards/TG-001.md)。
4. 未创建 `EvidencePack`、`ReviewPack`、`EnvSnapshot`。
5. 未变更 `order.status`、`order.cancel` 的执行实现；本次仅保证没有破坏它们所在的同一 session / audit 边界。

## Scope Boundary

如果要进一步证明本次改动在真实 broker 环境中正确，后续工作将进入以下超出本卡 dev 边界的范围：

1. `VAL-003` 的独立验证链：`order.place -> order.status -> orders.list -> order.cancel -> fills.list`
2. live 时段、broker 权限、真实 `up_queue_xtquant` 写权限、最小真单证据链

本次在上述边界之前停止，只提供 deterministic code/contract self-check。

## Files Changed

- [xtqmt_mcp/trade_gateway/capability_v2.py](../../xtqmt_mcp/trade_gateway/capability_v2.py)
- [xtqmt_mcp/trade_gateway/server.py](../../xtqmt_mcp/trade_gateway/server.py)
- [xtqmt_mcp/trade_gateway/prompts.py](../../xtqmt_mcp/trade_gateway/prompts.py)
- [tests/test_trade_gateway_server.py](../../tests/test_trade_gateway_server.py)
- [docs/MCP_DESIGN.md](../MCP_DESIGN.md)

## Self-check

Command:

```powershell
python -m unittest tests.test_trade_gateway_server -v
```

Result:

```text
Ran 5 tests in 0.005s
OK
```

Checked points:

1. `test_order_place_routes_through_governed_server_write_path`
   证明 `order.place` 公共入口经过 `session_manager.execute(... require_ready=True)` 并调用 `service.place_order(...)`。
2. `test_primary_account_flow_schemas_hide_explicit_account_inputs`
   证明 `order.place` 的对外描述已切换为 governed path，不再暴露 thin broker order 口径。
3. 其余同文件测试继续通过，说明本次收口没有破坏现有单账户 schema、资源缓存 fake-block 规则和显式账户拒绝行为。

## Known Risks

1. 当前只证明了代码路由、契约口径和 deterministic 输出，没有证明真实 broker 侧写入链路在 live 环境中完全成立。
2. `TradeOpsService.place_order(...)` 的 gate 顺序和持久化路径现在是公共入口唯一正式路径，但真实 broker/session 行为仍可能在 `VAL-003` 中暴露环境或集成问题。
3. 本次通过服务端内部 guard token 进入受控写路径；如果未来服务端写路径再被其他入口复用，必须继续保持该 guard 只由服务端注入，而不是由公共 schema 外露。

## Needs Independent Test

1. 在独立测试中确认 `order.status`、`order.cancel` 与 `order.place` 仍保持同一 session / audit 边界，没有因为入口收口产生状态查询或撤单回归。
2. 按 [docs/ACCEPTANCE_STANDARD.md](../ACCEPTANCE_STANDARD.md) 的 `G4` 链路执行最小真单验证，并由 `VAL-003` 形成正式 `EvidencePack` / `ReviewPack`。
3. 验证 gate 缺失时的失败分类是否仍与 live broker 侧行为一致，尤其是 write permission、risk、kill-switch 与 audit artifact 留档。

## Recommended Next Status

`In Independent Test`
