# ChangePack

Task ID: OPS-008
Role: dev
Date: 2026-04-13
Branch/Commit: /tmp/xtquant-mcp-repo (staging copy)
Stage: implemented-dev-selftest

## Goal

把 formal authority carrier 从 Markdown + cache 拼装迁移到 packet-bound typed source。

## Implemented

1. `trade_write_authority.py` 改为只消费 packet-bound typed authority source 与 `diag_probe` runtime truth，不再从 `CURRENT_STATUS.md` 解析 formal lane truth。
2. `run_controller_direct_test.ps1` 新增 `trade_write_authority_source` 产物输出，并维护 `trade_write_authority_source_latest.json`。
3. `run_trade_write_authority_check.ps1` 改为把 typed authority source 传给 authority 生成脚本。
4. 新增 fail-closed 测试，覆盖缺 carrier、trace mismatch 与 typed source runtime/formal green 三类场景。
5. authority builder 对 fresh/reopen packet 的 `review_ref` 改为 warning 语义：`trade_lane_write_closed=false` 时不再把顶层 blocker 退化成 `authority_source_incomplete`，但 close/pass packet 仍要求 `review_ref` 才能放行。

## Not Implemented

1. 不在本卡内修改 `VAL-003` 的 formal truth 文档。
2. 不在本卡内处理 live packet 的 reopen / pass 结论。

## Files Changed

- `xtqmt_mcp/trade_write_authority.py`
- `scripts/run_controller_direct_test.ps1`
- `scripts/run_trade_write_authority_check.ps1`
- `tests/test_trade_write_authority.py`
- `docs/task_cards/OPS-008.md`
- `docs/change_packages/OPS-008.md`

## Self-check

1. `python -m unittest tests.test_trade_write_authority`

## Known Risks

1. controller judgment 仍保留 Markdown 人工审阅输出；机器 gate 已不再依赖 `CURRENT_STATUS.md`，但仍依赖该 judgment ref 能被 carrier 正确回链。
2. reopen packet 顶层 blocker 现在优先暴露 formal/runtime 具体原因；若下游脚本硬编码匹配 `formal_trade_write_lane_not_closed`，需要同步按更具体 reason 适配。

## Needs Independent Test

1. 独立验证缺失或错配 `trade_write_authority_source_latest.json` 时 authority 会 fail closed。
2. 独立验证 typed carrier 与 `diag_probe_latest.json` 的 trace mismatch 不会把 authority 拼绿。
