# ChangePack

Task ID: OPS-003
Role: dev
Date: 2026-03-30
Branch/Commit: not a git repository in current workspace (`git rev-parse --short HEAD` failed)
Stage: implemented-dev-selftest

## Goal

修复 spec-task-harness 对 mixed timestamp artifact naming 的 latest artifact 选择偏差。

## Implemented

1. 在 `.agents/skills/spec-task-harness/scripts/harness_common.py` 中把文件名时间戳提取从单一 `YYYYMMDDHHMM` 末尾样式扩展为统一归一：
   - `YYYYMMDD-HHMMSS`
   - `YYYYMMDD_HHMMSS`
   - `YYYYMMDDHHMMSS`
   - `YYYYMMDDHHMM`
2. 对 `YYYYMMDDHHMM` 样式统一补 `00` 秒，按 14 位时间戳参与排序，保证 mixed naming 风格可直接比较。
3. 保持 `collect_artifacts.py`、`reconcile_runledger.py`、`select_next_safe_tasks.py` 的入口和 JSON 输出字段不变，只修 latest artifact 选择逻辑。
4. 新增 `tests/test_spec_task_harness.py`，覆盖混合命名时间戳归一和 latest artifact 选择回归。

## Not Implemented

1. 没有调整 review decision 的解析规则；本卡只解决 latest artifact 选取，不扩展 review 文本规范兼容面。
2. 没有修改 board sync contract、controller 角色边界或调度策略。
3. 没有新增 EvidencePack、EnvSnapshot、ReviewPack；这些留给后续 test/review。

## Files Expected to Change

- `.agents/skills/spec-task-harness/scripts/harness_common.py`
- `tests/test_spec_task_harness.py`
- `docs/change_packages/OPS-003.md`

## Self-check

1. `python -m unittest tests.test_spec_task_harness`
   - 结果：`Ran 2 tests ... OK`
2. `python .agents/skills/spec-task-harness/scripts/collect_artifacts.py --repo-root . --task-id VAL-002`
   - 结果：`latest_review_pack` 选中 `docs/reviews/VAL-002-review-20260330-143606-live-gateway-rerun-orders-fallback.md`
   - 结果：`latest_evidence_pack` 选中 `docs/evidence_packs/VAL-002-test-20260330-142745-live-gateway-rerun-orders-fallback.md`
3. `python -m pytest tests/test_spec_task_harness.py`
   - 结果：失败，当前解释器环境缺少 `pytest` 模块（`No module named pytest`）
4. `git rev-parse --short HEAD`
   - 结果：失败，当前目录不是 git repository

## Known Risks

1. 若后续出现新的文件名时间戳风格且不匹配当前四种模式，artifact 选择仍可能退回到 `mtime`。
2. 本卡没有扩大 review 文本解析兼容面；若 latest review 已选中但其 `Decision` 行格式不符合既有正则，后续 reconcile 仍可能需要单独修复那一层。

## Needs Independent Test

1. test 需要复跑 repo-only reconcile，确认 `VAL-002` 不再回落到旧的 `202603300701` review/evidence 对。
2. review 需要确认 timestamp 归一没有破坏其他 artifact 目录下的 latest 选择 contract。
