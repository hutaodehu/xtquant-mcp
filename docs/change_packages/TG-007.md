# ChangePack

Task ID: TG-007
Role: dev
Date: 2026-04-13
Branch/Commit: /tmp/xtquant-mcp-repo (staging copy)
Stage: implemented-dev-selftest

## Goal

把 session resolution 从运行时原地漂移收口为显式重建/切换事件。

## Implemented

1. `TradeOpsService` 改为区分 bootstrap/base session truth 与 runtime override，不再让 `realign_session_resolution()` 原地改写基础真相。
2. `session_manager` summary 新增 `effective_session_resolution` 与 `runtime_session_override`，同时保留 base `session_resolution`。
3. 调整 bootstrap / session manager 测试，验证 base truth 保持不变，effective view 才承接 runtime realign。

## Not Implemented

1. 不在本卡内处理 formal authority carrier。
2. 不在本卡内处理 live broker `connect=-1` 的宿主根因。

## Files Changed

- `xtqmt_mcp/session_resolution.py`
- `xtqmt_mcp/trade_ops.py`
- `xtqmt_mcp/trade_gateway/session_manager.py`
- `tests/test_trade_gateway_bootstrap.py`
- `tests/test_trade_gateway_session_manager.py`
- `docs/task_cards/TG-007.md`
- `docs/change_packages/TG-007.md`

## Self-check

1. `python -m unittest tests.test_trade_gateway_bootstrap tests.test_trade_gateway_session_manager`
2. `python -m unittest tests.test_trade_probe_readiness_split`

## Known Risks

1. 目前 write-adjacent payload 仍保留兼容字段 `session_resolution`，operator 需要以新增的 base/effective 分层理解其语义。

## Needs Independent Test

1. 独立验证 `session.status` / `session.warm` 的 base/effective 分层不会误导 operator。
2. 独立验证 broker adapter 重建继续读取 effective session view，而不是依赖被覆写的 config。
