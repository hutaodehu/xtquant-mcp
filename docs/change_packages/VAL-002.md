# ChangePack

Task ID: VAL-002
Role: test
Date: 2026-03-30
Branch/Commit: local-worktree (no git metadata in current sandbox)
Stage: test_evidence_recorded

## Goal

执行 Trade Gateway 首轮只读 smoke，并沉淀 `G3` 正式证据。

## Implemented

1. 在真实 prod 实例下按 `G3` 顺序执行 live smoke：
   - `GET /healthz`
   - `miniqmt.ensure_logged_in`
   - `session.warm`
   - `session.status`
   - `probe.connection`
   - `account.show`
   - `positions.list`
   - `orders.list`
   - `snapshot.l1`
2. 补读 `trade://session/current` 与 `diag://probe/latest` 资源，确认 session/probe 落盘视图。
3. 采集并回链运行态证据：
   - `trace_id`
   - `server_ts`
   - `D:\xtquant-mcp\instance\prod\artifacts/trade_gateway\20260330\trade_gateway_calls.jsonl`
4. 产出正式 `EnvSnapshot` 与 `EvidencePack`，并持续更新最新 rerun 结论。

## Not Implemented

1. 尚未形成完整 `G3 pass`。
2. `miniqmt.ensure_logged_in`、`probe.connection`、`orders.list` 仍未全部通过。
3. 未执行 review gate。

## Files Expected to Change

- `docs/evidence_packs/VAL-002-test-202603300306.md`
- `docs/env_snapshots/VAL-002-test-202603300306.md`
- `docs/task_cards/VAL-002.md`
- `docs/change_packages/VAL-002.md`

## Files Changed

1. `docs/change_packages/VAL-002.md`
2. `docs/task_cards/VAL-002.md`
3. `docs/evidence_packs/VAL-002-test-202603300306.md`
4. `docs/env_snapshots/VAL-002-test-202603300306.md`

## Self-check

1. pre-state 中 `127.0.0.1:8765` 无 listener；post-state `/healthz` 返回 `ok=true`，且 `account_contract=single_account_primary`、`account_input_mode=service_context_only`。
2. `miniqmt.ensure_logged_in` 返回 `desktop_not_interactive`。
3. `session.warm` 与 `session.status` 返回 `ready=true`，`trade://session/current` 也返回 `ready=true` / `session_id=101`。
4. `probe.connection` 与公开 `orders.list` 仍因 `xttrader connect failed: -1` 失败；`account.show`、`positions.list`、`snapshot.l1` 成功。

## Known Risks

1. 当前主要风险是环境交互层仍不稳定：MiniQMT 进程存在不等于可被 login/session 流程消费。
2. 在环境未恢复前，任何对 `TG-002/TG-003/TG-004` 的放行判断都不能声称完成 live G3 闭环。

## Needs Independent Test

1. 需要结合最新 fresh gateway-side rerun 证据重新评估当前剩余 blocker，而不是只依赖旧的 `session_not_ready` 叙事。
2. review 若要消费本次证据，应按最新结论 `partial` 且任务级 `blocked` 处理，不能误写为 `pass`，也不能据此启动 `VAL-003`。

## Update 2026-03-30T12:46:17.578620+08:00

### Fresh Gateway-side Rerun

1. 新增正式工件：
   - `docs/evidence_packs/VAL-002-test-20260330-124617-live-gateway-rerun.md`
   - `docs/env_snapshots/VAL-002-test-20260330-124617-live-gateway-rerun.md`
2. 本次 pre-state 与上一轮不同：
   - `127.0.0.1:8765` 在重跑前无 listener
   - `/healthz` 连接被拒绝
3. 仍按 repo-supported 路径执行：
   - `pwsh -File D:\xtquant-mcp\repo\scripts\wake_trade_gateway.ps1`
   - shell 调用超时，但新进程 `pid=34748`、`start_time=2026-03-30T12:40:38.004714+08:00`、新日志文件和健康 `/healthz` 共同证明 fresh process 已加载当前 repo 代码

### Latest Live G3 Result

1. 本轮不再停在 `session.warm`：
   - `session.warm=ready`
   - `session.status=ready`
   - `trade://session/current=ready`
2. 当前成功项：
   - `account.show`
   - `positions.list`
   - `snapshot.l1`
3. 当前失败项：
   - `miniqmt.ensure_logged_in=desktop_not_interactive`
   - `probe.connection=connect_failed`
   - `orders.list=xttrader connect failed: -1 after 3 attempts (...)`
4. 这说明 failure shape 已从“`session.warm` 后整体 `session_not_ready`”转为“已建立 owner-managed session，但登录探测与公开 connect-based 读路径仍失败”。

### Current Stage

1. 最新正式 EvidencePack 结论：`partial`
2. 任务级 posture：`blocked`
3. `VAL-003`：仍不得启动

## Update 2026-03-30T13:43:30.636772+08:00

### Fresh Live Rerun After TG-004 Probe Reuse Patch

1. 新增正式工件：
   - `docs/evidence_packs/VAL-002-test-20260330-134330-live-gateway-rerun.md`
   - `docs/env_snapshots/VAL-002-test-20260330-134330-live-gateway-rerun.md`
2. 本次 pre-state 与上一轮 fresh rerun 一致：
   - `127.0.0.1:8765` 在重跑前无 listener
   - `/healthz` 连接被拒绝
3. 仍按 repo-supported 路径执行：
   - `pwsh -File D:\xtquant-mcp\repo\scripts\wake_trade_gateway.ps1`
   - shell 调用返回 `exit_code=0`
   - 虽然脚本 JSON 未在当前 shell 回显，但新进程 `pid=36532`、`start_time=2026-03-30T13:37:03.631422+08:00`、新日志文件和健康 `/healthz` 共同证明 fresh process 已加载当前 repo 代码

### Latest Live G3 Result

1. 本轮 `miniqmt.ensure_logged_in` 已恢复为独立成功：
   - `miniqmt.ensure_logged_in=already_logged_in`
   - `diag://login/latest=already_logged_in`
2. owner-managed session 仍保持 ready：
   - `session.warm=ready`
   - `session.status=ready`
   - `trade://session/current=ready`
3. `probe.connection` 已从连接失败转为真实成功：
   - `probe.connection=ok`
   - `probe_mode=owner_managed_session_reuse`
   - `readiness_layers.read_only.source=active_owner_shadow`
   - `readiness_layers.write_permission.source=userdata_precheck`
   - `write_permission_probe.implies_write_permission=false`
4. 当前成功项：
   - `account.show`
   - `positions.list`
   - `snapshot.l1`
5. 当前失败项：
   - public `orders.list=xttrader connect failed: -1 after 3 attempts (...)`
6. 这说明 failure shape 再次收窄：
   - 上一轮 fresh rerun 仍是 `login + probe + public orders.list` 三处失败
   - 本轮 live `G3` 中，`login` 与 `probe` 都已恢复，剩余正式 blocker 收缩到 public `orders.list`

### Current Stage

1. 最新正式 EvidencePack 结论：`partial`
2. 任务级 posture：`blocked`
3. `VAL-003`：仍不得启动
4. 当前没有新的 `fail_design` 证明；本轮已验证的是 `TG-004` probe 语义修补在真实运行态生效，且没有把 public `orders.list` 或 `miniqmt.ensure_logged_in` 静默掩盖

## Historical Semantics Note

本 ChangePack 保留的是 `2026-03-30` 当时的 live rerun 记录。当前回读时请注意：

1. 其中 `write_permission_probe.implies_write_permission=false`、`probe.connection=ok` 等表述仅反映当时 payload 形态，不代表当前 contract 仍沿用同一字段语义。
2. 当前 `probe.connection` / write-path contract，应以 [MCP_DESIGN.md](../MCP_DESIGN.md) 与 [OPERATIONS_RUNBOOK.md](../OPERATIONS_RUNBOOK.md) 为准。
3. 历史 `VAL-002` 工件的价值在于保留当时的 `G3` 现场真相，不用于覆盖当前 `VAL-003/G4` 的 source-of-truth。

## Update 2026-03-30T14:27:45.669592+08:00

### Fresh Live Rerun After TG-004 Public `orders.list` Fallback Patch

1. 新增正式工件：
   - `docs/evidence_packs/VAL-002-test-20260330-142745-live-gateway-rerun-orders-fallback.md`
   - `docs/env_snapshots/VAL-002-test-20260330-142745-live-gateway-rerun-orders-fallback.md`
2. 本次 pre-state 与上一轮 fresh rerun 不同：
   - `127.0.0.1:8765` 在重跑前已有 listener
   - old pid=`36532`
   - old start_time=`2026-03-30T13:37:03.631422+08:00`
   - `/healthz` 在重跑前仍为 `ok=true`
3. 仓库仍只提供启动 helper、未提供独立 restart helper：
   - 继续采用 repo-supported path `pwsh -File D:\xtquant-mcp\repo\scripts\wake_trade_gateway.ps1`
   - 为保证 fresh repo code 被加载，本轮先受控停止旧 listener，再执行 supported wake path
   - 组合 stop+wake shell capture 超时，但 fresh process `pid=3984`、`start_time=2026-03-30T14:17:23.795229+08:00`、新日志文件和 post-restart `/healthz` 共同证明最新 repo 代码已加载

### Latest Live G3 Result

1. `miniqmt.ensure_logged_in`、warm/status、probe 和只读查询均保持通过：
   - `miniqmt.ensure_logged_in=already_logged_in`
   - `session.warm=ready`
   - `session.status=ready`
   - `probe.connection=ok`
   - `account.show=ok`
   - `positions.list=ok`
   - `snapshot.l1=ok`
2. public `orders.list` 已从 formal blocker 转为显式 fallback success：
   - `ok=true`
   - `degraded=true`
   - `fallback_used=true`
   - `fallback_reason=broker_connect_failed`
   - `source=active_owner_shadow`
   - `read_scope=public_fallback`
   - 同一 payload 内仍明确保留 `broker_read.error=xttrader connect failed: -1 after 3 attempts (...)`
3. 这说明本轮 delta 是：
   - 上一轮 fresh rerun：public `orders.list` 仍是显式 connect failure
   - 本轮 fresh rerun：public `orders.list` 按新 contract 返回 broker-first + explicit owner-shadow fallback success
4. 额外 observation 继续单列记录：
   - `session.status` / `trade://session/current` 为 `session_id=100`
   - `probe.connection` / `diag://probe/latest` 复用 shadow `session_id=111`
   - 当前只记 observation，不作为本轮 blocker

### Current Stage

1. 最新正式 EvidencePack 结论：`pass`
2. 工作流 posture：由 `Blocked` 变为建议进入 `In Review`
3. 本轮未启动 `VAL-003`
4. 是否可以进一步放行：仍需独立 ReviewPack 决定
