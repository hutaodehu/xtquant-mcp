# ChangePack

Task ID: OPS-004
Role: dev
Date: 2026-03-30T18:00:40.7764354+08:00
Branch/Commit: not a git repository in current workspace (`Test-Path .git` -> `False`; `git rev-parse --short HEAD` failed)
Stage: implemented-dev-selftest

## Goal

修复 spec-task-harness 对 formal ReviewPack 中 backticked `Decision` 行的解析兼容性，让 latest review 已经被正确选中时，controller 能恢复真实 `review_decision`，不再把任务误停留在 `tested_local` / `review_needed`。

## Implemented

1. 在 `.agents/skills/spec-task-harness/scripts/harness_common.py` 中收窄扩展 `REVIEW_DECISION_PATTERN`：
   - 继续只接受正式结论词 `pass` / `needs_fix` / `blocked`
   - 新增对成对单反引号包裹样式的兼容，例如 ``- Decision: `pass```、``- Decision: `needs_fix```、``- Decision: `blocked```
   - 保持 bare token 旧写法继续可解析
2. 解析命中后改为读取命名分组 `decision`，避免因为新增包裹分组而破坏既有 decision 归一逻辑。
3. 在 `tests/test_spec_task_harness.py` 新增回归覆盖：
   - 验证三种 backticked `Decision` token 都能被 latest review helper 正确提取
   - 构造 `VAL-002` 风格的 mixed-timestamp review fixture，证明 latest review 已正确选中时，不会再因为 decision token 带反引号而停留在 `review_needed`
4. 未改动 `collect_artifacts.py`、`reconcile_runledger.py`、`select_next_safe_tasks.py` 的外部 JSON 字段或返回结构；修复只落在共享解析 helper 和回归测试。

## Not Implemented

1. 没有修改 board sync contract 或任何 board 字段语义。
2. 没有回写或重写历史 ReviewPack 文本。
3. 没有调整 OPS-003 已完成的 latest artifact ordering 逻辑；本卡只修 review decision parsing residual。

## Files Changed

- `.agents/skills/spec-task-harness/scripts/harness_common.py`
- `tests/test_spec_task_harness.py`
- `docs/change_packages/OPS-004.md`

## Self-check

1. `python -m unittest tests.test_spec_task_harness`
   - Exit Code: `0`
   - Result:
     ```text
     ....
     ----------------------------------------------------------------------
     Ran 4 tests in 0.014s

     OK
     ```
2. `python .agents\skills\spec-task-harness\scripts\collect_artifacts.py --repo-root . --task-id VAL-002`
   - Exit Code: `0`
   - Result:
     - `local_stage`: `reviewed_pass_local`
     - `controller_action`: `controller_closeout`
     - `latest_review_pack`: `D:\xtquant-mcp\repo\docs\reviews\VAL-002-review-20260330-143606-live-gateway-rerun-orders-fallback.md`
     - `latest_review_decision`: `pass`
3. `python .agents\skills\spec-task-harness\scripts\select_next_safe_tasks.py --repo-root .`
   - Exit Code: `0`
   - Result:
     - `closeout_candidates` 现包含 `VAL-002`
     - 顶层字段仍为 `mode` / `controller_action` / `closeout_candidates` / `validation_preparation` / `dispatchable_candidates` / `selected_tasks`

## Known Risks

1. 当前兼容范围仍是 formal `Decision` 行加成对单反引号；如果后续出现更宽松的 Markdown 变体，例如额外强调符号、行尾补注释或非成对包裹，仍会保持未解析状态。
2. 本卡没有改变 review 之外的字段提取规则；若未来还有其他 templated header/value 写法进入 formal artifact，可能需要独立 follow-up，而不应继续往本卡混 scope。

## Needs Independent Test

1. test 需要确认 bare token 与 backticked token 两种 ReviewPack `Decision` 写法在 repo-only reconcile 下都能稳定恢复相同 `local_stage` / `controller_action`。
2. test 需要确认 `collect_artifacts.py`、`reconcile_runledger.py`、`select_next_safe_tasks.py` 的外部 JSON contract 仍与 OPS-003 后的字段集合一致。

## Recommended Next Board Status

- Target Status: `In Independent Test`
- Reason: 开发实现和自测已完成，剩余工作是让独立测试确认 backticked ReviewPack decision 兼容修复没有引入 contract drift。
