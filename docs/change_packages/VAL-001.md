# ChangePack

Task ID: VAL-001
Role: test
Date: 2026-03-30
Branch/Commit: local-worktree (no git metadata in current sandbox)
Stage: test_evidence_recorded

## Goal

执行 Data Gateway 首轮只读 live smoke，并沉淀正式 `EvidencePack`。

## Implemented

1. 在真实 prod 实例下完成 `G2` live smoke：
   - `GET /healthz`
   - `xtdata.status`
   - `xtdata.history.get_bars`
   - `xtdata.snapshot.batch`
2. 附加执行并记录：
   - `xtdata.calendar.query`
   - `xtdata.instruments.search`
   - `resources/read xtdata://leases/active`
3. 采集并回链运行态证据：
   - `trace_id`
   - `server_ts`
   - `D:\xtquant-mcp\instance\prod\artifacts\data_gateway\20260330\data_gateway_calls.jsonl`
4. 产出正式 `EnvSnapshot` 与 `EvidencePack`。

## Not Implemented

1. 未覆盖 subscription lease 在断连、恢复、重启残留场景下的重建稳定性验证。
2. 未执行 review gate；当前只完成测试证据沉淀。

## Files Expected to Change

- `docs/evidence_packs/VAL-001-test-202603300307.md`
- `docs/env_snapshots/VAL-001-test-202603300307.md`
- `docs/task_cards/VAL-001.md`
- `docs/change_packages/VAL-001.md`

## Files Changed

1. `docs/change_packages/VAL-001.md`
2. `docs/task_cards/VAL-001.md`
3. `docs/evidence_packs/VAL-001-test-202603300307.md`
4. `docs/env_snapshots/VAL-001-test-202603300307.md`

## Self-check

1. `xtdata.status` live 调用返回 `ready=true`，同时给出 configured/resolved endpoint 与六层 readiness。
2. `xtdata.history.get_bars` 和 `xtdata.snapshot.batch` 对 `000001.SZ` 返回有效数据。
3. `xtdata://leases/active` 在同窗下可读，且 runtime endpoint 与 `xtdata.status` 对齐。

## Known Risks

1. 这次 smoke 证明了 `DG-001` 的 live G2 主链路，但只部分补齐 `DG-002` 的 review 要求；lease reconnect/rebuild 仍需额外情景证据。
2. 当前验证样本集中在单标的 `000001.SZ` 和单市场日历，不代表更大样本面完全无差异。

## Needs Independent Test

1. review 应判定这份 live G2 证据对 `DG-001` 是否足以放行。
2. review 应判定 `DG-002` 是否仍需补断连/恢复类 lease 健康证据。
