# ChangePack

Task ID: VAL-003
Role: test
Date: 2026-04-13
Branch/Commit: local-worktree (no git metadata in current workspace)
Stage: active-followup-fresh-blocked

## Goal

把 `VAL-003 / G4` 的正式文档口径从停留在 `2026-04-03` 历史 governed-write baseline，更新为当前 active follow-up 语义：

1. `session_resolution.effective_session_plan` 是 canonical session plan 的唯一主真相。
2. 当前最新 formal truth 是 `2026-04-11` 的 fresh blocked，而不是旧 `connect_gate_failed`。
3. packet readiness 的正式入口是 `scripts/check_packet_readiness.ps1` 输出的 machine-readable `go/no_go`，并由 `scripts/run_controller_direct_test.ps1` 的 Round 1/2 hard-stop 消费。
4. `session.warm / session.status / probe.connection` 现在必须对齐同一 resolved write session；gateway-side fresh authority 是 Round 2 主 gate，legacy native probe 只保留为诊断补充。

## 当前 formal truth

1. 当前任务级状态继续保持 `Blocked`，但当前唯一 blocker 已切换为 `data_gateway_port_conflict_nonrepo_listener_8766`。
2. 该结论来自 `2026-04-11 00:30 +08` 的 fresh controller-direct packet 与 `2026-04-11 00:34 +08` 的独立 `ReviewPack`：
   - trade wake=`started` 且 health 正常
   - data wake=`port_conflict`
   - `8766` listener 不是当前 repo 期望的 `xtqmtDataGateway`
   - `order.place` 未执行
3. 因此当前 formal truth 必须收口为 fresh blocked，而不是继续沿用 `2026-04-03 / 2026-04-07` 的 `connect_gate_failed` 作为本轮结论。
4. `2026-04-11 00:30:25 +08` 的 pre-run artifact snapshot 仍指向 `2026-04-09` 的旧工件，这个 snapshot 只能作为 run 前镜像，不覆盖同窗 fresh packet 的 final closeout。

## 当前 canonical session plan 基线

1. 当前最近一轮完整记录 canonical session plan 的 machine-readable packet 是 `2026-04-09 14:13 +08`：
   - `preflight effective session plan=2111,2100,2101`
   - `preflight same-plan verdict=True`
   - `native probe same-plan verdict=True`
2. 该 packet 同时明确表明 write authority 尚未闭合：
   - `native probe overall_ok=False`
   - `host recovery ok=False`
   - `fresh_connect_verified=False`
   - `real order.place executed=False`
3. 因此当前可写入正式文档的结论是：
   - canonical session plan 语义已经收口到 `2111,2100,2101`
   - 但 fresh native probe / fresh connect 没有闭合
   - 这不是 `G4 pass`，也不是 formal write authority
4. 当前 runtime payload 已经把 `session_plan_version` 落到 `probe.connection`、`order.place(connect_gate)`、`trade_write_authority` 与 `check_packet_readiness.ps1`；但 reviewed formal artifacts 还没有在 fresh packet/ReviewPack 中把这批字段正式回收。`canonical_session_plan_version` 仍未单独落盘；在显式字段出现前，文档必须直接引用 canonical plan 文本及其证据来源，不能凭空发明版本号。

## Packet Readiness 入口

1. 当前仓内已新增独立 `check_packet_readiness.ps1`。
2. 现行正式对应物是该脚本输出的 machine-readable `go/no_go`，加上 `scripts/run_controller_direct_test.ps1` 的 Round 1/2 readiness hard-stop 与同窗 `Controller Judgment`。
3. 因此凡是文档中出现 `probe_complete_verdict`、packet readiness、preflight go/no-go 等语义，都必须回链到：
   - controller judgment
   - 对应 `EvidencePack`
   - 对应 `EnvSnapshot`
   - 独立 `ReviewPack`

## `2026-04-13` 运行推进（non-formal）

1. 当前 change package 的代码层推进已完成以下收口：
   - packet readiness 改为优先消费 gateway-side fresh authority
   - `session.warm / session.status` 已能跟随 resolved write session realign
   - `probe.connection` 在 owner shadow 与 resolved write session 不同时，仍会继续尝试 broker fresh verify
2. 当前尚未形成新的 formal closeout；`2026-04-11` 的 reviewed blocked truth 不变。
3. 最新 live runtime 的唯一剩余 blocker 已明确缩小为 `write_connect_failed`：
   - `session.warm / session.status / probe.connection` 的 top-level `session_id` 都已能收口到 `2101`
   - 但 `2101` 上的 broker fresh connect 仍为 `-1`
   - 因此当前离真实 `order.place` 还差最后一道 broker fresh connect closure，而不是再差一层文档、登录或 session-plan 对齐

## Implemented

1. 更新 [VAL-003_G4_EXECUTION_PLAN.md](../VAL-003_G4_EXECUTION_PLAN.md)，把当前 posture、canonical session plan、packet readiness 入口与 fresh blocked truth 收口到同一份 formal execution plan。
2. 更新 [task_cards/VAL-003.md](../task_cards/VAL-003.md)，明确 `2026-04-09 14:13 +08` 的 canonical session plan 基线与 `2026-04-11 00:30 +08` 的 fresh blocked truth 之间的边界。
3. 更新 [ACCEPTANCE_STANDARD.md](../ACCEPTANCE_STANDARD.md)，把 `probe_complete_verdict`、`session_plan_version/canonical_session_plan_version`、`check_packet_readiness.ps1` 的当前正式解释写入验收标准，并回链到新脚本输出。
4. 新增 [SESSION_PLAN_DECISION_LOG.md](../SESSION_PLAN_DECISION_LOG.md)，作为当前 canonical session plan、最新 blocker、下一交易窗口前置条件的单一决策入口。
5. 更新 [CURRENT_STATUS.md](../CURRENT_STATUS.md)，明确 `2026-04-11` pre-run artifact snapshot 仍是旧 formal 工件镜像，current truth 必须以同窗 `EvidencePack / ReviewPack` 为准。

## Not Implemented

1. 没有新增 live packet、没有执行新的 `order.place`。
2. 没有新增 live packet、没有执行新的 `order.place`。
3. `session_plan_version` 已在 runtime payload 落地，但 reviewed formal artifacts 仍需下一轮 fresh packet/ReviewPack 才能完成正式回收；`canonical_session_plan_version` 仍未新增为单独 machine-readable 字段。

## Files Changed

- `docs/ACCEPTANCE_STANDARD.md`
- `docs/VAL-003_G4_EXECUTION_PLAN.md`
- `docs/task_cards/VAL-003.md`
- `docs/change_packages/VAL-003.md`
- `docs/SESSION_PLAN_DECISION_LOG.md`
- `docs/CURRENT_STATUS.md`

## Known Risks

1. 当前最新 formal truth 是 out-of-scope 外部阻断：`8766` 被非 repo listener 占用；在该阻断解除前，任何 reopen 讨论都只能停在 fresh blocked。
2. 当前 canonical session plan 文本已经可引用，但 fresh native probe / fresh connect 尚未闭合；若后续有人把 same-plan 成立误写成 probe complete 或 write-ready，仍会造成假成功。
3. current runtime 已显式落 `session_plan_version`，但 reviewed formal artifacts 还未在 fresh packet 中完成正式回收；`canonical_session_plan_version` 仍未单独实现，字段与 formal truth 之间仍有下一轮收口工作。

## Needs Independent Test

1. 下个交易窗口前先恢复 data wake：`8766` 必须回到 repo 期望 listener，trade/data wake 都返回 `ok=true`。
2. 在新的 fresh packet 中重新完成 Round 2 bounded native probe，并继续要求它绑定当前 canonical session plan，而不是继承旧 probe 结果。
3. 若新 packet 再次形成 no-go，必须先补独立 `ReviewPack`，再决定是否刷新 state truth；不能只凭 runner runtime capture 改写 current formal truth。
