# ChangePack

Task ID: OPS-002
Role: dev
Date: 2026-03-30T18:40:13+08:00
Branch/Commit: N/A (`D:\xtquant-mcp\repo` 当前不是 git worktree)
Stage: dev_followup_complete_pending_independent_test

## Goal

修正 `OPERATIONS_RUNBOOK` 的 review-facing contract，使 formal review verdict 与 board `Review Result` 字段语义分离，同时保留 OPS-002 已接受的 runbook 结构、`VAL-002` / `VAL-003` posture、startup/preflight/hard-stop 内容和现有 backlinks。

## Implemented

1. 按 [OPS-002-review-20260330-183442.md](../reviews/OPS-002-review-20260330-183442.md) 的 high-severity finding，对 [OPERATIONS_RUNBOOK.md](../OPERATIONS_RUNBOOK.md) 的 `Operator Output Rules` 做 follow-up 修正。
2. 将 formal review verdict 明确收敛为仓库统一正式词汇：`pass`、`partial`、`blocked`、`fail_env`、`fail_design`，不再把 `needs_fix` 写成 `ReviewPack` 结论。
3. 保留 `needs_fix` 的唯一方式被显式缩窄为外部看板或任务卡 `Review Result` 字段语义，避免后续 operator 把 board enum 误写进正式 review artifact。
4. 保持 runbook 其余 contract 不变：`VAL-002/G3` truthful degraded posture、当时的 `VAL-003/G4 broker_blocked` posture、startup order、minimum preflight、`G0/G1/G3/G4` hard stops、higher-gate recovery flow、evidence checklist、environment-vs-design classification 及现有 backlinks 未被扩散修改。

## Not Implemented

1. 未修改任何 Trade/Data Gateway 业务实现、gateway 行为或 live 运行逻辑。
2. 未编辑 [AGENTS.md](../../AGENTS.md)、[WORKFLOW_AND_BOARD.md](../WORKFLOW_AND_BOARD.md)、[TEMPLATES.md](../TEMPLATES.md) 或其他非 owned write scope 文件。
3. 未创建新的 EvidencePack、EnvSnapshot 或 ReviewPack；本次只修正 dev 范围内的文档 contract。
4. 未宣称当时 `broker_blocked` 叙事已解除，也未宣称 `VAL-003/G4` 或 live 写路径已可放行。

## Files Changed

- `docs/OPERATIONS_RUNBOOK.md`
- `docs/change_packages/OPS-002.md`

## Self-check

1. `rg -n "Operator Output Rules|ReviewPack|Review Result|needs_fix|fail_design|fail_env|partial" D:\xtquant-mcp\repo\docs\OPERATIONS_RUNBOOK.md`
   - 结果：`Operator Output Rules` 中现已明确 formal `ReviewPack` 只能使用 `pass` / `partial` / `blocked` / `fail_env` / `fail_design`；`needs_fix` 仅保留为 board / `Review Result` 字段语义，不再作为 formal artifact verdict。
2. `rg -n "needs_fix" D:\xtquant-mcp\repo\docs\OPERATIONS_RUNBOOK.md D:\xtquant-mcp\repo\docs\ACCEPTANCE_STANDARD.md D:\xtquant-mcp\repo\AGENTS.md D:\xtquant-mcp\repo\docs\WORKFLOW_AND_BOARD.md D:\xtquant-mcp\repo\docs\TEMPLATES.md`
   - 结果：`needs_fix` 仍只出现在 board/template 语义来源文件和 runbook 的 board-field 说明中；`AGENTS.md` 与 `ACCEPTANCE_STANDARD.md` 继续维持 formal verdict vocabulary，不存在新的混写。
3. `rg -n "truthful degraded|broker_blocked|G0 Hard Stops|G1 Hard Stops|G3 Hard Stops|G4 Hard Stops|Minimum Preflight|Startup Order" D:\xtquant-mcp\repo\docs\OPERATIONS_RUNBOOK.md`
   - 结果：当时的 `VAL-002` / `VAL-003` posture、startup/preflight/hard-stop 主体内容仍在，未因本次 follow-up 被破坏。
4. `Test-Path D:\xtquant-mcp\repo\docs\OPERATIONS_RUNBOOK.md`
   - 结果：`True`

## Known Risks

1. `ReviewPack` 模板本身仍使用 `Decision: pass | needs_fix | blocked`，而 `ACCEPTANCE_STANDARD.md` 的正式 verdict vocabulary 是 `pass`、`partial`、`blocked`、`fail_env`、`fail_design`；本次按用户要求未扩散修改模板文件，因此跨文档 contract 仍需后续治理卡统一收口。
2. 当时 `broker_blocked` 叙事下的真实根因尚未闭合；runbook 只能规范恢复与停止判断，不能替代新的 `VAL-004` 正式取证。
3. 当前文档若继续引用 2026-03-30 的 accepted `VAL-002` / blocked `VAL-003` posture，必须显式标为历史上下文，避免旧口径继续误导 operator。

## Needs Independent Test

1. test 需要确认 [OPERATIONS_RUNBOOK.md](../OPERATIONS_RUNBOOK.md) 不再把 `needs_fix` 作为 formal `ReviewPack` verdict，同时保留其 board / `Review Result` 字段说明。
2. test/review 需要确认 runbook 对 `G3 pass but G4 blocked` 的描述与最新 accepted `VAL-002` review、`VAL-003` TaskCard 和 [ACCEPTANCE_STANDARD.md](../ACCEPTANCE_STANDARD.md) 仍完全一致。
3. test/review 需要确认本次 follow-up 未破坏 startup/preflight/hard-stop/higher-gate recovery/backlink 结构，也未扩散修改到非 owned write scope。

## Historical Semantics Note

本 ChangePack 保留的是 `2026-03-30` 当时的 runbook follow-up 语义。当前读取时请注意：

1. 这里出现的 `broker_blocked` 是历史 blocker 名，不代表当前正式主 blocker 词。
2. 当前 higher-gate 入口、恢复流程和 stop rule 以 [OPERATIONS_RUNBOOK.md](../OPERATIONS_RUNBOOK.md) 与 [CURRENT_STATUS.md](../CURRENT_STATUS.md) 为准。

## Recommended Next Board Status

- 建议状态：`In Independent Test`
- 理由：本次 dev follow-up 已按 review finding 修正 runbook contract，并完成最小自检；但修正后的 formal verdict / board enum 边界仍需要独立测试复核，之后才能重新进入审查。
