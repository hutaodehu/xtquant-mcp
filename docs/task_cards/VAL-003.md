Task ID: VAL-003
Title: `order.place` 受控最小真单验证
Type: investigation
Priority: P1
Owner Role: test
Current Role: review
Status: Blocked
Blocking Reason: data_gateway_port_conflict_nonrepo_listener_8766
Repo Spec Link: [MCP_DESIGN.md](../MCP_DESIGN.md)
Acceptance Gate: G4
Change Package Link: [VAL-003.md](../change_packages/VAL-003.md)
Evidence Pack Link: [VAL-003-test-202604110030-controller-direct-live.md](../evidence_packs/VAL-003-test-202604110030-controller-direct-live.md)
Review Pack Link: [VAL-003-review-202604110034.md](../reviews/VAL-003-review-202604110034.md)
Env Snapshot Link: [VAL-003-202604110030-controller-direct-live.md](../env_snapshots/VAL-003-202604110030-controller-direct-live.md)
Verifier: Governed-write G4 test role
Merge Owner: 主控
Review Result: blocked
Depends On: TG-001, VAL-002
Lane: validation
Risk Class: high
Write Scope: validation.trade_live_write_smoke
Automation Policy: manual_gate
Execution Class: test_only
Controller Test Policy: controller_direct_required
Execution Packet Side: BUY
Execution Packet Symbol: 515880.SH
Execution Packet Qty: 100
Execution Packet Price Mode: l1_protect
Execution Packet Cancel Timeout: 30s
Trade Config Path: D:\xtquant-mcp\instance\prod\config\trade_gateway.local.yaml
Data Config Path: D:\xtquant-mcp\instance\prod\config\data_gateway.local.yaml
Trade Health URL: http://127.0.0.1:8765/healthz
Data Health URL: http://127.0.0.1:8766/healthz

## Goal

在受控风险下执行 `order.place` 的最小真单验证，证明正式对外写路径已经真正经过服务端 gate 并具备完整审计追踪。

## Scope In

1. 按 `G0 -> G4` 执行最小真单验证。
2. 覆盖 `order.place`、`order.status`、`orders.list`、`order.cancel`、`fills.list`。
3. 记录 broker 侧标识、审计 artifact、`trace_id`、时间戳和环境快照。

## Scope Out

1. 不扩大验证规模，不做策略级交易验证。
2. 不把市场关闭、券商权限缺失或桌面问题误写成设计通过。

## Done Means

形成正式 `EvidencePack` 与 `EnvSnapshot`，并对 `order.place` 的唯一受控写路径给出明确 verdict；若无法执行，也必须保持 `blocked` / `fail_env` / `fail_design` 的严格分类。

## Notes

本卡是首轮最高风险验证卡。执行前必须确认交易窗口、账户权限、最小数量与撤单策略均已明确。

2026-03-30 controller repo-only reconcile 曾基于最新 accepted `VAL-002/G3` 工件把本卡镜像状态更新为 `Blocked / broker_blocked`：当时只证明了 truthful degraded read contract 成立，没有证明 broker fresh connect 或 `order.place` 的 higher-gate 写路径前置条件已经恢复。

2026-04-02 `14:20 +08` 的正式 Round 2 `EvidencePack` 与 `ReviewPack` 已关闭此前 live-window `session_not_ready` / native `connect=-1` 阻断，并建议把本卡返回 `In Independent Test`，等待 fresh Round 3 governed-write judgment；当时仍未执行 `order.place`，因此不能写 task-level `G4 pass`。

2026-04-03 `13:29 +08` 的正式 Round 3 `EvidencePack` 与 `ReviewPack` 已把本卡从“等待 fresh Round 3 judgment”推进到新的 reviewed `Blocked` truth：`order.place` 在 `13:15:48 +08` 被真实执行一次，但在 governed write 内以 `status=risk_rejected`、`code=connect_gate_failed`、`broker_order_id=""` 收口；同一 attempt 的 `connect_gate` 对 `session_id=2111` 给出 `0, -1, -1, -1, -1` 的采样序列，`success_rate=0.2 < 0.9`，因此本卡当前镜像状态改为 `Blocked / connect_gate_failed`，而不是继续停留在 `In Independent Test`。

2026-04-03 `14:10 +08` 的 fresh retry `EvidencePack` 与独立 `ReviewPack` 进一步强化了同一环境阻断：在 `13:54:37 +08` 完成 fresh preflight 后，`order.place` 于 `13:56:17 +08` 再次真实执行，但仍以 `status=risk_rejected`、`code=connect_gate_failed`、`broker_order_id=""` 收口；这次 `connect_gate` 对 `session_id=2111` 的 5 次采样全部为 `-1`，`success_rate=0.0 < 0.9`，比 `13:15` 那次更差。该 review 明确结论仍为 `blocked / fail_env`，并指出当日第 3 单 live retry 不再具有证据增量价值。

2026-04-03 `14:47 +08` 的 fresh pre-close `EvidencePack` 与独立 `ReviewPack` 继续强化了同一环境阻断：当轮由主控真实执行的 live test packet 在 `14:43:27 +08` 再次触发 `order.place`，但仍以 `status=risk_rejected`、`code=connect_gate_failed`、`broker_order_id=""` 收口；同一 call 的 `connect_gate` 对 `session_id=2111` 再次给出 `0/5` 的 `-1` 采样序列。该 review 明确保持本卡为 reviewed `Blocked`，执行分类仍是 `fail_env`，并认定不应继续消耗当日剩余 live retry。

当前准备好的最小验证包固定为：`BUY 515880.SH`、`100股`、`price_mode=l1_protect`、`cancel_timeout=30s`；这些字段现在以 TaskCard 结构化 header 为准，不再依赖正文口头约定。

当前默认的执行组织方式已升级为：`controller-only` 的 reconcile / judgment 循环，加上在本卡显式 `Controller Test Policy: controller_direct_required` 条件下由主控手动触发 `scripts/run_controller_direct_test.ps1`。这不是第三种主控模式，也不会把任务变成普通 `dispatchable`；若没有新的环境恢复 formal truth 或 gateway recovery 不通过，controller 仍只允许写 no-go judgment / repo-only sync。

`2026-04-03 14:43 +08` 的 fresh pre-close packet 现在只保留为历史 baseline evidence，不再是当前 formal truth 或当前 baseline；其文件名里的 `override` 只反映当时命名，不代表新的临时治理分支。后续同类运行统一走当前任务卡字段 + controller runner + 独立 ReviewPack 的正式 contract。正式执行计划见 [VAL-003_G4_EXECUTION_PLAN.md](../VAL-003_G4_EXECUTION_PLAN.md)。

`2026-04-07 14:04 +08` 的 controller-direct live packet 再次真实执行了 governed `order.place`，但仍在 same-call pretrade `connect_gate` 里以 `session_id=2111` 的 `0,-1,-1,-1,-1` 采样序列收口为 `connect_gate_failed`，没有拿到 `broker_order_id`。更重要的是，这次 packet 把一个执行层错误暴露得很清楚：`session.warm/session.status` 的 owner session 是 `1111`，native probe 只验证了 `100/101`，而 write-side `connect_gate` 落到了 `2111`。后续 reopen 前，必须先把 Round 2 probe 与 Round 3 write 收口到同一套 session 解析语义；`100/101` probe pass 不再允许被单独解释成写路径 ready。

当前 controller-direct runner 已进一步收口三项执行层耦合：Round 2 native probe session 集必须跟随 preflight `session_resolution.effective_session_plan`；native probe 的 `--user-data-path` 必须从 `Trade Config Path` 对应实例配置或 `miniqmt.ensure_logged_in` / `diag://login/latest` 暴露的 `evidence.qmt_userdata` 动态解析；Round 2 现在固定先执行 `session.close -> cooldown -> fresh native probe` 的 clean-window 步骤，若 fresh native probe 仍失败才允许进入受控 host recovery。下一个开盘窗口的 fresh packet 只允许沿用这套 runner 重新执行 Round 1 -> Round 3。

`2026-04-08 10:13 +08` 的 fresh 独立 `ReviewPack` 已正式覆盖 `2026-04-07 14:04 +08` 这批 formal test artifacts，并继续把本卡保持为 reviewed `Blocked`：执行分类仍为 `fail_env`，但 `1111 / 100,101 / 2111` 的 session-plan split 已不再只是解释性环境现象，而被明确上升为“reopen authority 的 material design/contract blocker”。因此本轮 controller-with-delegation 收口不再授权新的 live `order.place`；后续 reopen 只有在 Round 2 native probe 与 Round 3 write 绑定到同一套 canonical session plan，并形成 fresh formal evidence 后才允许重新判断。

`2026-04-11 00:30 +08` 的 fresh controller-direct packet 又形成了一轮新的正式 baseline，但这次停点已经前移到 gateway recovery：trade wake 为 `started` 且 health 正常，data wake 则返回 `status=port_conflict`。随后 `2026-04-11 00:34 +08` 的 root cause 取证确认 `8766` 被 `PID 26480` 占用，进程命令行为 `\\wsl.localhost\Ubuntu-22.04\home\yun\qlib\scripts\..\scripts\run_xtdata_gateway.py --transport streamable-http --host 127.0.0.1 --port 8766 --path /mcp`，且 `http://127.0.0.1:8766/healthz` 返回 `404 Not Found`，说明它不是当前 repo 期望的 `xtqmtDataGateway`。因此本轮 `order.place` 未执行，当前任务级 blocker 必须统一为 `data_gateway_port_conflict_nonrepo_listener_8766`；旧 `connect_gate_failed` 只能保留为历史 baseline，不能再充当当前 formal truth。

`2026-04-09 14:13 +08` 的 controller-direct runtime capture 还提供了当前最新一轮“已显式收口 canonical session plan”的 machine-readable packet 事实：preflight effective session plan=`2111,2100,2101`，preflight same-plan verdict=`True`，native probe same-plan verdict=`True`，但 native probe `overall_ok=False`、host recovery `ok=False`、`fresh_connect_verified=False`，因此它只能证明 session-plan 语义已经固定到同一套 canonical plan，不能证明 Round 2 已完成，更不能证明 write authority 恢复。若后续文档使用 `probe_complete_verdict` 这个收口词，它在本卡中的含义应固定为“当前 packet 的 Round 2 bounded native probe 是否在 canonical session plan 上完成”；对 `2026-04-11 00:30 +08` 这轮 fresh blocked packet，该 verdict 并未产生，因为 runner 在 gateway recovery 就停止了。

当前仓内已新增 `scripts/check_packet_readiness.ps1`，并由 `scripts/run_controller_direct_test.ps1` 在 Round 1/2 hard-stop 后消费它产出的 machine-readable `go/no_go`、`no_go_reason` 与 `session_plan_version`。这意味着 runtime payload 侧已经开始显式回收 `session_plan_version`，但 reviewed formal artifacts 仍要等下一轮 fresh packet/ReviewPack 才能把这些字段正式写进 current truth。

`2026-04-13` 的最新 live runtime 推进已经进一步把 blocker 收敛：clean recovery + 重新 UI 登录后，`session.warm` 与 `session.status` 已能稳定回到 `session_id=2101`，说明凭据 target、`up_queue_xtquant`、session-plan split 这些旧问题已不再是当前主阻断；但最新顺序化 `probe.connection` 仍在 `session_id=2101` 上返回 `reason=write_connect_failed`、`fresh_connect_verified=false`、`write_authority_ready=false`。因此当前卡点已明确缩小为 broker fresh connect 本身；在形成新的 fresh packet / ReviewPack 前，这些 runtime 进展只能视为 reopen 准备度提升，不能改写本卡的 reviewed `Blocked` posture。

