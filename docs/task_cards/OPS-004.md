Task ID: OPS-004
Title: spec-task-harness ReviewPack `Decision` 反引号兼容解析
Type: bug
Priority: P1
Owner Role: review
Current Role: review
Status: Accepted
Blocking Reason:
Repo Spec Link: [MCP_DESIGN.md](../MCP_DESIGN.md)
Acceptance Gate: G0
Change Package Link: [OPS-004.md](../change_packages/OPS-004.md)
Evidence Pack Link: [OPS-004-test-202603301804.md](../evidence_packs/OPS-004-test-202603301804.md)
Review Pack Link: [OPS-004-review-20260330180749.md](../reviews/OPS-004-review-20260330180749.md)
Env Snapshot Link: N/A
Verifier: Harness review-decision parser verifier
Merge Owner: 主控
Review Result: pass
Depends On: OPS-003
Lane: ops
Risk Class: medium
Write Scope: ops.review_pack_parsing,ops.controller_tooling
Automation Policy: auto_safe
Execution Class: handoff_required

## Goal

修复 spec-task-harness 对 formal ReviewPack 中带反引号 `Decision` 行的解析兼容性，让 latest review 已经被正确选中时，controller 也能恢复出真实 `review_decision`，不再把任务错误停留在 `tested_local` / `review_needed`。

## Scope In

1. 扩展 harness 对 ReviewPack `Decision` 行的解析兼容面，至少覆盖模板化的 ``- Decision: `pass``` / ``needs_fix`` / ``blocked`` 样式。
2. 保持 `collect_artifacts.py`、`reconcile_runledger.py`、`select_next_safe_tasks.py` 的外部 JSON contract 不变，只修 review-decision 提取与下游本地阶段恢复。
3. 增加回归测试，证明 `VAL-002` 这类 latest review 已选中但 `Decision` 带反引号的 formal artifact 不再被误判为 `review_needed`。

## Scope Out

1. 不改 board sync contract 或看板字段语义。
2. 不批量重写历史 ReviewPack 文本；以解析兼容为主，而不是回写历史工件。
3. 不在本卡内扩展 latest artifact 排序逻辑；该问题已由 `OPS-003` 单独收口。

## Done Means

当 latest formal ReviewPack 已经被正确选中时，controller 能稳定提取 `Decision`，从而让 repo-only reconcile 正确恢复 `reviewed_pass_local` / `reviewed_needs_fix_local` / `reviewed_blocked_local`，不再因为反引号模板样式而卡在 `tested_local`。

## Notes

本卡是 `OPS-003` 的 follow-up residual，不得把“ReviewPack 决策可解析”混写成新的排序修复或 board 语义变更。
