Task ID: VAL-004
Title: broker/session `connect=-1` higher-gate blocker 根因取证
Type: investigation
Priority: P0
Owner Role: review
Current Role: review
Status: Accepted
Blocking Reason:
Repo Spec Link: [MCP_DESIGN.md](../MCP_DESIGN.md)
Acceptance Gate: G3
Change Package Link: [VAL-004.md](../change_packages/VAL-004.md)
Evidence Pack Link: [VAL-004-test-202603311831-post-login-rerun.md](../evidence_packs/VAL-004-test-202603311831-post-login-rerun.md)
Review Pack Link: [VAL-004-review-202603311840.md](../reviews/VAL-004-review-202603311840.md)
Env Snapshot Link: [VAL-004-test-202603311831-post-login-rerun.md](../env_snapshots/VAL-004-test-202603311831-post-login-rerun.md)
Verifier: Broker/session blocker isolation verifier
Merge Owner: 主控
Review Result: pass
Depends On: VAL-002
Lane: validation
Risk Class: medium
Write Scope: validation.trade_broker_blocker,validation.host_log_correlation
Automation Policy: manual_gate
Execution Class: test_only

## Goal

把当时的 `broker_blocked` higher-gate 叙事收敛成可执行结论，明确下一步应继续按环境阻断处理，还是已经具备新 trade refactor 卡的证据门槛。

## Scope In

1. 在同一 host、同一 bundle、同一实例目录条件下，对齐 gateway rerun、native probe 和 bounded host-log 三类证据。
2. 明确拆开两类结论：
   - 已证实的宿主侧环境现象
   - 尚未闭合的根因假设
3. 产出正式 `EvidencePack`、`EnvSnapshot` 和 `ReviewPack`，给出后续分支建议。

## Scope Out

1. 不执行 `order.place`，不启动 `VAL-003`。
2. 不把 `queryNodeInfo` / `m_bInstantMode` 的同日上下文直接升级为单点修复结论。
3. 不把 `VAL-002/G3` 的 truthful degraded read success 外推为 broker fresh connect 或 `G4` ready。

## Done Means

本卡输出的正式工件能清楚回答三件事：

1. 当时 `broker_blocked` 叙事对应的 higher-gate blocker 是否仍应继续按 `fail_env` / `blocked` 处理。
2. 哪些现象已经被证实，哪些仍只是根因假设。
3. 是否已经有足够证据新开具体 trade refactor follow-up 卡，而不是继续混写成单一 blocker narrative。

## Notes

本卡是 higher-gate blocker 的隔离取证卡，不是写路径实现卡，也不是 `G4` 验收卡。

2026-03-31 最新 ReviewPack 已正式收口本卡：旧的“未登录导致 broker/session 未就绪” blocker 已被登录后复跑关闭；当时 first blocker 已转为 public `orders.list` 路径的 `fail_design`，后续实现跟进改由 [TG-005.md](./TG-005.md) 承接。

历史语义提示：

1. 上述描述是 `2026-03-31` 当时的阶段性结论，不代表当前 task-level posture。
2. [VAL-003.md](./VAL-003.md) 的当前正式状态应以 [CURRENT_STATUS.md](../CURRENT_STATUS.md) 和更晚 `ReviewPack` 为准；它已经不再使用旧的 `broker_blocked` 作为当前主 blocker，而是 reviewed `Blocked / connect_gate_failed`。
