# v2.0.0-alpha.1 验证记录

## 测试命令

| 范围 | 命令 | 结果 |
| --- | --- | --- |
| runtime port 配置 | `python -m unittest tests.test_trade_gateway_config` | 通过，1 test |
| MiniQMT 登录识别 | `python -m unittest tests.test_miniqmt_desktop_harness` | 通过，6 tests |
| market data tick | `python -m pytest tests/test_market_data_tick_payload.py -q` | 通过，3 tests |
| Data Gateway | `python -m pytest tests/test_data_gateway_config.py tests/test_data_gateway_server.py tests/test_data_gateway_service.py tests/test_wake_data_gateway_script.py -q` | 通过，67 tests；存在非阻断后台线程 warning |
| Trade Gateway session | `python -m pytest tests/test_trade_gateway_bootstrap.py tests/test_trade_gateway_server.py tests/test_trade_gateway_session_manager.py tests/test_trade_ops_warm_health.py -q` | 通过，45 tests |
| broker/order/fill truth | `python -m pytest tests/test_trade_gateway_fills.py tests/test_trade_write_authority.py tests/test_trade_order_submission_contract.py tests/test_trade_probe_readiness_split.py -q` | 通过，35 tests |
| controller helper | `python -m pytest tests/test_controller_direct_authority_refresh.py -q` | 通过，2 tests |
| 最终编译检查 | `PYTHONPYCACHEPREFIX=/tmp/xtquant-mcp-pycache python3 -m compileall xtqmt_mcp scripts` | 通过 |
| 公开候选编译检查 | `PYTHONPYCACHEPREFIX=/tmp/xtquant-mcp-public-pycache python3 -m compileall xtqmt_mcp scripts` | 通过 |
| 公开候选全量 unittest | `env -u WSL_DISTRO_NAME -u WSL_DISTRO PYTHONPYCACHEPREFIX=/tmp/xtquant-mcp-public-pycache /tmp/xtquant-mcp-public-venv/bin/python -m unittest discover -s tests -v` | 通过，175 tests |
| 公开 flow-smoke 脚本烟测 | `PYTHONPYCACHEPREFIX=/tmp/xtquant-mcp-public-pycache /tmp/xtquant-mcp-public-venv/bin/python scripts/run_trade_flow_smoke.py --config configs/trade_gateway.flow_smoke.yaml --output-json /tmp/xtquant-mcp-flow-smoke.json` | 通过，生成非生产 flow-smoke 报告 |
| 版本元数据复核 | `python - <<PY ...` 校验 `pyproject.toml`、示例配置和 `xtqmt_mcp.__version__` | 通过，均为 `2.0.0a1` |

## 扫描记录

| 范围 | 结论 |
| --- | --- |
| Git 历史 | 远端基线已命中敏感模式，结论为 `sanitized_public_history_required` |
| 暂存区功能提交 | 每组提交前均执行新增行敏感模式扫描 |
| 文档提交 | 真实路径、真实标的和真实 broker id 已脱敏 |
| 归档 | 未跟踪、ignored 运行态和已跟踪私有证据已移动至私有归档并生成 sha256 清单 |
| 公开候选路径扫描 | 真实本机路径、真实 Windows 用户目录、真实账号和私有 WSL 路径扫描无命中 |
| 公开候选残留脱敏 | controller-direct 示例路径改为 `C:/xtquant-mcp-example/...`，flow-smoke 示例标的改为 `SAMPLE001.SH`，flow-smoke 运行产物路径改为 `.tmp/trade_flow_smoke/...` |
| 公开候选提交 | 以 `git rev-parse --short HEAD` 为准，提交信息为 `chore(release): create sanitized v2.0.0-alpha.1 public baseline` |

## 已知限制

1. 当前分支历史不能直接公开；需要脱敏公开历史或新公开仓。
2. Data Gateway 全量 unittest 通过，但其中一个后台下载线程在测试临时目录清理后写 job 状态时打印非阻断异常；主测试结果仍为 OK。
3. 当前环境默认 `python` / `python3` 未安装 pytest，因此公开候选最终采用临时 venv 的 `unittest discover` 验证。
4. 已跟踪证据目录和内部计划/规格已从公开 baseline 删除，只保留 README stub；原文位于私有归档。
5. 本记录没有执行真实交易、远端推送、tag 或 GitHub release。
